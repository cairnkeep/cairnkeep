#!/usr/bin/env node

import { main } from "../scripts/cairn-cli.mjs";

main(process.argv.slice(2)).catch((error) => {
  console.error(`cairn: ${error instanceof Error ? error.message : String(error)}`);
  process.exitCode = 1;
});
