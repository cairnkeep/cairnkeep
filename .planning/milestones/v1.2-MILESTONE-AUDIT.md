---
milestone: 1.2
milestone_name: Context Exploration
audited: 2026-07-06
status: passed
closeout_readiness: ready
scores:
  requirements: 7/7
  phases: 4/4
  integration: 7/7
  flows: 1/1
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 07-context-explore-mcp-tool
    items:
      - "T-07-06 / Pitfall #1: an unreachable-but-configured endpoint yields exit 0 + empty Evidence, indistinguishable from a genuine empty result. Mitigated by transparency only (renderCitations surfaces stats.turns/tool_calls). Accepted residual — full closure would mean re-implementing the explorer loop."
      - "runCommand 12,000-char stdout truncation (pre-existing): a very large Evidence payload can truncate mid-JSON and be reported as ok:false ('malformed Evidence JSON'). Fail-closed, never a silent wrong-success, but a real limit for large result sets."
  - phase: 09-live-verification-a-b-token-savings
    items:
      - "Small-model unreliability on broad/loosely-worded queries (D-01): the 4B FastContext quant wanders to the turn cap or fabricates non-existent paths. Reliable on specific queries; disclosed transparently in 09-AB.md and explicitly excluded from the CTX-07 headline."
      - "A/B harness self-referential contamination (info): the harness header and 09-AB.md contain the literal grep patterns as prose, so a later --native broad-set run can pick up the doc's own text as extra hits (observed 33-byte drift). Does not affect the tight-query headline or the PASS verdict."
nyquist: skipped_capability_inactive
---

# Milestone v1.2 — Context Exploration — Audit

**Audited:** 2026-07-06
**Status:** ✅ **passed**
**Phases:** 6–9 (4/4 complete, all verified)

All 7 requirements satisfied. Cross-phase integration verified clean. The end-to-end `/context-explore` flow connects start to finish in committed code. No critical gaps, no unsatisfied or orphaned requirements. Only pre-accepted, documented, non-blocking tech debt remains.

## Requirements Coverage (3-source cross-reference)

Each requirement was cross-referenced against three independent sources: the REQUIREMENTS.md traceability table (`[x]`/status), each phase's VERIFICATION.md status, and each phase SUMMARY.md's `requirements-completed` frontmatter.

| REQ-ID | Phase | VERIFICATION | SUMMARY frontmatter | REQUIREMENTS.md | Final |
|--------|-------|--------------|---------------------|-----------------|-------|
| CTX-01 | 7 | passed | listed | `[x]` Complete | ✅ satisfied |
| CTX-02 | 7 | passed | listed | `[x]` Complete | ✅ satisfied |
| CTX-03 | 7 | passed | listed | `[x]` Complete | ✅ satisfied |
| CTX-04 | 8 | passed | listed | `[x]` Complete | ✅ satisfied |
| CTX-05 | 8 | passed | listed | `[x]` Complete | ✅ satisfied |
| CTX-06 | 6 | passed | listed | `[x]` Complete | ✅ satisfied |
| CTX-07 | 9 | passed | listed | `[x]` Complete | ✅ satisfied |

**Coverage: 7/7 satisfied.** No unsatisfied, partial, or orphaned requirements. (CTX-04's live-invocation behavior, marked PRESENT_BEHAVIOR_UNVERIFIED in Phase 8, was deliberately deferred to and discharged by Phase 9's live SC-3 run.)

## Phase Verification Summary

| Phase | Name | Verification | Score | Notes |
|-------|------|--------------|-------|-------|
| 6 | FastContext Reliability Spike | ✅ passed | 9/9 | GO verdict: 15/15 turns real `tool_calls`, 0 narration, `--full` exit 0. CR-01 (latent false-NO-GO on parallel calls) fixed + live re-run, verdict unchanged. |
| 7 | context_explore MCP Tool | ✅ passed | 4/4 SC | SC-1 discharged live (07-UAT.md). Full `test:smoke` green (10/10 explore-guard checks). CTX-03 grep audit clean. |
| 8 | Operating-Layer Wiring | ✅ passed | 5/6 | One item (live invocation) deferred by design to Phase 9. Non-blocking REQUIREMENTS.md checkbox drift resolved at close. |
| 9 | Live Verification + A/B | ✅ passed | 10/10 | ~99.9% byte-savings on verified pinpoint queries (D-03 PASS). Broad-set model unreliability disclosed transparently. |

## Cross-Phase Integration (integration checker: clean)

Static wiring check across all committed integration seams:

| Seam | Verdict | Evidence |
|------|---------|----------|
| P7→P8 tool-name wiring | ✅ PASS | `index.ts:1001` registers `context_explore`; Claude `mcp__cairn-memory__context_explore` and OpenCode `cairn-memory_context_explore` both match exactly — no drift |
| `repo_root` contract | ✅ PASS | Tool schema accepts optional `repo_root` (`index.ts:1004-1008`); both commands resolve via `git rev-parse --show-toplevel` and pass it explicitly; server falls back to `CAIRN_EXPLORE_REPO_ROOT`, resolves absolute |
| Env-var consistency | ✅ PASS | `CAIRN_EXPLORE_BINARY`/`CAIRN_EXPLORE_REPO_ROOT` identical across `index.ts`, `verify-token-savings-ab.sh`, `docs/operating.md` |
| Installer correctness | ✅ PASS | `sync-opencode-explore-assets.sh` installs exactly `command/context-explore.md`, `--apply` self-verifies via `--check`; Claude command auto-discovered by `sync-claude-assets.sh` glob |
| CTX-03 provider-neutrality | ✅ PASS | No endpoint/model/API-key/private-host committed on the explore surface (unrelated pre-existing keys belong to other features, outside the tool/command/A/B code) |
| E2E flow | ✅ PASS | `/context-explore` → git rev-parse → MCP `context_explore` → fail-closed validation → `runCommand(token_miser explore …)` → parse Evidence → `renderCitations()` → citations surfaced. No break in committed chain |
| Smoke/CI | ✅ PASS | `check:explore-guard` chained into `test:smoke`; guard file exercises registration + all fail-closed tiers fully offline |

**No cross-phase gaps, broken flows, or config drift found.**

## Tech Debt (non-critical, pre-accepted)

All items below were surfaced and explicitly accepted during phase verification. None block milestone close.

**Phase 7:**
- T-07-06 / Pitfall #1 — unreachable-but-configured endpoint → exit 0 + empty `Evidence`, indistinguishable from a genuine empty result. Mitigated by transparency (empty-citation text surfaces `stats.turns`/`stats.tool_calls`).
- `runCommand` 12,000-char stdout truncation (pre-existing) — a very large `Evidence` payload can truncate mid-JSON and report `ok:false`. Fail-closed, but a real limit for large result sets.

**Phase 9:**
- Small-model unreliability on broad/loosely-worded queries (D-01) — the 4B quant wanders or fabricates non-existent paths. Disclosed in 09-AB.md; explicitly excluded from the CTX-07 headline (which uses verified pinpoint queries).
- A/B harness self-referential contamination (info) — the harness/doc contain the literal grep patterns as prose, causing a small `--native` re-run drift (observed 33 bytes). No effect on the headline or verdict.

## Nyquist Compliance

**Skipped** — the Nyquist `validate-phase` capability is inactive for this project (no active `verify:post` step hook). VALIDATION.md files exist for phases 6–8 as drafts but are not scored, per the audit workflow's skip rule.

## Verdict

**passed** — v1.2 achieved its definition of done. 7/7 requirements satisfied, 4/4 phases verified, integration clean, E2E flow connected, and the milestone's value proposition proven with a live measured number. Ready for `/gsd-complete-milestone 1.2`.
