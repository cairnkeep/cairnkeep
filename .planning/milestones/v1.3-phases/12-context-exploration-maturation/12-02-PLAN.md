---
phase: 12-context-exploration-maturation
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - mcp-memory-server/src/index.ts
  - mcp-memory-server/scripts/smoke-explore-crossref.mjs
  - mcp-memory-server/package.json
autonomous: true
requirements: [CTX-08]

must_haves:
  truths:
    - "A citation whose path stem (basename minus extension, >= 4 chars) case-insensitively matches a project-memory entry key/value gets a memory_refs annotation; one matching a wiki page name/content gets a wiki_refs annotation"
    - "A citation with no memory/wiki match gets NO annotation — no memory_refs, no wiki_refs, no extra rendered text"
    - "A result with ZERO total cross-ref hits renders byte-identical to the pre-phase renderCitations() output (D-03) — verify-token-savings-ab.sh's shape gate stays green"
    - "Cross-refs read the EXPLORED repo_root's .agentfs/project.db and .planning/wiki/sources — NOT the MCP server process's own cwd (D-04, Pitfall 2)"
    - "A missing .agentfs db, missing wiki dir, or any enrichment error returns the result WITHOUT cross-refs and never fails or degrades the exploration result itself (D-04 fail-open)"
    - "Cross-refs recompute on every return — cache hit AND cache miss (D-12) — never served from the cache"
  artifacts:
    - "mcp-memory-server/src/index.ts — cwd-threaded openScope/listEntries + cross-ref enrichment inside runContextExplore + rendering that appends markers only on hits"
    - "mcp-memory-server/scripts/smoke-explore-crossref.mjs — seeded-fixture proof (memory hit, wiki hit, no-match silence, zero-hit byte-identity)"
  key_links:
    - "listEntries('project', '', { cwd: resolvedRepoRoot }) — the cwd passthrough is load-bearing; without it cross-refs silently read the server's own cwd and always miss (Pitfall 2)"
    - "Enrichment runs AFTER the JSON.parse (hit or miss), BEFORE payload/renderCitations shaping (D-01, D-12)"
    - "Wiki reads confined to repo_root/.planning/wiki/sources via the isContained relative()-based idiom; citation paths derive only a search stem, never a read path (Security V12)"
---

<objective>
Deliver CTX-08: `context_explore` cross-references each citation against the
explored repo's project memory and wiki, flagging which cited ranges have related
memory/wiki context. Enrichment runs in-process inside `runContextExplore` after
the Evidence JSON parse (D-01) — token_miser is untouched. Matching is
deterministic path-stem substring matching mirroring `memory-recall.sh` (D-02):
no embeddings for paths, so the result is verifiable.

Purpose: The injected auto-invoke context (Plan 03) is only trustworthy because
cross-refs surface what memory already knows (Phase 12 success criterion 1). The
output contract is preserved: a zero-hit result renders identically to today's
output (D-03), so the Phase 9 A/B shape gate keeps passing.

Output: cwd-threaded memory reads + a cross-ref matcher + hit-only rendering in
`index.ts`, and an offline seeded-fixture smoke test.
</objective>

<execution_context>
@/home/stondo/PARA/Projects/cairnkeep/.claude/gsd-core/workflows/execute-plan.md
@/home/stondo/PARA/Projects/cairnkeep/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-context-exploration-maturation/12-CONTEXT.md
@.planning/phases/12-context-exploration-maturation/12-RESEARCH.md
@.planning/phases/12-context-exploration-maturation/12-PATTERNS.md
@.planning/phases/12-context-exploration-maturation/12-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols this plan creates (exclude from source-drift acknowledgement):
- Optional `cwd?: string` parameter added to `openScope` and to `listEntries`'s options object in `index.ts`
- A cross-ref enrichment function in `index.ts` (e.g. `crossReferenceCitations(citations, repoRoot)`; exact name = executor discretion) returning per-citation `memory_refs` / `wiki_refs`
- `memory_refs` and `wiki_refs` optional fields on citation objects in the `context_explore` structuredContent payload
- `mcp-memory-server/scripts/smoke-explore-crossref.mjs` (new)
- `check:explore-crossref` npm script in `mcp-memory-server/package.json` (new)
Note: `runContextExplore`, the `explore` CLI, `cached`, and explore-cache.ts are produced by Plan 01, not this plan.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Thread cwd through openScope/listEntries</name>
  <files>mcp-memory-server/src/index.ts</files>
  <read_first>
    - mcp-memory-server/src/index.ts (resolveScopePath at 135-163 — ALREADY takes cwd, defaults process.cwd(); openScope at 181-193 — calls resolveScopePath(scope) WITHOUT cwd; listEntries at 215-236 — options object, no cwd; all other callers of openScope/listEntries must keep working with no cwd)
    - .planning/phases/12-context-exploration-maturation/12-PATTERNS.md (cwd-threading section, lines 144-179)
    - .planning/phases/12-context-exploration-maturation/12-RESEARCH.md (Pattern 2, Pitfall 2)
  </read_first>
  <action>
    Add an optional `cwd?: string` parameter to `openScope` (signature becomes
    `openScope(scope, create, cwd?)`) and pass it into its `resolveScopePath(scope, cwd)`
    call (it currently omits the second arg, silently defaulting to `process.cwd()`).
    Add `cwd?: string` to `listEntries`'s `options` object and forward it as the third
    arg to its internal `openScope(scope, false, options.cwd)` call. Both parameters are
    OPTIONAL and default to today's behavior — every existing caller (memory_read,
    wakeup, etc.) that passes no cwd must be byte-for-byte unaffected. This is the small,
    load-bearing change Pitfall 2 flags: without it, cross-ref reads hit the server
    process's own working directory instead of the explored repo_root and always miss.
  </action>
  <verify>
    <automated>cd mcp-memory-server && npm run build && npm run check:explore-guard && npm run check:search</automated>
  </verify>
  <acceptance_criteria>
    - openScope's signature accepts an optional cwd and passes it to resolveScopePath(scope, cwd)
    - listEntries's options object accepts cwd and forwards it to openScope
    - Both params are optional; existing callers compile and pass no cwd (default preserved)
    - `npm run check:search` and `npm run check:explore-guard` still pass (no regression to existing memory reads)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>openScope and listEntries accept an optional cwd that threads to resolveScopePath; existing no-cwd callers are unchanged; the memory smoke tests stay green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cross-ref matcher + enrichment wiring + hit-only rendering</name>
  <files>mcp-memory-server/src/index.ts</files>
  <read_first>
    - mcp-memory-server/src/index.ts (runContextExplore from Plan 01 — insert enrichment after the JSON.parse; renderCitations at 604-615 — the exact zero-citation and per-line format to preserve; listEntries now with cwd from Task 1)
    - claude/hooks/memory-recall.sh (stem derivation: basename, strip extension, skip <4 chars at lines 33-37; substring matching against the wakeup index and wiki grep at 41-66 — the deterministic matching semantics to mirror)
    - opencode/plugins/memory-recall.ts (isContained at lines 41-44 — relative()-based containment to reuse for wiki paths)
    - .planning/phases/12-context-exploration-maturation/12-CONTEXT.md (D-01, D-02, D-03, D-04)
    - .planning/phases/12-context-exploration-maturation/12-RESEARCH.md (Pitfall 3 recompute-every-return, Pitfall 4 byte-identity)
  </read_first>
  <behavior>
    - For citation src/foo.rs with a seeded project-memory entry whose key or value contains "foo": that citation gains memory_refs listing the matching entry key(s).
    - For a seeded wiki page under .planning/wiki/sources whose filename or content contains "foo": that citation gains wiki_refs listing the page name(s).
    - Citation src/bar.rs with no seeded match: no memory_refs, no wiki_refs, and its rendered line is exactly `src/bar.rs:5-9` (unchanged).
    - A stem shorter than 4 chars (e.g. a file named "io.rs" → stem "io") is skipped — no matching attempted (noise guard).
    - Zero total hits across all citations → renderCitations output is byte-identical to the pre-phase output for the same evidence.
    - Missing .agentfs db or missing wiki dir → citations returned with no refs; the ok:true result is otherwise unchanged (fail-open).
  </behavior>
  <action>
    Add an enrichment function (e.g. `crossReferenceCitations(citations, repoRoot)`)
    that, per D-02, derives each unique citation's stem (basename minus extension),
    skips stems < 4 chars, and computes: memory_refs = keys of project-memory entries
    (read via `listEntries("project", "", { cwd: repoRoot })` — the cwd from Task 1)
    whose key or value contains the stem case-insensitively; wiki_refs = names of
    `<repoRoot>/.planning/wiki/sources/*.md` pages whose filename or content contains
    the stem case-insensitively. List the wiki dir with a fixed `readdirSync`; confine
    each page path with the `isContained` relative()-based idiom (reuse the pattern, do
    not reinvent). Citation paths are used ONLY to derive the search stem, never
    concatenated into a read path (Security V12). Wrap the whole function in try/catch —
    any error (missing db, missing dir, read failure) returns citations with no refs and
    never throws (D-04 fail-open).

    Wire it into `runContextExplore` AFTER the Evidence JSON parse succeeds and BEFORE
    building the success payload — on BOTH the cache-hit and cache-miss paths (D-12:
    cross-refs recompute every return because memory/wiki evolve independently of repo
    HEAD; they are never cached). Attach memory_refs/wiki_refs to the citation objects
    in `structuredContent`.

    Update `renderCitations` (D-03) so that when a citation has memory_refs and/or
    wiki_refs it appends a compact marker to that citation's line (e.g. a trailing
    ` <- memory: <keys> - wiki: <pages>` segment using a distinct separator glyph of
    your choice). A citation with no refs renders EXACTLY as today. Critically: when
    NO citation has any ref, the whole output MUST be byte-identical to the current
    renderCitations output — do not add any header, footer, or "cross-refs: none" line
    (Pitfall 4; the Phase 9 verify-token-savings-ab.sh shape gate re-derives this exact
    output and will regress otherwise).
  </action>
  <verify>
    <automated>cd mcp-memory-server && npm run build && cd .. && bash scripts/verify-token-savings-ab.sh --self-test</automated>
  </verify>
  <acceptance_criteria>
    - index.ts contains an enrichment function that reads listEntries with { cwd: repoRoot } and reads .planning/wiki/sources
    - Enrichment is called inside runContextExplore after JSON.parse on both cache-hit and cache-miss paths
    - renderCitations appends a marker only for citations that have memory_refs or wiki_refs; ref-less citations are unchanged
    - `bash scripts/verify-token-savings-ab.sh --self-test` exits 0 (the render-shape self-test — proves zero-hit output is byte-identical, Pitfall 4)
    - The enrichment function skips stems < 4 chars and is wrapped in try/catch (fail-open, D-04)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Citations gain memory_refs/wiki_refs from deterministic stem matching against the explored repo's memory + wiki; ref-less and zero-hit outputs are byte-identical to today; enrichment recomputes every return and fails open.</done>
</task>

<task type="auto">
  <name>Task 3: Cross-ref smoke test + package.json wiring</name>
  <files>mcp-memory-server/scripts/smoke-explore-crossref.mjs, mcp-memory-server/package.json</files>
  <read_first>
    - mcp-memory-server/scripts/smoke-explore-guard.mjs (offline smoke pattern; how it opts CAIRN_EXPLORE_BINARY at a fixture and asserts structuredContent)
    - mcp-memory-server/scripts/fixtures/fake-tokenmiser-cited.sh (cites src/foo.rs and src/bar.rs — the two citations this test discriminates)
    - mcp-memory-server/src/index.ts (how the server opens a project scope: AgentFS.open on <cwd>/.agentfs/project.db — the seeding target; see openScope/resolveScopePath)
    - mcp-memory-server/package.json (test:smoke chain, lines 17-20)
  </read_first>
  <action>
    Create `mcp-memory-server/scripts/smoke-explore-crossref.mjs` (offline, no network).
    Build a temp repo_root: seed `<root>/.agentfs/project.db` via `AgentFS.open` +
    `kv.set` with an entry whose key or value mentions "foo" (matching src/foo.rs's
    stem), and write `<root>/.planning/wiki/sources/foo-notes.md` mentioning "foo".
    Point `CAIRN_EXPLORE_REPO_ROOT` (or the per-call repo_root arg) at the temp root and
    `CAIRN_EXPLORE_BINARY` at `fake-tokenmiser-cited.sh`. Set `CAIRN_EXPLORE_CACHE=0` so
    the test exercises enrichment on a fresh spawn deterministically. Drive the
    `context_explore` MCP tool (reuse smoke-explore-guard's withClient/callExplore) or
    the `explore` CLI. Assert: (1) the src/foo.rs citation has non-empty memory_refs AND
    wiki_refs in structuredContent; (2) the src/bar.rs citation has neither; (3) the
    rendered text contains a cross-ref marker on the foo line and the bar line is a plain
    `src/bar.rs:5-9`; (4) a second run against a temp root with NO seeded db/wiki returns
    both citations with no refs and rendered text byte-identical to the plain
    path:line-range listing (D-03/D-04 fail-open + zero-hit identity). Clean up temp dirs;
    exit non-zero on any failed assertion.

    Wire `check:explore-crossref` into `package.json`
    (`node scripts/smoke-explore-crossref.mjs`) and append it to the `test:smoke` chain
    next to check:explore-cache.
  </action>
  <verify>
    <automated>cd mcp-memory-server && npm run build && npm run check:explore-crossref && npm run test:smoke</automated>
  </verify>
  <acceptance_criteria>
    - mcp-memory-server/scripts/smoke-explore-crossref.mjs exists and seeds a temp .agentfs/project.db + .planning/wiki/sources fixture
    - It asserts src/foo.rs gets memory_refs + wiki_refs and src/bar.rs gets neither
    - It asserts the no-seed run returns citations with no refs and byte-identical plain rendering
    - `npm run check:explore-crossref` exits 0
    - package.json test:smoke chain includes check:explore-crossref
    - `npm run test:smoke` exits 0 (full offline chain green)
  </acceptance_criteria>
  <done>An offline seeded-fixture smoke test proves memory/wiki flags appear on a matching citation, are absent on a non-match, and that a no-seed run is byte-identical to plain output; wired into test:smoke.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| token_miser citation paths → filesystem reads | Citation paths come from the external binary's output; used only to derive a search stem, never a read path |
| explored repo_root → wiki dir reads | Wiki pages under repo_root/.planning/wiki/sources are read for content matching |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-04 | Tampering / Information Disclosure | wiki cross-ref path building | medium | mitigate | Fixed `readdirSync` of `repo_root/.planning/wiki/sources` + `isContained` relative()-based containment (reused from memory-recall.ts); citation path derives only a stem, never a read path (V12) |
| T-12-05 | Denial of Service | enrichment over a huge memory scope / many wiki pages | low | accept | Bounded by the existing project-scope size and a fixed maxdepth wiki listing; enrichment is synchronous but small; fail-open try/catch prevents a hang from degrading the result |
| T-12-06 | Tampering | reading the wrong repo's memory (cwd not threaded) | medium | mitigate | Task 1 threads cwd=repoRoot through openScope/listEntries; smoke test seeds a NON-cwd repo and asserts the cross-ref hits it (Pitfall 2) |

No package installs this phase — no T-12-SC supply-chain checkpoint required.
</threat_model>

<verification>
- `cd mcp-memory-server && npm run test:smoke` — full offline chain green (adds check:explore-crossref)
- `bash scripts/verify-token-savings-ab.sh --self-test` — render-shape self-test green (zero-hit byte-identity, Pitfall 4)
- `bash scripts/verify-docs-parity.sh` — no new src env keys introduced here, stays green
</verification>

<success_criteria>
- Cited ranges are flagged with related memory/wiki hits, surfaced alongside the citation (Phase 12 criterion 1)
- Zero-hit results render byte-identical to pre-phase output (D-03); enrichment reads the explored repo (D-04) and fails open; recomputes every return (D-12)
</success_criteria>

<output>
Create `.planning/phases/12-context-exploration-maturation/12-02-SUMMARY.md` when done
</output>
