---
phase: 11-self-consistency-public-positioning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-no-private-references.sh
  - scripts/verify-docs-parity.sh
autonomous: true
requirements: [SC-02, SC-03]
user_setup:
  - service: local-denylist
    why: "The guard's specific-term denylist (employer name, private repo names) must never be committed (D-06). The operator maintains it as an uncommitted local file. Optional for the cairnkeep tree (already clean); becomes mandatory for the token-miser scrub gate in Plan 02."
    env_vars:
      - name: CAIRN_GUARD_DENYLIST
        source: "Operator-created uncommitted file — one literal private/vendor term per line (employer name, private repo names). Kept outside the repo or gitignored; never committed. Unset → generic-only scan."

must_haves:
  truths:
    - "verify-no-private-references.sh exits 0 on the current cairnkeep tracked tree + commit log (no real private/vendor/AI-authorship references present)"
    - "verify-no-private-references.sh exits non-zero when CAIRN_GUARD_DENYLIST names a term that is present in the scanned tree (detection actually works — not a silent pass)"
    - "verify-no-private-references.sh exits non-zero when CAIRN_GUARD_DENYLIST is set but the referenced file is unreadable (fail-closed, D-06)"
    - "verify-docs-parity.sh correctly detects and names the current known drift (the context-explore command and the CAIRN_EXPLORE_BINARY/CAIRN_EXPLORE_REPO_ROOT keys missing from docs) and exits non-zero"
    - "verify-docs-parity.sh treats the check as one-directional: every CAIRN_*/MCP_HTTP_* env key read in mcp-memory-server/src is named in the docs; doc-only keys (e.g. CAIRN_GIT_PROVIDER, read outside the server) are NOT failures"
  artifacts:
    - "scripts/verify-no-private-references.sh (executable, --help works, verify-by-execution shape)"
    - "scripts/verify-docs-parity.sh (executable, --help works, verify-by-execution shape)"
  key_links:
    - "guard git grep self-excludes its own script + the phase-11 detector-documentation files so quoting a detector pattern is not itself a violation (D-07 scope preserved for real refs)"
    - "parity script scopes code-key extraction to mcp-memory-server/src/*.ts only (not scripts/), so script-local env vars like CAIRN_GUARD_DENYLIST/CAIRN_ROUTE_BINARY never register as drift"
---

<objective>
Build the two verify-by-execution gate scripts this phase depends on: the
no-private-references guard (SC-03, D-05/D-06/D-07) and the docs-vs-code parity
checker (SC-02, D-10). Both follow the established `scripts/verify-*.sh` shape.

These are the phase's Wave 0 gaps: the guard must exist before the token-miser
publish (it gates the publish tree, Plan 02); the parity checker mechanizes the
docs-drift proof (Plan 03 makes it green).

Purpose: give every later plan an executable, fail-loud gate instead of a prose
claim — matching the repo's verify-by-execution bar.
Output: two committed bash scripts. The guard is GREEN on cairnkeep today; the
parity checker is correctly RED (it detects the real, known drift Plan 03 fixes).
</objective>

<execution_context>
@/home/stondo/PARA/Projects/cairnkeep/.claude/gsd-core/workflows/execute-plan.md
@/home/stondo/PARA/Projects/cairnkeep/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-self-consistency-public-positioning/11-CONTEXT.md
@.planning/phases/11-self-consistency-public-positioning/11-RESEARCH.md
@.planning/phases/11-self-consistency-public-positioning/11-PATTERNS.md
@scripts/verify-routing-seam.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: verify-no-private-references.sh (SC-03 guard)</name>
  <files>scripts/verify-no-private-references.sh</files>
  <read_first>
    - scripts/verify-routing-seam.sh (the analog: header/usage/main shape, `log_binary_presence` presence-only logging idiom, fail-loud-never-silent conditionals — copy the shape)
    - .planning/phases/11-self-consistency-public-positioning/11-RESEARCH.md (Architecture Patterns 1/2, Code Examples "generic-pattern stage" + "commit-message stage", Common Pitfall 1, Security Domain table — the exact generic patterns and fail-closed conditional live here)
    - .planning/phases/11-self-consistency-public-positioning/11-PATTERNS.md (guard-specific check-logic block, `log_denylist_presence` idiom)
    - .planning/PROJECT.md §Constraints (DEC-no-private-references, DEC-no-ai-authorship, DEC-commit-scanning — the LOCKED rules this guard enforces)
  </read_first>
  <action>
    Create scripts/verify-no-private-references.sh, executable, matching the
    verify-routing-seam.sh shape: `#!/usr/bin/env bash`, `set -euo pipefail`, a
    `usage()` heredoc reachable via `-h|--help`, a `main()` that parses flags
    (unknown flag → `usage >&2; exit 2`), evidence to stdout/stderr, non-zero
    exit on ANY hit — never a silent pass.

    Three stages, all sourced from 11-RESEARCH.md Code Examples and
    11-PATTERNS.md (Stages 1-3) — copy the generic patterns from there, do not
    re-derive:
    - Stage 1 (generic denylist, committed, always runs): `git grep -niE` over
      tracked files for the AI-authorship + obvious-private-marker patterns
      defined in 11-RESEARCH.md. Any match → exit 1.
    - Stage 2 (specific denylist, env-gated, fail-closed per D-06): if
      CAIRN_GUARD_DENYLIST is set, require the file readable (`[[ -r ... ]]`,
      else `FATAL ... exit 1`), then `git grep -F -f "$CAIRN_GUARD_DENYLIST"`;
      any match → exit 1. Fixed-string (`-F`) matching per Open Question #2.
    - Stage 3 (commit-message scan per D-07): `git log --format=%B` piped to a
      grep of the same generic patterns; any match → exit 1. `git grep` never
      sees commit bodies — this is a separate, required stage.

    Use `git grep` (not `grep -r`) so scope is exactly the tracked tree,
    including `.planning/` (Pitfall 1, D-07). Self-exclude via `:!` pathspecs:
    exclude scripts/verify-no-private-references.sh itself AND the two phase-11
    detector-documentation files that quote the patterns
    (.planning/phases/11-self-consistency-public-positioning/11-RESEARCH.md and
    11-PATTERNS.md) from Stage 1's generic scan only — they document the
    detector, they are not violations. Stage 2's specific-literal scan still
    covers those files (they contain no employer/private-repo literals).

    Log denylist presence only ("CAIRN_GUARD_DENYLIST set: yes/no") — never echo
    the path or file contents (D-06 no-leak, mirrors `log_binary_presence`).

    Discretion (per CONTEXT): a `--self-test` mode and the exact exclusion/
    anchoring strategy are yours to refine — preserve fail-closed behavior,
    non-zero exit on any hit, one-command local run, and never committing a
    private term.
  </action>
  <verify>
    <automated>bash -n scripts/verify-no-private-references.sh && scripts/verify-no-private-references.sh --help && scripts/verify-no-private-references.sh; echo "clean-tree-exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify-no-private-references.sh` is executable and `bash -n` reports no syntax error
    - `scripts/verify-no-private-references.sh --help` prints usage and exits 0
    - `scripts/verify-no-private-references.sh` exits 0 against the current cairnkeep tracked tree + commit log (any non-zero here is a real SC-03 finding to clean, not a script defect)
    - With CAIRN_GUARD_DENYLIST pointing to a scratch file containing a token known to be present in the tree, the script exits non-zero (proves detection is live)
    - With CAIRN_GUARD_DENYLIST set to a non-existent/unreadable path, the script exits non-zero with a FATAL message (fail-closed, D-06)
    - The script contains `git log --format=%B` (Stage 3 commit-message scan, D-07) and `git grep` (not `grep -r`)
    - Denylist handling logs presence only; the script never echoes `$CAIRN_GUARD_DENYLIST`'s value or the file's contents
  </acceptance_criteria>
  <done>The guard runs green on cairnkeep, fails closed on a misconfigured/unreadable denylist, and provably flags a planted term — ready to gate the token-miser publish tree in Plan 02.</done>
</task>

<task type="auto">
  <name>Task 2: verify-docs-parity.sh (SC-02 parity checker)</name>
  <files>scripts/verify-docs-parity.sh</files>
  <read_first>
    - scripts/verify-routing-seam.sh (same header/usage/main shape to reuse)
    - .planning/phases/11-self-consistency-public-positioning/11-RESEARCH.md (Code Examples "Docs-code parity check core"; Common Pitfall 4)
    - .planning/phases/11-self-consistency-public-positioning/11-PATTERNS.md ("Core diff logic" block, the verified env-key + command-name ground truth)
    - docs/operating.md (Configuration table lines 98-107, HTTP transport table lines 143-149, "10 commands" line 55)
    - README.md (Configuration table lines 67-75)
    - mcp-memory-server/src/index.ts (env-key ground truth — grep only, do not read whole file)
  </read_first>
  <action>
    Create scripts/verify-docs-parity.sh, executable, same verify-*.sh shape.
    Two one-directional checks (code surface must be documented; doc-only names
    are allowed):

    Env-key check: extract the sorted-unique set of `CAIRN_[A-Z_]+` /
    `MCP_HTTP_[A-Z_]+` tokens from mcp-memory-server/src/*.ts ONLY (the runtime
    server config surface — do NOT scan scripts/ or bin/, so script-local vars
    like CAIRN_GUARD_DENYLIST and CAIRN_ROUTE_BINARY never register). Extract the
    same token set named across docs/operating.md + README.md. FAIL (exit 1)
    only for keys present in code but absent from docs — use `comm -23` on the
    two sorted lists, not a symmetric `diff`, so a doc-only key like
    CAIRN_GIT_PROVIDER (read by the CLI/provider layer, not the server) is not a
    false failure. The 14 code keys are: CAIRN_AGENTFS_BASE_DIR,
    CAIRN_EXPLORE_BINARY, CAIRN_EXPLORE_REPO_ROOT, CAIRN_LLM_API_KEY,
    CAIRN_LLM_API_URL, CAIRN_LLM_EXTRACTION_MODEL, CAIRN_MEMORY_EMBEDDING_MODEL,
    CAIRN_MEMORY_EMBEDDING_URL, CAIRN_MEMORY_HTTP_ALLOWED_HOSTS,
    CAIRN_MEMORY_HTTP_ALLOWED_ORIGINS, CAIRN_MEMORY_HTTP_TOKEN,
    CAIRN_ROUTE_ENDPOINT, MCP_HTTP_HOST, MCP_HTTP_PORT. Today CAIRN_EXPLORE_BINARY
    and CAIRN_EXPLORE_REPO_ROOT are undocumented → the check must report them and
    exit non-zero.

    Command check: for each basename in claude/commands/*.md (11 files), assert
    the name appears in docs/operating.md; FAIL (exit 1) for any command file
    whose name is absent. Today `context-explore` is absent from the "10
    commands" list → the check must name it and exit non-zero.

    On any drift, print each missing key/command by name (so the failure is
    actionable) then exit non-zero. On zero drift, exit 0. Report both check
    results even when the first fails, so the operator sees the full drift set in
    one run.
  </action>
  <verify>
    <automated>bash -n scripts/verify-docs-parity.sh && scripts/verify-docs-parity.sh --help && scripts/verify-docs-parity.sh; echo "parity-exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify-docs-parity.sh` is executable and `bash -n` reports no syntax error
    - `scripts/verify-docs-parity.sh --help` prints usage and exits 0
    - Running the script today names `context-explore` as a missing command and names `CAIRN_EXPLORE_BINARY` and `CAIRN_EXPLORE_REPO_ROOT` as missing keys, and exits non-zero
    - The env-key extraction reads only `mcp-memory-server/src/*.ts` for the code side (grep in the script confirms the path scope); `CAIRN_GIT_PROVIDER` present only in docs does NOT cause a failure
    - The failure set is one-directional (code-keys-not-in-docs via `comm -23`), not a symmetric diff
  </acceptance_criteria>
  <done>The parity checker exists, correctly detects the known SC-02 drift by name, and will exit 0 once Plan 03 lands the doc fixes.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (introduced by this plan)

- `scripts/verify-no-private-references.sh` — new bash gate script (SC-03 guard)
- `scripts/verify-docs-parity.sh` — new bash gate script (SC-02 parity checker)
- `CAIRN_GUARD_DENYLIST` — new operator-facing env var, read ONLY by the guard
  script (never by `mcp-memory-server/src`); it is script-local config, not
  server config, so it must NOT appear in the docs Configuration table and must
  NOT be flagged by verify-docs-parity.sh
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-provided denylist file → guard script | An env-referenced local file supplies the untrusted-path input; its readability and contents cross into the verification process |
| tracked tree + commit log → guard verdict | The scanned content determines a pass/fail that gates a public publish downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | guard verdict when misconfigured (env typo, empty/unreadable denylist) | high | mitigate | Fail-closed exit codes (D-06): CAIRN_GUARD_DENYLIST set + file unreadable → non-zero exit, never a silent skip. Acceptance proves a planted term is flagged (detection is live, not a trivial pass) |
| T-11-02 | Information Disclosure | denylist path/contents leaked via script output | medium | mitigate | Presence-only logging ("set: yes/no"); the script never echoes `$CAIRN_GUARD_DENYLIST` or the file body |
| T-11-SC | Tampering | npm/pip/cargo installs | low | accept | N/A — this plan installs no packages (RESEARCH Package Legitimacy Audit: not applicable); two bash scripts only |
</threat_model>

<verification>
- `bash -n` clean on both scripts; both `--help` exit 0
- Guard: exit 0 on cairnkeep; non-zero on planted denylist term; non-zero on unreadable denylist path
- Parity: names `context-explore`, `CAIRN_EXPLORE_BINARY`, `CAIRN_EXPLORE_REPO_ROOT` as drift and exits non-zero (expected RED — Plan 03 flips it green)
- Regression net unaffected: no `mcp-memory-server/src` files touched
</verification>

<success_criteria>
Both gate scripts exist, are executable, follow the verify-by-execution shape,
fail loud, and behave exactly as specified (guard green + fail-closed; parity
correctly RED naming the known drift). SC-02/SC-03 now have runnable gates.
</success_criteria>

<output>
Create `.planning/phases/11-self-consistency-public-positioning/11-01-SUMMARY.md` when done.
</output>
