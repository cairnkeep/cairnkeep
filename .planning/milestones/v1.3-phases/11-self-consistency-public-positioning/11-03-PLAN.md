---
phase: 11-self-consistency-public-positioning
plan: 03
type: execute
wave: 3
depends_on: [11-01, 11-02]
files_modified:
  - README.md
  - docs/operating.md
  - docs/git-providers.md
autonomous: false
requirements: [SC-01, SC-02]

must_haves:
  truths:
    - "README.md and docs/operating.md name and link token-miser as a public cairnkeep-org sibling (both contain `cairnkeep/token-miser`)"
    - "docs/operating.md says '11 commands' and its command list includes context-explore (the string '10 commands' no longer appears)"
    - "docs/operating.md's configuration surface names every CAIRN_*/MCP_HTTP_* env key the server reads, including CAIRN_EXPLORE_BINARY and CAIRN_EXPLORE_REPO_ROOT"
    - "README.md's Configuration table includes CAIRN_ROUTE_ENDPOINT, CAIRN_EXPLORE_BINARY, and CAIRN_EXPLORE_REPO_ROOT"
    - "README.md's Status section reflects shipped reality (memory server + CLI + operating layer on both harnesses + context exploration + routing seam + public token-miser sibling) with no 'Early... being carved out' framing"
    - "context_explore is described in docs/operating.md's workflow (one sentence: delegates to token_miser explore subprocess; token-miser named as the public cairnkeep-org sibling owning it, with link)"
    - "the §Routing seam section names token-miser as the public cairnkeep-org sibling with a link, without weakening the frozen Phase 10 seam contract or expanding into token-miser internals (D-12)"
    - "verify-docs-parity.sh exits 0 (zero code-vs-docs drift)"
  artifacts:
    - "README.md (Status refreshed, Related projects section added, Configuration table completed)"
    - "docs/operating.md (command count/list, Configuration table, workflow context_explore entry, routing-seam sibling naming)"
  key_links:
    - "the Related-projects / routing-seam links point at github.com/cairnkeep/token-miser, which Plan 02 has already made public (dead-link avoidance)"
    - "verify-docs-parity.sh (Plan 01) flips from RED to GREEN once both doc edits land — it is the mechanized proof the drift is closed"
  prohibitions:
    - statement: "The docs must not describe token-miser's internal architecture (tiers, FastContext internals, overlay story) beyond one sentence + link per wire"
      status: resolved
      verification: "cold-read checkpoint + D-12: each wire gets one sentence naming token-miser + link; no dedicated internals section is added"
---

<objective>
Close the SC-02 docs drift and land the SC-01 public-sibling positioning across
the three-doc sweep (D-09): README.md, docs/operating.md, docs/git-providers.md.
Fix the known drift (command count 10→11 + context-explore; missing
CAIRN_EXPLORE_* config rows; missing context_explore workflow entry), add
token-miser's public-sibling naming/link where the wires are documented
(D-11/D-12), and refresh README's stale Status (D-13).

Purpose: make the public story true and self-consistent — every count, list,
env key, and wire description matches the shipped Phase 10 code, and token-miser
reads as a public dependency, not a private one.
Output: three swept docs, verify-docs-parity.sh green, and an operator cold-read
confirming prose-level accuracy (D-10).
</objective>

<execution_context>
@/home/stondo/PARA/Projects/cairnkeep/.claude/gsd-core/workflows/execute-plan.md
@/home/stondo/PARA/Projects/cairnkeep/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-self-consistency-public-positioning/11-CONTEXT.md
@.planning/phases/11-self-consistency-public-positioning/11-RESEARCH.md
@.planning/phases/11-self-consistency-public-positioning/11-PATTERNS.md
@README.md
@docs/operating.md
@docs/git-providers.md
@scripts/verify-docs-parity.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh README (Status, Related projects, config table)</name>
  <files>README.md</files>
  <read_first>
    - README.md (current Status lines 10-15, Components lines 17-26, Configuration table lines 67-75)
    - .planning/phases/11-self-consistency-public-positioning/11-CONTEXT.md (D-11, D-13, and Claude's Discretion on exact wording)
    - .planning/phases/11-self-consistency-public-positioning/11-PATTERNS.md (README edit section — the stale-text location and the config-row shape to copy)
    - docs/operating.md (lines 107 CAIRN_ROUTE_ENDPOINT row; lines 98-107 table shape — reuse the "(unset → behavior)" phrasing)
  </read_first>
  <action>
    Three edits to README.md, keeping the existing heading/list style:

    1. Status (lines 10-15, D-13): replace the "Early... being carved out...
       first component is the memory server" prose with shipped reality — memory
       server + `cairn` CLI + operating layer on both Claude Code and OpenCode +
       context exploration + routing seam + token-miser as a public
       cairnkeep-org sibling. Same `## Status` heading, prose swap only. Keep the
       thin-delegate framing (LOCKED v1.2). Exact wording is your discretion.

    2. Related projects (net-new section, D-11): add a short `## Related
       projects` section (after `## Components`, matching its bullet style)
       naming token-miser, a one-liner, and a link to
       github.com/cairnkeep/token-miser. One entry — do not describe tiers or
       FastContext internals (D-12 prohibition).

    3. Configuration table (lines 67-75, D-11 completion): add rows for
       CAIRN_ROUTE_ENDPOINT, CAIRN_EXPLORE_BINARY, and CAIRN_EXPLORE_REPO_ROOT
       using the existing "Variable | Purpose" shape. Ground-truth purposes:
       CAIRN_ROUTE_ENDPOINT = base URL of an already-running token-miser
       routing/tiering proxy (unset → route_check is inert); CAIRN_EXPLORE_BINARY
       = absolute path to the token_miser binary for context_explore (unset →
       the tool throws); CAIRN_EXPLORE_REPO_ROOT = default repo root for
       context_explore when no per-call repo_root is given.
  </action>
  <verify>
    <automated>grep -q 'cairnkeep/token-miser' README.md && grep -q 'CAIRN_ROUTE_ENDPOINT' README.md && grep -q 'CAIRN_EXPLORE_BINARY' README.md && grep -q 'CAIRN_EXPLORE_REPO_ROOT' README.md && ! grep -qi 'being carved out' README.md && echo README-OK</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains a `## Related projects` (or equivalent) section naming token-miser and linking `github.com/cairnkeep/token-miser`
    - README.md's Configuration table contains rows for `CAIRN_ROUTE_ENDPOINT`, `CAIRN_EXPLORE_BINARY`, `CAIRN_EXPLORE_REPO_ROOT`
    - README.md `## Status` no longer contains "being carved out" / "first component ... is the memory server"; it names both harnesses, context exploration, routing seam, and the public token-miser sibling
    - No token-miser internals (tiers/FastContext) are described beyond one line + link (D-12)
  </acceptance_criteria>
  <done>README front door names token-miser as a public sibling, its config table is complete, and its Status reflects the shipped v1.0-v1.3 surface.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep docs/operating.md and audit git-providers.md, parity green</name>
  <files>docs/operating.md, docs/git-providers.md</files>
  <read_first>
    - docs/operating.md (line 55 "10 commands" + list; Configuration table lines 98-107; §Routing seam lines 109-134; §The workflow lines 155-184)
    - docs/git-providers.md (audit-only target — read to confirm no token-miser / routing drift)
    - .planning/phases/11-self-consistency-public-positioning/11-CONTEXT.md (D-09, D-11, D-12; the confirmed known-drift list)
    - .planning/phases/11-self-consistency-public-positioning/11-RESEARCH.md (Common Pitfall 4 — do not fix one string and leave the adjacent count/list stale)
    - .planning/phases/11-self-consistency-public-positioning/11-PATTERNS.md (operating.md edit section — exact drift sites and the config-row shape)
    - scripts/verify-docs-parity.sh (the gate this task must turn green)
  </read_first>
  <action>
    Edits to docs/operating.md (Pitfall 4: update every coupled string, not just
    the one known-wrong line):

    1. Command count/list (line 55-57): change "10 commands" to "11 commands"
       and add `context-explore` to the enumerated list. The true command set
       (claude/commands/, 11 files) is: remember, recall, memory-sync,
       memory-review, wiki-ingest, wiki-query, wiki-lint, security-audit,
       repo-review, graphify, context-explore.

    2. Configuration table (lines 98-107): add rows for CAIRN_EXPLORE_BINARY and
       CAIRN_EXPLORE_REPO_ROOT using the existing "(unset → behavior)" phrasing.
       Ground-truth purposes match the tool's own error contract in
       mcp-memory-server/src/index.ts (lines ~1012-1023): CAIRN_EXPLORE_BINARY =
       absolute path to the token_miser binary (unset → context_explore throws);
       CAIRN_EXPLORE_REPO_ROOT = default repo root when no per-call repo_root is
       given (unset + no param → throws).

    3. §The workflow (lines 155-184): add a short context-exploration entry —
       one sentence that `/context-explore <query>` delegates to the external
       `token_miser explore` subprocess and relays compact path:line-range
       citations, naming token-miser as the public cairnkeep-org sibling that
       owns exploration, with the link. Keep thin-delegate framing (holds no
       endpoint/model config).

    4. §Routing seam (lines 109-134, D-11/D-12): add ONE sentence naming
       token-miser as the public cairnkeep-org sibling that owns routing, with
       the github.com/cairnkeep/token-miser link. Do NOT weaken the frozen Phase
       10 seam contract prose already there, and do NOT expand into tier/model
       internals (D-12 prohibition).

    Then audit docs/git-providers.md: read it and confirm no token-miser or
    routing-seam references need updating (D-09 expects no edit here). If drift
    is found, fix it minimally; otherwise leave unchanged and note "no drift" in
    the SUMMARY.

    Finally run scripts/verify-docs-parity.sh — it must now exit 0.
  </action>
  <verify>
    <automated>scripts/verify-docs-parity.sh && ! grep -q '10 commands' docs/operating.md && grep -q 'context-explore' docs/operating.md && grep -q 'CAIRN_EXPLORE_BINARY' docs/operating.md && grep -q 'CAIRN_EXPLORE_REPO_ROOT' docs/operating.md && grep -q 'cairnkeep/token-miser' docs/operating.md && echo OPERATING-OK</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify-docs-parity.sh` exits 0 (zero env-key/command drift)
    - docs/operating.md no longer contains the string "10 commands"; it says "11 commands" and its command list includes `context-explore`
    - docs/operating.md's configuration surface contains `CAIRN_EXPLORE_BINARY` and `CAIRN_EXPLORE_REPO_ROOT` rows
    - docs/operating.md §The workflow describes `/context-explore` in one sentence naming token-miser as the public sibling + link
    - docs/operating.md §Routing seam names token-miser as the public sibling + link; the Phase 10 seam-contract bullet points are unchanged (still: reads exactly CAIRN_ROUTE_ENDPOINT, one GET /health request, execution-tier failures return not throw)
    - docs/git-providers.md is either unchanged (no drift) or minimally corrected, with the outcome recorded
  </acceptance_criteria>
  <done>operating.md matches the shipped code — command count/list, env keys, and both wires — with parity green and token-miser named as the public sibling.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Cold-read confirmation of swept docs (D-10)</name>
  <read_first>
    - README.md (edited Status, Related projects, Configuration table)
    - docs/operating.md (edited command count/list, Configuration table, §The workflow, §Routing seam)
  </read_first>
  <action>Blocking human-verify gate. After Tasks 1-2 land and verify-docs-parity.sh is green, present the edited README and operating.md sections for an operator cold read (D-10) — the mechanized parity check cannot catch prose-level inaccuracy (a stale count adjacent to a fixed list, a dead link, or token-miser internals creeping past one sentence). Do not mark the plan done until the operator types "approved".</action>
  <what-built>README.md, docs/operating.md swept for SC-02 drift and SC-01 positioning; docs/git-providers.md audited. verify-docs-parity.sh green.</what-built>
  <how-to-verify>
    Parity green catches env-key/command drift mechanically; this cold read (D-10)
    catches prose-level inaccuracy the script cannot see:
    1. Read README.md Status, Related projects, and Configuration table — is the
       shipped-reality framing accurate, and does the token-miser link resolve to
       the now-public repo?
    2. Read docs/operating.md §Setup order (command count + list), §Configuration,
       §The workflow (context-explore entry), and §Routing seam — is every count
       consistent with the adjacent list (Pitfall 4), and does each wire name
       token-miser as the public sibling in exactly one sentence + link (no tier/
       FastContext internals, D-12)?
    3. Confirm the Phase 10 seam contract prose was not weakened.
  </how-to-verify>
  <resume-signal>Type "approved", or list the specific prose fixes needed.</resume-signal>
  <acceptance_criteria>
    - Operator confirmed README Status reads as shipped reality and the token-miser link resolves to the public repo
    - Operator confirmed every command count matches its adjacent list and each wire names token-miser in one sentence + link (no tier/FastContext internals)
    - Operator confirmed the Phase 10 seam-contract prose is unchanged
    - Operator typed "approved" (or the plan halts with the requested prose fixes)
  </acceptance_criteria>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (introduced by this plan)

- README.md `## Related projects` section (new heading)
- README.md Configuration-table rows: CAIRN_ROUTE_ENDPOINT, CAIRN_EXPLORE_BINARY,
  CAIRN_EXPLORE_REPO_ROOT (documenting existing server env keys — not new keys)
- docs/operating.md context-exploration workflow entry + CAIRN_EXPLORE_BINARY /
  CAIRN_EXPLORE_REPO_ROOT config rows + token-miser sibling naming
- No new code symbols or env vars — all referenced env keys already exist in
  mcp-memory-server/src (this plan documents them, it does not create them)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc prose → public README/docs | Edited content is world-readable in the public cairnkeep repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-06 | Information Disclosure | a doc edit introduces a private/vendor reference or a dead link | medium | mitigate | The Plan 04 guard re-run scans the tracked tree (including these edits); the cold-read checkpoint confirms the token-miser link resolves to the public repo |
| T-11-07 | Tampering | docs drift silently re-accumulates (edit fixes one string, leaves a coupled one stale) | medium | mitigate | verify-docs-parity.sh must exit 0 (mechanized) AND the cold read covers coupled count/list strings (Pitfall 4) |
| T-11-SC | Tampering | npm/pip/cargo installs | low | accept | N/A — docs-only plan, no packages |
</threat_model>

<verification>
- README grep: contains cairnkeep/token-miser + the three CAIRN_* keys; no "being carved out"
- operating.md grep: no "10 commands"; contains context-explore, CAIRN_EXPLORE_BINARY, CAIRN_EXPLORE_REPO_ROOT, cairnkeep/token-miser
- verify-docs-parity.sh exits 0
- git-providers.md audit outcome recorded
- cold-read checkpoint approved
</verification>

<success_criteria>
The three docs match the shipped code (parity green), token-miser is named and
linked as a public cairnkeep-org sibling where each wire is documented, README's
Status reflects shipped reality, and an operator cold read confirms prose
accuracy — SC-01 and SC-02 satisfied at the docs layer.
</success_criteria>

<output>
Create `.planning/phases/11-self-consistency-public-positioning/11-03-SUMMARY.md` when done.
</output>
