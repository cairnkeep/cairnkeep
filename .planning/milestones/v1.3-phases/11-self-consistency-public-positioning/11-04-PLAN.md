---
phase: 11-self-consistency-public-positioning
plan: 04
type: execute
wave: 4
depends_on: [11-03]
files_modified:
  - .planning/MILESTONES.md
autonomous: true
requirements: [SC-03]

must_haves:
  truths:
    - "verify-no-private-references.sh exits 0 against the final Phase 11 tracked tree + commit log (the SC-03 gate, run live)"
    - "verify-docs-parity.sh exits 0 against the final docs (SC-02 gate, run live)"
    - ".planning/MILESTONES.md records the Phase 11 no-private-references gate: the exact command(s), the run date, and the zero-hit / zero-drift output"
    - "the recorded evidence reflects a real live run of the scripts, not a transcribed assertion (D-08)"
  artifacts:
    - ".planning/MILESTONES.md Phase 11 self-consistency gate record (command + date + zero-hit/zero-drift evidence, plus the token-miser PUBLIC verdict)"
  key_links:
    - "this plan runs LAST (Wave 4) so the recorded gate reflects the final tree after the docs sweep (Plan 03) — recording earlier would capture a pre-fix state"
    - "the guard scans .planning/ (tracked, D-07); adding this record must not itself introduce a private term (it records zero-hit output, never denylist contents)"
---

<objective>
Run the no-private-references guard and the docs-parity checker as the explicit
Phase 11 milestone gate and record the evidence in MILESTONES.md (D-08, SC-03's
"run and recorded as an explicit milestone gate" clause). This is the phase
capstone: it must run last, against the final tree, so the recorded result is
the shipped state.

Purpose: SC-03 requires the scan be not just green but recorded as a gate. This
plan makes "recorded" concrete and plan-delivered rather than deferred.
Output: a Phase 11 gate record in .planning/MILESTONES.md with the command(s),
date, and zero-hit / zero-drift output, plus the token-miser PUBLIC verdict.
</objective>

<execution_context>
@/home/stondo/PARA/Projects/cairnkeep/.claude/gsd-core/workflows/execute-plan.md
@/home/stondo/PARA/Projects/cairnkeep/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/MILESTONES.md
@.planning/phases/11-self-consistency-public-positioning/11-CONTEXT.md
@scripts/verify-no-private-references.sh
@scripts/verify-docs-parity.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the gate live and record it in MILESTONES.md</name>
  <files>.planning/MILESTONES.md</files>
  <read_first>
    - .planning/MILESTONES.md (existing structure — the v1.0/v1.1/v1.2 shipped sections; append a v1.3-in-progress Phase 11 gate note without altering shipped-milestone records)
    - .planning/phases/11-self-consistency-public-positioning/11-CONTEXT.md (D-08 — what to record: command, date, zero-hit output; and the SC-01 evidence triad from "Specific Ideas")
    - scripts/verify-no-private-references.sh (the SC-03 gate to run)
    - scripts/verify-docs-parity.sh (the SC-02 gate to run)
  </read_first>
  <action>
    Run both gates live against the final tree, then record the real output:

    1. Run scripts/verify-no-private-references.sh (with CAIRN_GUARD_DENYLIST set
       to the operator's denylist so the specific-term stage is exercised) — it
       must exit 0. Capture the actual command line and the zero-hit output.
    2. Run scripts/verify-docs-parity.sh — it must exit 0. Capture the actual
       zero-drift output.
    3. Also capture the token-miser PUBLIC verdict from Plan 02
       (`gh repo view cairnkeep/token-miser --json visibility` → PUBLIC) as the
       SC-01 side of the milestone triad.

    Append a "v1.3 Routing Seam & Context Maturation (in progress) — Phase 11
    self-consistency gate" note to .planning/MILESTONES.md recording: the run
    date (2026-07-06), each command as run, the zero-hit / zero-drift results,
    and the PUBLIC verdict. Do NOT write a full v1.3 shipped-milestone section
    (the milestone is not complete — Phases 12-13 remain); this is a phase-gate
    record. Record only zero-hit/zero-drift OUTPUT — never the denylist file's
    path or contents (the guard scans .planning/, so a leaked term here would
    fail the guard itself).
  </action>
  <verify>
    <automated>scripts/verify-docs-parity.sh && grep -qi 'Phase 11' .planning/MILESTONES.md && grep -q 'verify-no-private-references' .planning/MILESTONES.md && echo GATE-RECORDED</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify-no-private-references.sh` exits 0 when run live (with CAIRN_GUARD_DENYLIST set) against the final tree
    - `scripts/verify-docs-parity.sh` exits 0 when run live
    - .planning/MILESTONES.md contains a Phase 11 gate record naming `verify-no-private-references.sh`, the run date, and a zero-hit result
    - The record includes the docs-parity zero-drift result and the token-miser PUBLIC verdict (SC-01/02/03 triad)
    - The record contains no employer/private-repo literal and no denylist path (the guard, which scans .planning/, still exits 0 after the edit)
    - The existing v1.0/v1.1/v1.2 milestone sections are unchanged
  </acceptance_criteria>
  <done>The Phase 11 self-consistency gate is run live and recorded in MILESTONES.md — SC-03's "run and recorded as an explicit milestone gate" is satisfied.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (introduced by this plan)

- `.planning/MILESTONES.md` — new "v1.3 (in progress) — Phase 11 self-consistency
  gate" record (command + date + zero-hit/zero-drift evidence + PUBLIC verdict)
- No new code symbols, files, or env vars
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate run → recorded evidence | The recorded result must reflect a real run, not an asserted one |
| recorded evidence → tracked .planning/ tree | The record is itself scanned by the guard (D-07) — it must contain no private literal |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-08 | Repudiation | milestone-gate evidence recorded without actually running the gate | medium | mitigate | The task's automated verify re-runs verify-docs-parity.sh live; acceptance requires both scripts exit 0 when run, and the record captures real output |
| T-11-09 | Information Disclosure | recording a denylist term/path into the tracked MILESTONES.md | high | mitigate | Record zero-hit OUTPUT only, never the denylist contents; acceptance requires the guard still exits 0 after the edit (it scans .planning/) |
| T-11-SC | Tampering | npm/pip/cargo installs | low | accept | N/A — recording-only plan, no packages |
</threat_model>

<verification>
- Both gate scripts exit 0 when run live against the final tree
- MILESTONES.md contains the Phase 11 gate record (command + date + zero-hit + zero-drift + PUBLIC verdict)
- The guard still exits 0 after the MILESTONES.md edit (no private term recorded)
- v1.0/v1.1/v1.2 sections untouched
</verification>

<success_criteria>
The no-private-references scan (and the parity checker) are run as the explicit
Phase 11 milestone gate and recorded in MILESTONES.md with command, date, and
zero-hit/zero-drift evidence — SC-03 fully satisfied and the phase's three-part
SC evidence is captured in one place.
</success_criteria>

<output>
Create `.planning/phases/11-self-consistency-public-positioning/11-04-SUMMARY.md` when done.
</output>
