---
phase: 13-headless-harness-hardening
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - scripts/verify-opencode-live-parity.sh
autonomous: true
requirements: [OCP-07]

must_haves:
  truths:
    - "scripts/verify-opencode-live-parity.sh --repeat 5 runs 5 independent iterations, each doing fresh setup_scratch + seed_canary + install_assets + write_scratch_config + start_capture_server + run_stage_remember_recall seeded + stop_capture_server + per-iteration teardown (D-03)."
    - "--repeat prints a per-iteration PASS/FAIL row with the iteration's retry count and a final aggregate verdict line (D-02/D-04)."
    - "--repeat runs the preflight probe once before the loop; a not-tool-call-reliable model aborts the soak before any iteration, printing a message naming the trait requirement (D-06)."
    - "With CAIRN_LLM_API_KEY / CAIRN_LLM_API_URL / CAIRN_LLM_EXTRACTION_MODEL unset, --repeat fails fast (non-zero) via the preflight env check without hanging on an unreachable endpoint (Pitfall 2)."
    - "--repeat exits non-zero unless all N iterations pass; one failed iteration fails the soak and resets the streak (5/5 bar, D-01)."
    - "--repeat appears in the usage/help text with the three-tier --stage / --full / --repeat speed structure preserved (D-12)."
  artifacts:
    - scripts/verify-opencode-live-parity.sh
  key_links:
    - "The --repeat loop calls run_stage_remember_recall (Plan 01) per iteration and reads LAST_ROUNDTRIP_RETRIES for the evidence row."
    - "preflight_tool_call_probe reuses assert-tool-event.mjs (Plan 01) to confirm a genuine tool call, and the CAIRN_LLM_* three-var env check before spending any timeout budget."
  prohibitions:
    - statement: "The preflight probe must NOT spend its timeout budget calling opencode when a required CAIRN_LLM_* var is empty (empty provider config hangs forever — Pitfall 2)."
      status: resolved
      verification: "Running --repeat with the three CAIRN_LLM_* vars unset returns non-zero within a couple of seconds (bounded, no opencode call reached); code review confirms the env check precedes any opencode invocation."
    - statement: "The soak must NOT count an iteration as PASS on retry-masked infra flakiness that never produced a canary-linked tool event (retries absorb flakiness, never mask a broken round-trip — SC#2/D-13)."
      status: resolved
      verification: "The PASS row derives from run_stage_remember_recall's exit code (which is 0 only on a genuine canary-linked tool_use event per Plan 01); the retry count is reported alongside, not in place of, the PASS/FAIL verdict."
    - statement: "opencode serve must NOT bind a non-loopback interface in the probe or per-iteration server."
      status: resolved
      verification: "The probe and loop reuse start_capture_server unchanged (--hostname 127.0.0.1); grep confirms no new bind widening."
    - statement: "The CAIRN_LLM_API_KEY value must NOT appear in the evidence table or any --repeat/probe log line."
      status: resolved
      verification: "New logging routes through log_env_presence (presence-only); grep confirms no evidence/probe line echoes the CAIRN_LLM_API_KEY value."
---

<objective>
Add the soak and its gate: a mechanical preflight probe (D-06) that fails
fast when the configured model is not tool-call-reliable, and a `--repeat N`
mode (D-02) that loops the hardened round-trip stage N times with a fresh
scratch environment each iteration (D-03), emitting a per-iteration
PASS/FAIL + retry-count table and an aggregate verdict (D-04).

Purpose: turn "proven once" into "5/5 consecutive cold reproductions"
(D-01) with one command and one recorded output, without burning five
scratch setups against a model that can't make a real tool call.
Output: `preflight_tool_call_probe` and the `--repeat` dispatch case in
`scripts/verify-opencode-live-parity.sh`.
</objective>

<execution_context>
@/home/stondo/PARA/Projects/cairnkeep/.claude/gsd-core/workflows/execute-plan.md
@/home/stondo/PARA/Projects/cairnkeep/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-headless-harness-hardening/13-CONTEXT.md
@.planning/phases/13-headless-harness-hardening/13-RESEARCH.md
@.planning/phases/13-headless-harness-hardening/13-PATTERNS.md
@.planning/phases/13-headless-harness-hardening/13-01-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols created or modified by THIS plan (exclude from drift verification):

- `preflight_tool_call_probe()` — new bash function in `scripts/verify-opencode-live-parity.sh`.
- `--repeat N` — new CLI dispatch case in `main()`.
- New `usage()` help lines documenting `--repeat`.
- Consumes (does not define): `assert_tool_event`, `assert-tool-event.mjs`, `LAST_ROUNDTRIP_RETRIES`, `run_stage_remember_recall` (all from Plan 01).

<tasks>

<task type="auto">
  <name>Task 1: preflight_tool_call_probe with env fail-fast and trait message</name>
  <files>scripts/verify-opencode-live-parity.sh</files>
  <read_first>
    - scripts/verify-opencode-live-parity.sh (lines 285-292: log_env_presence — the three CAIRN_LLM_* var names to reuse for the fail-fast check; lines 142-161: setup_scratch; lines 202-257: install_assets + write_scratch_config; lines 441-470: start_capture_server/stop_capture_server; the assert_tool_event wrapper added by Plan 01)
    - scripts/verify-fastcontext-reliability.sh (lines 23-45: usage staged-probe wording; assert_tool_call_turn ~line 246 — the closest existing "did this turn make a genuine tool call" assertion to mirror in intent)
    - .planning/phases/13-headless-harness-hardening/13-RESEARCH.md (Pattern 3 preflight probe; Pitfall 2 empty-config-hang; Pitfall 3 bundled-default-model masking; Open Question 2 — probe against the real cairn-memory tool)
    - .planning/phases/13-headless-harness-hardening/13-CONTEXT.md (D-05, D-06)
  </read_first>
  <action>
    Add `preflight_tool_call_probe()`. Step 1 (before ANY opencode call —
    Pitfall 2): if any of CAIRN_LLM_API_KEY, CAIRN_LLM_API_URL,
    CAIRN_LLM_EXTRACTION_MODEL is empty, call log_env_presence and return
    non-zero immediately with a bracketed-tag message
    `[preflight] FAIL: CAIRN_LLM_* not fully set — see docs/operating.md`.
    Step 2: reuse the existing scratch bring-up (setup_scratch, seed_canary
    seeded, install_assets, write_scratch_config, start_capture_server) so
    the probe drives the REAL cairn-memory MCP tool (Open Question 2 answer),
    then run ONE cheap turn via run_opencode with `--attach "$CAPTURE_SERVE_URL"`
    that forces a single write-style tool call against a disposable canary,
    piped into `assert_tool_event 'cairn-memory_memory_(write|supersede)'`.
    Step 3: stop_capture_server. If the assertion passes, print
    `[preflight] OK: model made a genuine tool call` and return 0. If it
    fails, print exactly
    `[preflight] FAIL: model is not tool-call-reliable (no-thinking required) — see docs/operating.md`
    and return non-zero (per the specifics: name the trait so a future
    operator knows it is a model problem, not a harness regression). Never
    echo the CAIRN_LLM_API_KEY value. Keep the probe's own timeout bounded
    (use run_opencode's timeout arg, e.g. 90) so an unreachable endpoint fails
    loudly rather than hanging.
    Note (comment only, load-bearing per Pitfall 3): opencode ships a bundled
    default model, so the env-var presence check in Step 1 is the guard that
    prevents a false PASS against opencode's own default when CAIRN_LLM_* is
    unset — do not remove it.
  </action>
  <verify>
    <automated>bash -n scripts/verify-opencode-live-parity.sh && grep -q 'preflight_tool_call_probe' scripts/verify-opencode-live-parity.sh && grep -q 'not tool-call-reliable (no-thinking required)' scripts/verify-opencode-live-parity.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/verify-opencode-live-parity.sh` exits 0.
    - `scripts/verify-opencode-live-parity.sh` defines `preflight_tool_call_probe`.
    - The probe's first action checks CAIRN_LLM_API_KEY, CAIRN_LLM_API_URL, and CAIRN_LLM_EXTRACTION_MODEL are non-empty and returns non-zero before any `opencode run` if any is empty (source review: the env check precedes the run_opencode call).
    - The FAIL message contains the exact substring `not tool-call-reliable (no-thinking required)`.
    - The probe reuses `start_capture_server` (--hostname 127.0.0.1 unchanged) and `assert_tool_event` — no new opencode-serve bind flags introduced.
  </acceptance_criteria>
  <done>A cheap preflight gate confirms the operator's model makes a genuine tool call, or fails fast with a trait-named message before the soak burns any scratch setups.</done>
</task>

<task type="auto">
  <name>Task 2: --repeat N soak mode with per-iteration scratch, evidence table, aggregate verdict, usage text</name>
  <files>scripts/verify-opencode-live-parity.sh</files>
  <read_first>
    - scripts/verify-opencode-live-parity.sh (lines 642-709: main() dispatch — the --setup-only / --stage / --full cases whose per-run sequence the loop mirrors; lines 82-135: capture_real_config_fingerprint + cleanup — the per-run teardown/fingerprint guard; lines 628-640: run_negative_controls — the accumulate-failures-then-one-verdict shape; lines 19-49: usage heredoc to extend)
    - preflight_tool_call_probe (added in Task 1) and run_stage_remember_recall + LAST_ROUNDTRIP_RETRIES (added in Plan 01)
    - .planning/phases/13-headless-harness-hardening/13-CONTEXT.md (D-01, D-02, D-03, D-04, D-12)
    - .planning/phases/13-headless-harness-hardening/13-RESEARCH.md (Pitfall 5 — the soak is materially slower; keep it a separate explicit mode)
  </read_first>
  <action>
    Add a `--repeat` case to main()'s dispatch, parsing the iteration count
    from `$2` (default 5) and validating it is a positive integer (exit 2 on
    a non-numeric/zero argument with a usage message). Flow:
    (1) Run `preflight_tool_call_probe` once; if it returns non-zero, print
    the abort and exit non-zero WITHOUT starting the loop (D-06 — do not burn
    N setups).
    (2) Loop `for i in $(seq 1 "$N")`. Each iteration is a fully independent
    cold reproduction (D-03): capture_real_config_fingerprint, setup_scratch,
    seed_canary seeded, install_assets, write_scratch_config,
    positive_load_check, start_capture_server, then
    `run_stage_remember_recall "$SCRATCH_PROJECT" seeded`, then
    stop_capture_server. After each iteration reset CLEANED_UP=0 and call
    cleanup so scratch dirs are removed and the fingerprint guard re-runs per
    iteration (no state bleed). Record a row per iteration in the existing
    bracketed-tag style, e.g. `[repeat:$i/$N] PASS (retries=$LAST_ROUNDTRIP_RETRIES)`
    or `[repeat:$i/$N] FAIL (retries=$LAST_ROUNDTRIP_RETRIES)`, reading
    LAST_ROUNDTRIP_RETRIES set by the stage. Accumulate a pass count; a failed
    iteration is recorded and the loop continues (so the full table prints),
    but the streak is broken (5/5 bar — any FAIL means the aggregate is not
    5/5).
    (3) After the loop print the aggregate: `[repeat] $passes/$N PASSED` and,
    if `$passes -ne $N`, a `[repeat] FAIL` line and `return 1`; else
    `[repeat] OK: $N/$N consecutive round-trips` and success.
    Extend the `usage()` heredoc with a `--repeat N` synopsis line and an
    indented paragraph: it soaks the hardened remember->recall stage N times
    (default 5) with a fresh scratch environment each iteration, gated by the
    preflight probe, and is the explicit slow reliability mode — distinct from
    `--stage wakeup` (fastest per-commit signal) and `--full` (one-shot
    regression of every stage). Do NOT wire --repeat into any fast/per-commit
    path (Pitfall 5).
    Do not soak the whole --full suite (D-12 — the round-trip stage only);
    do not convert any other stage's transport or assertions (out of scope,
    Deferred Ideas).
  </action>
  <verify>
    <automated>bash -n scripts/verify-opencode-live-parity.sh && scripts/verify-opencode-live-parity.sh --help 2>&1 | grep -q -- '--repeat'</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/verify-opencode-live-parity.sh` exits 0.
    - `scripts/verify-opencode-live-parity.sh --help` output names `--repeat`.
    - The `--repeat` case calls `preflight_tool_call_probe` before the loop and exits non-zero without looping when the probe fails.
    - Running `--repeat` with all three CAIRN_LLM_* vars unset exits non-zero within ~30s (bounded by the preflight env fail-fast, no hang) — e.g. `env -u CAIRN_LLM_API_KEY -u CAIRN_LLM_API_URL -u CAIRN_LLM_EXTRACTION_MODEL timeout 30 scripts/verify-opencode-live-parity.sh --repeat 1` returns non-zero (and does NOT hit the 124 timeout kill).
    - Each iteration's row includes `retries=$LAST_ROUNDTRIP_RETRIES`; the aggregate prints `N/N` and returns non-zero unless all N pass.
    - A non-numeric or zero `--repeat` argument exits 2 with a usage message.
  </acceptance_criteria>
  <done>`--repeat N` soaks the hardened round-trip N times behind the preflight gate, prints a per-iteration + aggregate verdict with retry counts, and returns non-zero unless all N pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env → preflight/soak logs | CAIRN_LLM_* flow through the shell; values must never be echoed |
| opencode serve → loopback socket | per-iteration and probe servers must stay bound to 127.0.0.1 |
| opencode bundled default model → probe verdict | a false PASS against opencode's own default (not the operator's model) must be prevented |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-02 | Information Disclosure | per-iteration + probe start_capture_server | medium | mitigate | Reuse start_capture_server unchanged (--hostname 127.0.0.1); no bind widening |
| T-13-03 | Information Disclosure | evidence table + probe logs | high | mitigate | All new logging via log_env_presence (presence-only); no CAIRN_LLM_API_KEY value echoed |
| T-13-05 | Spoofing (of the reliability signal) | preflight probe vs opencode bundled default | high | mitigate | Env-var presence gate (Pitfall 3) prevents a false PASS against opencode's bundled default model when CAIRN_LLM_* is unset |

No package installs occur in this plan (RESEARCH.md Package Legitimacy Audit: not applicable) — no supply-chain checkpoint (T-13-SC) is required.
</threat_model>

<verification>
- `bash -n scripts/verify-opencode-live-parity.sh` — syntax clean.
- `scripts/verify-opencode-live-parity.sh --help` names `--repeat`.
- `env -u CAIRN_LLM_* ... timeout 30 scripts/verify-opencode-live-parity.sh --repeat 1` returns non-zero fast (preflight env fail-fast; no hang).
- The live 5/5 soak (`scripts/verify-opencode-live-parity.sh --repeat 5` green) is the phase gate, run by the operator with a real tool-call-reliable model and recorded in the phase UAT/VERIFICATION doc (D-04) — not runnable in this sandbox.
</verification>

<success_criteria>
- `--repeat N` exists, gated by the preflight probe, with per-iteration fresh scratch (D-03), an evidence table + aggregate verdict (D-04), and 5/5 semantics (D-01).
- Missing CAIRN_LLM_* config fails fast without hanging (Pitfall 2).
- No other stage's transport/assertions changed; --full still runs once for regression (D-12).
</success_criteria>

<output>
Create `.planning/phases/13-headless-harness-hardening/13-02-SUMMARY.md` when done.
</output>