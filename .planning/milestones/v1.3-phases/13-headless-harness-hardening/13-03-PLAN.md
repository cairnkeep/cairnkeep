---
phase: 13-headless-harness-hardening
plan: 03
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - docs/operating.md
  - .planning/MILESTONES.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [OCP-07]

must_haves:
  truths:
    - "docs/operating.md documents the headless round-trip harness's model precondition as a no-thinking, tool-call-reliable local model, citing qwen3.5-27b as the proven public example, with model selection operator-env-driven via CAIRN_LLM_* (D-07)."
    - "docs/operating.md names scripts/verify-opencode-live-parity.sh --repeat 5 as the reliability soak and preserves the --stage / --full / --repeat three-tier speed structure (D-04/D-12)."
    - ".planning/MILESTONES.md records the OCP-06 reliable-headless-reproduction gap as resolved by OCP-07 / Phase 13, pointing at the --repeat 5 soak evidence (D-04, SC#3)."
    - ".planning/REQUIREMENTS.md OCP-07 is checked ([x]) and its Traceability row reads Complete (D-04, SC#3)."
  artifacts:
    - docs/operating.md
    - .planning/MILESTONES.md
    - .planning/REQUIREMENTS.md
  key_links:
    - "The precondition text and --repeat reference must match the flag and trait-message wording shipped by Plans 01-02 (docs-vs-code parity)."
    - "The MILESTONES.md 'type:tool' shorthand footnote must clarify part.type vs the top-level type == tool_use filter (RESEARCH State of the Art / Deprecated note)."
  prohibitions:
    - statement: "The docs and records must NOT introduce any private/vendor reference (qwen3.5-27b is a public model name and is allowed per D-07)."
      status: resolved
      verification: "scripts/verify-no-private-references.sh exits 0 after the edits."
    - statement: "The docs edits must NOT introduce a new CAIRN_*/MCP_HTTP_* env key undocumented elsewhere (no new env keys this phase)."
      status: resolved
      verification: "scripts/verify-docs-parity.sh exits 0 after the edits."
---

<objective>
Document the model precondition and close the record: state in docs/operating.md
that the headless round-trip harness requires a no-thinking, tool-call-reliable
local model (citing the proven public example, D-07), and update MILESTONES.md
and REQUIREMENTS.md so the v1.1 OCP-06 known gap is recorded resolved via
OCP-07 (D-04, Success Criterion #3).

Purpose: the gap was carried as a v1.1 override known gap; closing it in the
milestone/requirements traceability — and documenting the precondition that
makes the soak reproducible — is the third success criterion of the phase.
Output: updated docs/operating.md, MILESTONES.md, REQUIREMENTS.md.
</objective>

<execution_context>
@/home/stondo/PARA/Projects/cairnkeep/.claude/gsd-core/workflows/execute-plan.md
@/home/stondo/PARA/Projects/cairnkeep/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-headless-harness-hardening/13-CONTEXT.md
@.planning/phases/13-headless-harness-hardening/13-RESEARCH.md
@.planning/phases/13-headless-harness-hardening/13-01-SUMMARY.md
@.planning/phases/13-headless-harness-hardening/13-02-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols created or modified by THIS plan (exclude from drift verification):

- New prose subsection in `docs/operating.md` (model precondition + `--repeat 5` soak note).
- Edited "Known Gaps (override closeout)" entry in `.planning/MILESTONES.md`.
- Edited OCP-07 checkbox + Traceability row in `.planning/REQUIREMENTS.md`.

<tasks>

<task type="auto">
  <name>Task 1: Document the trait-based model precondition and the --repeat soak</name>
  <files>docs/operating.md</files>
  <read_first>
    - docs/operating.md (lines 70-111: Setup order (OpenCode) + Configuration table with the three CAIRN_LLM_* rows already present — add the precondition near this OpenCode context, do NOT introduce new env keys)
    - .planning/phases/13-headless-harness-hardening/13-CONTEXT.md (D-07 exact wording: "a no-thinking, tool-call-reliable local model", citing qwen3.5-27b; model selection operator-env-driven, no committed defaults, no known-good allowlist in the harness)
    - .planning/phases/13-headless-harness-hardening/13-02-SUMMARY.md (the shipped --repeat flag name/ergonomics and preflight trait message to describe accurately)
    - scripts/verify-docs-parity.sh (the one-directional env-key + command gate this edit must keep green)
  </read_first>
  <action>
    Add a short subsection to docs/operating.md (under the OpenCode setup /
    configuration area) titled for the headless round-trip harness. State,
    per D-07: the harness's headless remember->recall round-trip is reliable
    only given a no-thinking, tool-call-reliable local model; qwen3.5-27b is
    the publicly-known model that passed (a thinking model that narrates
    pseudo-tool-calls instead of emitting real tool_use events will not pass
    and no amount of retry will fix it). Model selection stays operator-env-
    driven through the existing CAIRN_LLM_API_KEY / CAIRN_LLM_API_URL /
    CAIRN_LLM_EXTRACTION_MODEL vars (already in the Configuration table) — the
    harness commits no default model and carries no known-good allowlist.
    Document the reliability soak command `scripts/verify-opencode-live-parity.sh --repeat 5`
    and the preflight probe that fails fast with a trait-named message if the
    configured model is not tool-call-reliable. Preserve the three-tier speed
    structure in the description: `--stage wakeup` (fastest per-commit signal),
    `--full` (one-shot regression of every stage), `--repeat N` (the slow
    reliability soak — do not run it on every commit). Do NOT add any new
    CAIRN_*/MCP_HTTP_* env key (none are introduced this phase); reference only
    the three existing ones by their exact names so verify-docs-parity stays
    green.
  </action>
  <verify>
    <automated>scripts/verify-docs-parity.sh && scripts/verify-no-private-references.sh && grep -q 'tool-call-reliable' docs/operating.md && grep -q 'qwen3.5-27b' docs/operating.md && grep -q -- '--repeat' docs/operating.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/operating.md` contains the substring `tool-call-reliable`, the substring `qwen3.5-27b`, and the substring `--repeat`.
    - `scripts/verify-docs-parity.sh` exits 0 (no new undocumented env key, no missing command).
    - `scripts/verify-no-private-references.sh` exits 0 (public model name is allowed).
    - The precondition text references CAIRN_LLM_EXTRACTION_MODEL (and the other two CAIRN_LLM_* vars) by their exact existing names — no new env key.
  </acceptance_criteria>
  <done>docs/operating.md documents the no-thinking tool-call-reliable model precondition with the proven public example and the --repeat soak, with both parity gates green.</done>
</task>

<task type="auto">
  <name>Task 2: Record the OCP-06 gap resolved in MILESTONES.md and REQUIREMENTS.md</name>
  <files>.planning/MILESTONES.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/MILESTONES.md (lines 63-68: the "Known Gaps (override closeout)" section — the OCP-06 reliable-headless-reproduction bullet and the carried-forward follow-up about genuine "type":"tool" events)
    - .planning/REQUIREMENTS.md (line 30: OCP-07 checkbox; lines 52-68: the Traceability table + Coverage block)
    - .planning/phases/13-headless-harness-hardening/13-CONTEXT.md (D-04 — mirror the way Phase 11 recorded its milestone gate; point at the --repeat evidence)
    - .planning/phases/13-headless-harness-hardening/13-RESEARCH.md (State of the Art / Deprecated: the "type":"tool" shorthand means part.type; the top-level filter is type == "tool_use")
  </read_first>
  <action>
    In .planning/MILESTONES.md's "Known Gaps (override closeout)" section:
    annotate the "OCP-06 — reliable headless reproduction of the /remember->/recall
    round-trip" bullet as RESOLVED by OCP-07 / Phase 13, noting the gap is
    closed by the serve/--attach conversion of the round-trip stage plus the
    genuine tool_use NDJSON-event assertions and the `--repeat 5` soak
    (5/5 consecutive cold reproductions), with the per-run + aggregate evidence
    recorded in the Phase 13 UAT/VERIFICATION doc. Mark the carried-forward
    follow-up ("verify each stage with a genuine \"type\":\"tool\" event ...")
    as discharged for the round-trip stage, and add the precise footnote from
    research: the shorthand \"type\":\"tool\" refers to part.type, while the
    top-level envelope field the parser filters on is type == "tool_use". Leave
    the separate "OCP-06 — interactive TUI session not run" bullet as an
    explicit still-open item (out of scope for OCP-07 per REQUIREMENTS.md — do
    NOT claim it resolved).
    In .planning/REQUIREMENTS.md: change the OCP-07 checkbox from `[ ]` to
    `[x]`, and update the Traceability row `| OCP-07 | Phase 13 | Pending |`
    to `| OCP-07 | Phase 13 | Complete |`. Update the "Last updated" line to
    note the OCP-07 closeout. Keep OCP-07's "interactive-TUI confirm out of
    scope" qualifier intact.
    Use Edit (scoped) for all three edits — never rewrite these files wholesale.
  </action>
  <verify>
    <automated>grep -Eq '\| OCP-07 \| Phase 13 \| Complete \|' .planning/REQUIREMENTS.md && grep -q '\[x\] \*\*OCP-07' .planning/REQUIREMENTS.md && grep -q 'OCP-07' .planning/MILESTONES.md && scripts/verify-no-private-references.sh</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` OCP-07 line begins `[x] **OCP-07`.
    - `.planning/REQUIREMENTS.md` Traceability contains the row `| OCP-07 | Phase 13 | Complete |`.
    - `.planning/MILESTONES.md` Known Gaps section marks the OCP-06 reliable-headless-reproduction bullet resolved by OCP-07 / Phase 13 and references the `--repeat` soak.
    - `.planning/MILESTONES.md` retains the separate "interactive TUI session not run" bullet as still-open (not claimed resolved).
    - `.planning/MILESTONES.md` includes the part.type vs top-level type == "tool_use" footnote.
    - `scripts/verify-no-private-references.sh` exits 0.
  </acceptance_criteria>
  <done>The v1.1 OCP-06 headless-reproduction gap is recorded resolved by OCP-07 in both MILESTONES.md and REQUIREMENTS.md traceability, with the interactive-TUI item still correctly open.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored docs/records → public repo | prose committed to a public repo must contain no private/vendor references |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-04 | Information Disclosure | docs/operating.md, MILESTONES.md, REQUIREMENTS.md prose | high | mitigate | Cite only the public model name (qwen3.5-27b); run verify-no-private-references.sh as the gate; never record a CAIRN_LLM_API_KEY value or a private endpoint/model |
| T-13-03 | Information Disclosure | recorded soak evidence | medium | mitigate | The --repeat evidence table is presence-only (Plan 02); the closeout record points at it without transcribing secrets |

No package installs occur in this plan (docs only) — no supply-chain checkpoint (T-13-SC) is required.
</threat_model>

<verification>
- `scripts/verify-docs-parity.sh` — green (no new env keys, no missing command).
- `scripts/verify-no-private-references.sh` — green (public model name allowed).
- Greps confirm the precondition text, the OCP-07 checkbox flip, and the Complete traceability row.
- The soak evidence itself (`--repeat 5` output) is produced by the operator at phase verification and pasted into the Phase 13 UAT/VERIFICATION doc (D-04) — this plan writes the records that point at it.
</verification>

<success_criteria>
- docs/operating.md documents the trait-based precondition (D-07) and the --repeat soak, both parity gates green.
- MILESTONES.md + REQUIREMENTS.md record OCP-06 headless reproduction resolved via OCP-07 (SC#3), interactive-TUI still open.
</success_criteria>

<output>
Create `.planning/phases/13-headless-harness-hardening/13-03-SUMMARY.md` when done.
</output>