---
milestone: v1.0
audited: 2026-07-03
status: passed   # OpenCode-wakeup ordering debt closed via docs/operating.md edit; only routine pre-close bookkeeping remains
scores:
  requirements: 6/6
  phases: 3/3
  integration: wired
  flows: verified (memory round-trip smoke-tested)
method: >
  Direct code + live-test verification. This project predates GSD plan-tracking,
  so no SUMMARY.md / VERIFICATION.md / VALIDATION.md artifacts exist; the standard
  3-source cross-reference is not applicable. Requirements were verified against the
  actual codebase and by running the memory-server build + smoke suite.
evidence:
  build: "npm run build → exit 0 (tsc clean)"
  tests: "npm test (5 smoke checks) → all pass; search-e2e + embeddings exit 0"
  secrets: "git ls-files | grep -iE '\\.env$|secret|credential|\\.pem$|\\.key$' → none tracked"
  provider_neutral: "git grep for api.github.com / gitlab.com/api / codeberg.org/api in mcp-memory-server/src, scripts, bin → none"
  baseline_tag: "v1.0.0 annotated tag exists at HEAD (0fe22ce)"
gaps: {}   # no unsatisfied requirements, no critical blockers
tech_debt:
  - phase: 02-operating-layer-verification
    items:
      - "[CLOSED 2026-07-03] OpenCode memory-wakeup plugin (opencode/plugins/memory-wakeup.ts:19,30) reads ~/.claude/hooks/memory-wakeup.sh and fails open if absent. Fixed: docs/operating.md §OpenCode now documents the Claude-first install ordering."
  - phase: bookkeeping
    items:
      - "REQUIREMENTS.md traceability + PROJECT.md list 5/6 reqs as Pending/Active with unchecked boxes. These are stale checkboxes, not incomplete work — reconciled by the PROJECT.md evolution + checkbox update inside /gsd-complete-milestone."
---

# Milestone v1.0 — Audit: "OSS core → parity"

**Status: tech_debt** — all 6 requirements satisfied by code + live-test evidence; no critical blockers. One real tech-debt item (OpenCode wakeup install-ordering) plus routine pre-close bookkeeping.

## Method note

Cairnkeep predates GSD plan-tracking. There are **no** `SUMMARY.md`, `VERIFICATION.md`, or `VALIDATION.md` files — only one phase dir (`02-operating-layer-verification/02-LEARNINGS.md`). The workflow's standard 3-source cross-reference (VERIFICATION + SUMMARY frontmatter + traceability) is therefore not applicable. Instead, each requirement was verified **directly against the codebase** and by **running the memory-server build and smoke suite**. This is stronger evidence than a paper trail, not weaker.

## Requirements coverage (6/6 satisfied)

| REQ-ID | Phase | Status | Evidence |
|--------|-------|--------|----------|
| REQ-provider-neutral-core | 1 | ✅ satisfied | `docs/git-providers.md` — one `CAIRN_GIT_PROVIDER` key + operation→tool map; `git grep` for hardcoded git hosts in `mcp-memory-server/src`, `scripts`, `bin` → none |
| REQ-memory-mcp-server | 2 | ✅ satisfied | `mcp-memory-server/src/index.ts:598+` registers 10 MCP tools on stdio + opt-in token-gated HTTP; `npm run build` clean; `smoke-scope-guard`, `smoke-search-e2e`, `smoke-extract-cli`, `smoke-embeddings` all pass |
| REQ-operating-layer | 2 | ✅ satisfied (⚠ 1 debt) | `claude/{commands,agents,hooks}` + `opencode/` assets present; `sync-claude-assets.sh` registers hooks on SessionStart/SessionEnd/PreToolUse. Debt: OpenCode wakeup ordering (below) |
| REQ-cli-bootstrap | 3 | ✅ satisfied | `bin/cairn` → `scripts/bootstrap.sh` writes `.ai/` + `.planning/` from `templates/`; STATE.md documents fresh-bootstrap parity verified into a scratch `--live-root` |
| REQ-feature-parity | 3 | ✅ satisfied | `docs/operating.md` present (setup order, workflow reference, config table); baseline `v1.0.0` tag at HEAD |
| REQ-oss-hygiene | 3 | ✅ satisfied | Apache-2.0 `LICENSE` (real-name copyright); `.github/workflows/ci.yml` runs `npm ci && build && test` on push/PR; no tracked secrets |

## Live verification run (this audit)

```
npm run build        → exit 0 (tsc -p tsconfig.json, clean)
npm test (smoke)     → all pass
  ok: scope-guard    (7 assertions incl. scope 'all' write-reject)
  ok: http-guard     (5 assertions: no-token refuse, 401/403, valid 200)
  ok: extract-cli    (empty-stdin reject, api-key required)
  ok: search-e2e     → exit 0 (fallback substring mode)
  ok: embeddings     → exit 0 (explicit-model config)
git ls-files | grep secrets/.env/.key → none tracked
```

## Tech debt

**Phase 2 — OpenCode wakeup install-ordering (WARNING, non-blocking)**
`opencode/plugins/memory-wakeup.ts:19,30` reuses the rendered Claude hook `~/.claude/hooks/memory-wakeup.sh` as the single source of truth and fails open when it's absent. An OpenCode-only install (per `docs/operating.md` §OpenCode, which doesn't state the dependency) gets a silent no-op wakeup — no error. Fix: document the Claude-first ordering in `docs/operating.md`, or have `sync-opencode-plugin-assets.sh --check` warn when the Claude hook isn't installed. Claude Code is the complete, verified path.

**Bookkeeping (resolved by milestone close)**
REQUIREMENTS.md traceability and PROJECT.md still show 5/6 reqs as Pending/Active with unchecked boxes. Stale markers, not incomplete work — the PROJECT.md evolution + checkbox update is a step inside `/gsd-complete-milestone`.

## Verdict

No unsatisfied requirements → the FAIL gate does **not** fire. Milestone is functionally complete. The single tech-debt item (OpenCode wakeup ordering) is safe to accept and track, or close with a one-line docs edit before completing.
