---
phase: 09-live-verification-a-b-token-savings
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - .planning/phases/09-live-verification-a-b-token-savings/09-AB.md
autonomous: false
requirements: [CTX-07]
user_setup:
  - service: fastcontext-backend
    why: "The measured A/B run and the live /context-explore run both require the operator's FastContext server + token_miser explore binary (runtime prerequisites, D-04)."
    env_vars:
      - name: CAIRN_EXPLORE_BINARY
        source: "Operator's ambient shell / gitignored .ai/.env — absolute path to the token_miser binary"
      - name: CAIRN_EXPLORE_REPO_ROOT
        source: "Operator's ambient shell — target repo root (defaults to cairnkeep's own repo per D-02)"
must_haves:
  truths:
    - "Running the committed harness `--full` against the real backend yields a measured per-query + median before/after byte delta plus a provider-neutral token estimate (SC-2)"
    - "The measured number and the D-03 verdict are recorded in the committed `09-AB.md` and referenced from the phase UAT/SUMMARY (SC-2)"
    - "At least one live `/context-explore` command runs end-to-end on Claude Code against the real backend, transcript captured (SC-3)"
    - "An un-standable backend is recorded as a documented gap, never a silent skip, and does not block milestone close-out (D-04)"
  artifacts:
    - .planning/phases/09-live-verification-a-b-token-savings/09-AB.md
  key_links:
    - "09-AB.md measured number <-> harness evidence-log output (byte delta + median + verdict)"
    - "SC-3 transcript <-> live /context-explore run (claude/commands/context-explore.md)"
  prohibitions:
    - "No endpoint host/IP or model name copied from the live transcript into the committed 09-AB.md (DEC-no-private-references)"
    - "No source changes to context_explore (Phase 7) or the /context-explore commands (Phase 8)"
    - "Backend bring-up must never block milestone close-out; document the gap and continue (D-04)"
---

<objective>
Run the committed A/B harness live against the real FastContext backend to produce cairnkeep's own measured before/after number (SC-2), run one live `/context-explore` command on Claude Code end-to-end (SC-3), and record the measured number + D-03 verdict + live transcript in a committed `09-AB.md` verdict doc referenced from the phase UAT/SUMMARY. Backend bring-up is operator-gated and non-blocking on close-out (D-04); an un-standable backend is a documented gap, not a silent skip.

Purpose: Closes CTX-07 with a measured (not cited) number and corroborates the explore side against the real backend — the same verify-by-execution bar v1.0/v1.1 held.
Output: `.planning/phases/09-live-verification-a-b-token-savings/09-AB.md` (committed).
</objective>

<execution_context>
@/home/stondo/PARA/Projects/cairnkeep/.claude/gsd-core/workflows/execute-plan.md
@/home/stondo/PARA/Projects/cairnkeep/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-live-verification-a-b-token-savings/09-CONTEXT.md
@.planning/phases/09-live-verification-a-b-token-savings/09-01-PLAN.md
@claude/commands/context-explore.md
</context>

<artifacts>
## Artifacts this phase produces (Plan 02)

- `.planning/phases/09-live-verification-a-b-token-savings/09-AB.md` — the committed verdict doc recording: the query-to-pattern-to-window-to-cap recipe table (copied from the harness header, for auditability), the measured per-query + median before/after byte delta, the `chars/4` token estimate, the D-03 PASS/FAIL verdict, and the live SC-3 `/context-explore` transcript (or the documented backend-gap note). Referenced from the phase UAT/SUMMARY (SC-2).
</artifacts>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Operator-gated live run — measured A/B (--full) + one live /context-explore (SC-2, SC-3)</name>
  <action>
    Operator-only step (the agent cannot reach the private FastContext infra — DEC-no-private-references, D-04). The operator stands up the FastContext backend + `token_miser` binary (runtime prerequisites), exports CAIRN_EXPLORE_BINARY (and optionally CAIRN_EXPLORE_REPO_ROOT / `.ai/.env`, never committed), runs `scripts/verify-token-savings-ab.sh --full` to produce the measured per-query + median before/after byte delta + `chars/4` token estimate + D-03 verdict (SC-2), and runs one live `/context-explore` command inside a Claude Code session at this repo root to corroborate the explore side (SC-3). The operator reports the redacted measured numbers + live citation output — OR reports the backend un-standable, which is a documented gap per D-04 (never a silent skip) and does not block close-out. See <how-to-verify> for exact steps.
  </action>
  <what-built>
    The committed, offline-self-tested A/B harness `scripts/verify-token-savings-ab.sh` (Plan 01) and the unchanged live `/context-explore` command (claude/commands/context-explore.md, Phase 8). Both need the operator's FastContext server + token_miser binary, which cannot be provisioned by the agent (DEC-no-private-references, D-04).
  </what-built>
  <how-to-verify>
    Operator, with the FastContext backend up and `CAIRN_EXPLORE_BINARY` (and optionally `CAIRN_EXPLORE_REPO_ROOT`/`.ai/.env`) exported in your shell:

    1. Run the measured A/B (SC-2):
       `scripts/verify-token-savings-ab.sh --full`
       Capture stdout + the per-query table, median byte-savings %, `chars/4` token estimate, and the PASS/FAIL verdict it prints. (Add `--repo <path>` to additionally point at a fresh `cairn bootstrap` scratch project per D-02 if desired.)

    2. Run one live operating-layer command (SC-3) inside a Claude Code session at this repo root:
       `/context-explore Where is scope path containment implemented in this repo?`
       Capture the returned compact `path:line-range` citation list verbatim (this is the live corroboration of the explore side).

    3. Paste back: the measured numbers from step 1 and the citation output from step 2 — with any endpoint host/IP or model name redacted.

    If the backend cannot be stood up: report "backend un-standable" with the reason. This is a documented gap per D-04, NOT a failure — milestone close-out proceeds and Task 2 records the gap.
  </how-to-verify>
  <resume-signal>Paste the measured A/B numbers + the live citation output, OR type "backend un-standable: <reason>" to record the documented gap. Then Task 2 records the result.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record the measured number + live transcript + D-03 verdict in the committed 09-AB.md, reference from UAT/SUMMARY</name>
  <files>.planning/phases/09-live-verification-a-b-token-savings/09-AB.md</files>
  <read_first>
    - scripts/verify-token-savings-ab.sh (header comment) — copy the query-to-pattern-to-window-to-cap recipe table verbatim so the recorded baseline is auditable and reproducible.
    - .planning/phases/06-fastcontext-reliability-spike/06-SPIKE.md — the verdict-doc role/shape to mirror (measured evidence + a clear verdict line).
  </read_first>
  <action>
    Create `.planning/phases/09-live-verification-a-b-token-savings/09-AB.md` recording the CTX-07 A/B result from the checkpoint.

    Include: (1) the native-exploration recipe table (query -> grep pattern -> window -> hit cap) copied from the harness header, with a one-line note that it models a focused native agent and is a conservative floor on native cost; (2) the measured per-query before/after byte counts, the byte delta, the savings %, and the `chars/4` provider-neutral token estimate — stating explicitly that the byte delta is the anchor and the token count is an estimate (D-01a); (3) the median byte-savings % and the D-03 verdict: PASS when median net savings > 0, or a loud documented finding when <= 0 (never a silent pass) — noting cairnkeep's number need not match FastContext's ~60% paper figure (D-03); (4) the live SC-3 `/context-explore` transcript (redacted citation output) as corroboration of the explore side.

    If the operator reported the backend un-standable: record the documented gap in place of the measured number and the live transcript — state clearly that SC-2/SC-3 live evidence is a known gap per D-04, that the offline harness (`--self-test`, `--native`) still passes, and that this does NOT block milestone close-out.

    Do NOT copy any endpoint host/IP or model name into this committed doc (DEC-no-private-references). Add a one-line reference to `09-AB.md` in the phase UAT or SUMMARY doc so SC-2's "recorded in the phase's UAT/SUMMARY docs" is literally satisfied.
  </action>
  <verify>
    <automated>test -f .planning/phases/09-live-verification-a-b-token-savings/09-AB.md && grep -qiE 'byte delta|byte-savings|documented gap' .planning/phases/09-live-verification-a-b-token-savings/09-AB.md && grep -qiE 'verdict|PASS|FAIL|gap' .planning/phases/09-live-verification-a-b-token-savings/09-AB.md && ! grep -nEi 'https?://(?!127\.0\.0\.1|localhost)' .planning/phases/09-live-verification-a-b-token-savings/09-AB.md</automated>
  </verify>
  <done>`09-AB.md` exists, records the recipe table + measured byte delta/median + `chars/4` estimate + D-03 verdict (or the documented D-04 gap), carries no non-loopback endpoint or model name, and is referenced from the phase UAT/SUMMARY (SC-2). SC-3 transcript (or the documented gap) is captured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live transcript / harness stdout -> committed `09-AB.md` | The operator's live run may surface the real endpoint/model; only redacted byte counts, citations, and the verdict may cross into the committed doc. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-04 | Information Disclosure | committed `09-AB.md` | high | mitigate | Operator redacts host/model in the resume paste; Task 2 verify greps `09-AB.md` for any non-loopback host and fails on a match; only byte counts / citations / verdict are recorded. |
| T-09-05 | Repudiation | measured number | medium | mitigate | The recipe table (query/pattern/window/cap) is recorded alongside the number so the baseline is reproducible and auditable, not an unbacked claim (D-01/D-02). |
| T-09-06 | Denial of Service | backend bring-up blocking close-out | medium | accept | D-04: backend is an operator-provided runtime prerequisite; an un-standable backend is a documented gap, not a close-out blocker — recorded, never silently skipped. |
</threat_model>

<verification>
- `09-AB.md` exists and records the recipe table + measured byte delta + median + `chars/4` estimate + D-03 verdict, OR a clearly documented D-04 backend gap.
- The live SC-3 `/context-explore` transcript (redacted) is captured, or its absence is the documented gap.
- `09-AB.md` carries no non-loopback endpoint host/IP or model name (DEC-no-private-references).
- `09-AB.md` is referenced from the phase UAT/SUMMARY (SC-2 literal satisfaction).
</verification>

<success_criteria>
- A measured (not cited-from-paper) before/after token count is recorded and reported in the phase's UAT/SUMMARY docs [SC-2].
- At least one operating-layer `/context-explore` command is run live end-to-end against the real backend [SC-3].
- The D-03 gate is honored (net savings > 0 = PASS; <= 0 = loud documented finding).
- CTX-07 is closed with cairnkeep's own measured number (or a documented D-04 backend gap that does not block close-out).
</success_criteria>

<output>
Create `.planning/phases/09-live-verification-a-b-token-savings/09-02-SUMMARY.md` when done.
</output>
