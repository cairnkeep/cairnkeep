---
phase: 09-live-verification-a-b-token-savings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-token-savings-ab.sh
autonomous: true
requirements: [CTX-07]
must_haves:
  truths:
    - "`--self-test` proves the byte-delta arithmetic, the chars/N token estimate, and the D-03 net-savings gate offline — no backend required (Nyquist backstop)"
    - "`--native --repo <path>` deterministically counts the bytes a fixed grep->window recipe pulls into context, per query, against a git repo"
    - "`--explore` reproduces the exact renderCitations `path:line-range` shape from Evidence JSON and counts its bytes; fails loud when the binary/backend is absent (D-04)"
    - "Byte/char delta is the reported anchor; the token number is a provider-neutral `chars/4` estimate — never a vendor tokenizer (D-01a)"
  artifacts:
    - scripts/verify-token-savings-ab.sh
  key_links:
    - "explore-side byte counting <-> renderCitations shape (mcp-memory-server/src/index.ts:604)"
    - "harness env vars <-> context_explore's CAIRN_EXPLORE_BINARY / CAIRN_EXPLORE_REPO_ROOT"
  prohibitions:
    - "No non-loopback endpoint host/IP, model alias, or vendor tokenizer committed (DEC-no-private-references)"
    - "No source changes to context_explore (Phase 7) or the /context-explore commands (Phase 8)"
    - "No new exploration capability, caching, annotation, or UI (CTX-F1..F3 deferred)"
    - "No swap of the exploration backend"
---

<objective>
Build the committed, re-runnable A/B token-savings harness `scripts/verify-token-savings-ab.sh` — the measurement instrument for CTX-07. It computes the native-exploration ("before") vs `context_explore` ("after") byte/char delta deterministically, reports the raw byte delta as the anchor plus a provider-neutral token estimate, and gates on net savings > 0 (D-03). Every stage except `--explore`'s live half is offline-self-testable so the harness lands and passes CI without a backend.

Purpose: A milestone-closing headline number must not depend on a flaky live model (D-01). The harness makes the delta deterministic and re-runnable; Plan 02 runs it live to record the measured number.
Output: `scripts/verify-token-savings-ab.sh` (new, committed).
</objective>

<execution_context>
@/home/stondo/PARA/Projects/cairnkeep/.claude/gsd-core/workflows/execute-plan.md
@/home/stondo/PARA/Projects/cairnkeep/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-live-verification-a-b-token-savings/09-CONTEXT.md
@.planning/phases/09-live-verification-a-b-token-savings/09-PATTERNS.md
</context>

<artifacts>
## Artifacts this phase produces (Plan 01)

- `scripts/verify-token-savings-ab.sh` — committed, env-driven, loopback-only, staged A/B harness. Stages: `--self-test` (offline Nyquist backstop), `--native` (offline, reads the local git repo), `--explore` (operator-gated live half), `--full` (both sides + per-query delta + median + D-03 verdict), `-h|--help`.
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Harness scaffold — header, env config, presence-only logging, byte/char + token-estimate helpers, D-03 net-savings gate, main() dispatch</name>
  <files>scripts/verify-token-savings-ab.sh</files>
  <read_first>
    - scripts/verify-fastcontext-reliability.sh (lines 1-74, 172-188, 587-624) — copy the header discipline, ROOT_DIR resolution, env-var-with-loopback-default block, log_*_presence presence-not-value idiom, append_evidence helper, and the main() case-dispatch shape verbatim in spirit.
    - .planning/phases/09-live-verification-a-b-token-savings/09-CONTEXT.md (D-01a, D-03) — byte anchor + provider-neutral estimate; record-and-net-savings gate.
  </read_first>
  <action>
    Create `scripts/verify-token-savings-ab.sh` with a bash shebang and `set -euo pipefail`. Mirror the Phase 6 harness structure (per D-01 re-runnability + DEC-no-private-references).

    Header comment block: state this is the CTX-07 A/B token-savings harness; the byte/char delta is the tokenizer-free ground-truth anchor and the token number is a provider-neutral `chars/4` estimate (D-01a); no endpoint host/IP, model name, or vendor tokenizer is ever committed or written to the evidence log — only loopback/placeholder defaults are committed and real values come from the ambient shell / gitignored `.ai/.env` (DEC-no-private-references).

    `usage()`: one `Usage:` line per stage (`--self-test`, `--native`, `--explore`, `--full`, `-h|--help`), a sentence per flag naming what it proves, and an `Environment:` section documenting CAIRN_EXPLORE_BINARY, CAIRN_EXPLORE_REPO_ROOT, the `--repo` override, and the evidence-log path — noting each is read from the ambient shell and never echoed.

    Config block: set ROOT_DIR to the repo root via the BASH_SOURCE dirname idiom. Read CAIRN_EXPLORE_BINARY (the real env var context_explore uses; default empty), CAIRN_EXPLORE_REPO_ROOT (default ROOT_DIR — cairnkeep's own repo per D-02), and AB_EVIDENCE_LOG defaulting to a `*.log` under `.planning/phases/09-live-verification-a-b-token-savings/` (the repo `.gitignore` already excludes `*.log`). Accept a `--repo <path>` CLI override for the repo root (D-02 fresh-bootstrap coverage). Reuse the append_evidence helper verbatim and add a log_repo_presence helper that logs only whether `--repo`/env overrode the default, never the path value.

    Helper functions: count_bytes via `wc -c`, count_chars via `wc -m`, and est_tokens(chars) computing integer chars-divided-by-4 — document N=4 inline as the industry-standard ~4-chars/token English approximation, chosen because it is provider-neutral and dependency-free while the byte delta remains the mandatory anchor (D-01a). Do NOT add or vendor any tokenizer.

    net_savings_gate(native_bytes, explore_bytes) (D-03): returns 0 (PASS) only when native minus explore is strictly greater than zero; returns 1 (FAIL) otherwise and prints a loud stderr + evidence line stating net savings were not positive and this is a documented finding, not a silent pass (D-03). This is the milestone-close gate: an honest number that shows net savings, or a loud documented finding — never a silent pass.

    main() case dispatch over `--self-test` / `--native` / `--explore` / `--full` / `-h|--help`, with the default case printing usage to stderr and exiting 2. Wire `--native`/`--explore`/`--full` to the stage functions Task 2 adds (stub functions Task 2 fills in are acceptable here). End with the `main "$@"` invocation.

    Do NOT commit any non-loopback host, model alias, or tokenizer file. Make the script executable.
  </action>
  <verify>
    <automated>bash -n scripts/verify-token-savings-ab.sh && scripts/verify-token-savings-ab.sh --help >/dev/null; if command -v shellcheck >/dev/null 2>&1; then shellcheck -S error scripts/verify-token-savings-ab.sh; fi</automated>
  </verify>
  <done>Script parses (`bash -n`), `--help` exits 0 and documents all stages + env vars, config reads CAIRN_EXPLORE_BINARY/CAIRN_EXPLORE_REPO_ROOT/`--repo` with loopback-safe defaults, and the byte/char + `chars/4` estimate + D-03 gate helpers exist.</done>
</task>

<task type="auto">
  <name>Task 2: Measurement stages — `--native` (fixed grep->window recipe), `--explore` (renderCitations-shape byte count, fail-loud), `--full` (per-query delta + median + D-03 verdict)</name>
  <files>scripts/verify-token-savings-ab.sh</files>
  <read_first>
    - mcp-memory-server/src/index.ts (lines 604-615) — the exact renderCitations `path:start_line-end_line` newline-joined shape (and the empty-citations note shape) the explore side must reproduce byte-for-byte. Do NOT count the full Evidence JSON.
    - mcp-memory-server/src/index.ts (lines 1010-1082) — the explore subcommand invocation (`<binary> explore --query <q> --repo-root <root>`) with a color-disabled env and the Evidence JSON shape citations[]{path,start_line,end_line}, stats{turns,tool_calls}.
    - scripts/verify-fastcontext-reliability.sh (lines 138-149, 396-412) — the curated cairnkeep query set to reuse and the fail-loud-on-missing-prerequisite idiom.
  </read_first>
  <action>
    Add the query set and the three stage functions to the harness.

    Query set + native recipe table (D-02, reproducible/auditable): reuse the 5 curated cairnkeep exploration prompts from `verify-fastcontext-reliability.sh` (the EXPLORATION_PROMPTS array) as the default set (satisfies 3–5). For each query define a fixed regex pattern that yields at least one hit against cairnkeep's repo, a fixed context window of plus-or-minus 25 lines, and a documented per-query hit cap of 12 (models a focused native agent reading the top grep hits; keeps the "before" number bounded and honest so an over-broad pattern cannot inflate savings). Record the final query-to-pattern-to-window-to-cap table in the harness header comment so the baseline is auditable (Plan 02 copies it into 09-AB.md).

    run_native(repo) — the "before" side, offline (reads only the local git repo, no backend). For each query: run `git -C <repo> grep -nE "<pattern>"`, take the first 12 `path:line` hits, for each extract a window of the surrounding plus-or-minus 25 lines from the file via `sed -n`, dedup identical (path,window) spans, concatenate, and count the total bytes and chars. Emit a per-query line to stdout + evidence with the query index, hit count, byte count, and char count. Use `git grep` (gitignore-aware, tracked-files-only) so the recipe is deterministic across runs. Store per-query native bytes/chars for `--full`.

    run_explore(repo) — the "after" side, live half (D-04 operator-gated). Resolve the binary from CAIRN_EXPLORE_BINARY (fall back to `token_miser` on PATH). If neither is set/executable, print + append_evidence a loud FATAL line naming the missing exploration binary as a documented gap per D-04 (never a silent skip) and return non-zero. Otherwise, for each query run the binary as `<binary> explore --query "<q>" --repo-root "<repo>"` with a color-disabled env and a generous `timeout` (e.g. 120s); on timeout or non-zero exit, record a loud FATAL line and return non-zero (fail-loud). Parse stdout Evidence JSON and reproduce the renderCitations shape in one `jq -r` expression producing `path:start_line-end_line` lines joined by newlines, or the exact empty-citations note string (with turns and tool_calls interpolated) when citations is empty — then count the bytes and chars of that citation text only (NOT the JSON). Emit a per-query line with the query index, citation count, byte count, and char count. Never echo the binary path or any endpoint value.

    run_full(repo) — runs run_native then run_explore over the same query set; if run_explore returns non-zero, record the documented gap and exit non-zero (D-04 fail-loud). For each query compute the byte delta (native minus explore), the char delta, the `chars/4` token-estimate delta, and the savings percentage; compute the median byte-savings percentage across queries. Run net_savings_gate on the median (D-03). Write a per-query table + the median + the token estimate + the PASS/FAIL verdict to the evidence log. Exit 0 only when the gate passes. Reaffirm in the log that no endpoint/model/secret value was written.
  </action>
  <verify>
    <automated>scripts/verify-token-savings-ab.sh --native --repo . >/tmp/ab_native.out 2>&1 && grep -q 'query=' /tmp/ab_native.out; CAIRN_EXPLORE_BINARY='' scripts/verify-token-savings-ab.sh --explore --repo . >/tmp/ab_explore.out 2>&1; test $? -ne 0 && grep -qi 'documented gap' /tmp/ab_explore.out</automated>
  </verify>
  <done>`--native --repo .` prints per-query native byte/char counts and exits 0 with no backend; `--explore` with the binary unset exits non-zero and prints the documented-gap message (D-04 fail-loud); `--full` computes per-query delta + median and applies the D-03 gate. The explore side counts the renderCitations text shape, not the Evidence JSON.</done>
</task>

<task type="auto">
  <name>Task 3: `--self-test` offline Nyquist backstop — fixtures asserting byte-delta arithmetic, chars/4 estimate, D-03 gate both directions, and the renderCitations-shape reproduction</name>
  <files>scripts/verify-token-savings-ab.sh</files>
  <read_first>
    - scripts/verify-fastcontext-reliability.sh (lines 450-585) — the self_test_* fixture-assertion pattern (fixture in, scoring function, assert expected PASS/FAIL, print `[self-test:X] OK`/`FAIL`, nonzero return on any failure) and run_self_test aggregation.
  </read_first>
  <action>
    Add a run_self_test entry point and per-concern self-test functions, wired to the `--self-test` case in main(). No network, no backend, no live binary.

    self_test_delta: assert count_bytes/count_chars on a fixed string return the known length, and that est_tokens on a known char count returns the expected chars-divided-by-4 integer.

    self_test_gate (D-03, both directions): with a native-bytes > explore-bytes fixture assert net_savings_gate returns 0 (PASS); with a native-bytes <= explore-bytes fixture (equal and less-than) assert it returns non-zero AND emits the loud documented-finding line — proving a regression cannot pass silently.

    self_test_render: feed a canned Evidence JSON fixture (two citations with path/start_line/end_line, plus stats.turns and stats.tool_calls) through the same jq expression run_explore uses, and assert the output equals the expected `path:start-end` newline-joined string; feed an empty-citations fixture and assert the output equals the exact empty-citations note string. This gives the explore-side byte-counting logic offline coverage without a backend.

    run_self_test runs all three, prints a `[self-test] PASSED`/`FAILED` summary, and returns non-zero if any failed. This is the automated Nyquist backstop for the operator-gated live run in Plan 02.
  </action>
  <verify>
    <automated>scripts/verify-token-savings-ab.sh --self-test</automated>
  </verify>
  <done>`--self-test` exits 0 and prints `[self-test] PASSED`; the gate fixtures prove both PASS (savings>0) and loud FAIL (savings<=0) directions; the render fixture proves the explore side reproduces the exact citation-text shape offline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell / `.ai/.env` -> committed harness | The real FastContext endpoint, model alias, and binary path live only in the operator's ambient shell; they must never cross into the committed script or the evidence log. |
| `token_miser explore` stdout -> harness | Untrusted subprocess output (Evidence JSON, possibly stderr echoing config) parsed by the harness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Information Disclosure | committed `verify-token-savings-ab.sh` | high | mitigate | Only loopback/empty defaults committed; endpoint/model/binary path read from ambient shell at runtime; presence-only logging (`log_repo_presence`), never the value. Verified by grep for non-loopback hosts (Plan 02 secret-scan). |
| T-09-02 | Information Disclosure | `AB_EVIDENCE_LOG` | high | mitigate | Evidence log path is a `*.log` already gitignored; the log records byte counts/verdict only, and the run_full close reaffirms no endpoint/model/secret value was written (mirrors Phase 6 scrub-check). |
| T-09-03 | Tampering | subprocess Evidence JSON parse | medium | mitigate | Malformed/timeout/non-zero from the explore binary is a loud FATAL + non-zero exit (fail-closed, D-04), never a silent empty pass. |
| T-09-SC | Tampering | package installs | low | accept | No npm/pip/cargo installs this phase — bash + curl/jq/git/sed/wc + the operator-supplied `token_miser` binary, all already present. No package-legitimacy gate required. |
</threat_model>

<verification>
- `scripts/verify-token-savings-ab.sh --self-test` exits 0 (offline; proves delta arithmetic, chars/4 estimate, D-03 gate both directions, renderCitations-shape reproduction).
- `scripts/verify-token-savings-ab.sh --native --repo .` exits 0 offline and prints per-query native byte/char counts.
- `scripts/verify-token-savings-ab.sh --explore` with no exploration binary exits non-zero with a documented-gap message (D-04 fail-loud).
- No non-loopback host, model alias, or vendor tokenizer appears in the committed script.
</verification>

<success_criteria>
- The committed harness computes the native-vs-explore byte/char delta deterministically and reports the byte delta as the anchor plus a `chars/4` provider-neutral token estimate (D-01a) [SC-1].
- Every stage but `--explore`'s live half is offline-self-testable; `--self-test` is a passing Nyquist backstop.
- The D-03 net-savings gate cannot pass silently on a regression (loud documented finding on savings <= 0).
- CTX-07 measurement instrument exists and is re-runnable (D-01).
</success_criteria>

<output>
Create `.planning/phases/09-live-verification-a-b-token-savings/09-01-SUMMARY.md` when done.
</output>
