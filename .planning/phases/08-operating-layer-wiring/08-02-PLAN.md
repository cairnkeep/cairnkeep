---
phase: 08-operating-layer-wiring
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - scripts/sync-opencode-explore-assets.sh
  - docs/operating.md
autonomous: true
requirements: [CTX-05]

must_haves:
  truths:
    - "The OpenCode /context-explore command installs via a dedicated scripts/sync-opencode-explore-assets.sh that mirrors the existing sync-opencode-*-assets.sh siblings (D-04)."
    - "The script round-trips cleanly: --apply into a live root then --check reports in-sync (parity with the five sibling scripts)."
    - "docs/operating.md lists the new script for install (Setup order OpenCode) and its --check as a sanity check beside sync-claude-assets.sh --check (D-04 documentation parity)."
    - "The sync script contains no FastContext endpoint/model/host/IP or vendor default (DEC-no-private-references)."
  artifacts:
    - scripts/sync-opencode-explore-assets.sh
    - docs/operating.md
  key_links:
    - "ASSETS array contains exactly `command/context-explore.md` (the file written in Plan 01), so ensure_source_assets_exist finds its source."
    - "Flags --check (default) / --apply / --live-root / -h behave identically to sync-opencode-wiki-assets.sh."
    - "docs/operating.md references sync-opencode-explore-assets.sh in both the OpenCode setup list and the verification section."
---

<objective>
Create the dedicated `scripts/sync-opencode-explore-assets.sh` install/drift script for the OpenCode `/context-explore` command, and add its documentation parity to `docs/operating.md` (D-04). This completes the install-path half of CTX-05 and keeps the one-script-per-feature convention every other OpenCode feature already has (memory, wiki, security, graphify, plugin — this is the sixth).

D-04 says to wire the script's `--check` into CI "the same way the other `sync-opencode-*-assets.sh --check` runs are invoked." VERIFIED CORRECTION (RESEARCH.md Pitfall 1 / PATTERNS.md, confirmed this session by `grep -rn "sync-opencode\|sync-claude" .github/workflows/` → no matches): **no such CI invocation exists.** All five existing sibling scripts are manual/documented-only; `.github/workflows/ci.yml`'s single `memory-server` job never touches `scripts/sync-*.sh`. The de-facto existing pattern is a documented manual `--check` in `docs/operating.md`, not a CI job. Therefore D-04 is honored as **documentation parity**: add the new script beside the sibling `--apply` install commands and add its `--check` beside `sync-claude-assets.sh --check` in the verification section. Inventing a first-of-its-kind CI job is deliberately OUT OF SCOPE — no success criterion demands CI, "thin wiring" is the phase constraint, and doing so would silently introduce new CI scope not applied to the five siblings. This assumption is surfaced here explicitly rather than chosen silently.

Purpose: Deliver the install path for CTX-05 (OpenCode command installed via a `sync-opencode-*-assets.sh` script, parity with Claude).
Output: One new sync script + a `docs/operating.md` edit.
</objective>

<execution_context>
@/home/stondo/PARA/Projects/cairnkeep/.claude/gsd-core/workflows/execute-plan.md
@/home/stondo/PARA/Projects/cairnkeep/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-operating-layer-wiring/08-CONTEXT.md
@.planning/phases/08-operating-layer-wiring/08-RESEARCH.md
@.planning/phases/08-operating-layer-wiring/08-PATTERNS.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

New symbols created by THIS plan (source-grounding: these are net-new, not drift):

- `scripts/sync-opencode-explore-assets.sh` — new dedicated OpenCode install/drift script (CTX-05 install path).
  - CLI flags: `--check` (default), `--apply`, `--live-root PATH`, `-h`/`--help` (identical to the sibling `sync-opencode-*-assets.sh` scripts).
  - `ASSETS` array entry: `command/context-explore.md` (the only managed asset; no `LEGACY_ASSETS`).
- `docs/operating.md` — edited (not a new symbol): the OpenCode setup list gains a `sync-opencode-explore-assets.sh --apply` line, and the verification section gains its `--check` sanity line.

Consumes (from Plan 01, not created here): `opencode/command/context-explore.md` — the source asset the script's ASSETS array references.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/sync-opencode-explore-assets.sh (D-04, CTX-05 install path)</name>
  <files>scripts/sync-opencode-explore-assets.sh</files>
  <read_first>
    - scripts/sync-opencode-wiki-assets.sh — the full 250-line template to copy-and-narrow. Reuse verbatim: header/usage block, `ROOT_DIR`/`SOURCE_ROOT`/`LIVE_ROOT`/`MODE` setup, arg-parse loop (`--check`/`--apply`/`--live-root`/`-h`), `ensure_source_assets_exist`, `report_extra_live_assets`, `run_check`, `run_apply`, and the final `case "$MODE"` dispatch.
    - opencode/command/context-explore.md — the single source asset this script manages (must already exist from Plan 01; `ensure_source_assets_exist` will fail with exit 2 if it does not).
    - scripts/sync-opencode-graphify-assets.sh — a single-asset sibling for reference on how a narrow ASSETS array looks in practice.
    - .planning/phases/08-operating-layer-wiring/08-PATTERNS.md — the "scripts/sync-opencode-explore-assets.sh" section listing exactly which blocks to copy verbatim, narrow, and drop.
  </read_first>
  <action>
    Create `scripts/sync-opencode-explore-assets.sh` as a structural copy of `scripts/sync-opencode-wiki-assets.sh`, narrowed to this feature:

    - Keep the `#!/usr/bin/env bash` + `set -euo pipefail` header and the `ROOT_DIR`/`SOURCE_ROOT="$ROOT_DIR/opencode"`/`LIVE_ROOT="${OPENCODE_CONFIG_DIR:-$HOME/.config/opencode}"`/`MODE="check"` setup unchanged.
    - `ASSETS` array contains exactly one entry: `command/context-explore.md`. Nothing else.
    - Drop the `LEGACY_ASSETS` array entirely, the `report_legacy_live_assets` function entirely, and its call sites inside `run_check` and `run_apply` (the wiki script calls it after `report_extra_live_assets`). Also drop the legacy-removal loop and the `removed_legacy` counter from `run_apply`, and simplify `run_apply`'s final printf to report only applied/unchanged counts (no legacy count). This is a brand-new feature — there is nothing to migrate away from.
    - Narrow `collect_extra_live_assets` to a single scan of the command dir: `find "$root/command" -maxdepth 1 -type f -name 'context-explore*.md' -printf 'command/%f\n' 2>/dev/null || true`. Drop the agents/workflows/templates/gsd-overrides find calls (this feature ships one file only, per D-01).
    - Keep the `--check`/`--apply`/`--live-root`/`-h` arg-parse loop, `ensure_source_assets_exist`, `report_extra_live_assets`, `run_check` (minus the legacy call), `run_apply` (minus legacy), and the `case "$MODE"` dispatch unchanged in structure.
    - Update user-facing strings: the usage/help text and the `run_check`/`run_apply` messages say "explore asset(s)" instead of "wiki asset(s)"; the header comment says "OpenCode explore assets"; remove the usage "Notes:" line about removing legacy gsd-overrides files (no longer applicable). Rewrite the usage line "This script manages only the wiki-specific OpenCode assets" to say it manages the explore command asset.
    - Contains no endpoint/model/host/IP/vendor default (DEC-no-private-references) — the wiki template has none either, so this holds by copying.
    - Make the file executable (`chmod +x`).
  </action>
  <verify>
    <automated>bash -n scripts/sync-opencode-explore-assets.sh && test -x scripts/sync-opencode-explore-assets.sh && grep -q '"command/context-explore.md"' scripts/sync-opencode-explore-assets.sh && tmp=$(mktemp -d) && bash scripts/sync-opencode-explore-assets.sh --apply --live-root "$tmp" && bash scripts/sync-opencode-explore-assets.sh --check --live-root "$tmp" && rm -rf "$tmp" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/sync-opencode-explore-assets.sh` reports no syntax errors and the file is executable.
    - `ASSETS` contains exactly `command/context-explore.md` (`grep -q '"command/context-explore.md"'` matches; no other `command/` or `agents/` or `workflows/` or `templates/` entries).
    - Round-trip against a temp live root succeeds: `--apply --live-root $tmp` exits 0, then `--check --live-root $tmp` exits 0 and reports the assets in sync.
    - No `LEGACY_ASSETS` array and no `report_legacy_live_assets` function remain (the copy was narrowed, not left with dead legacy handling).
    - `grep -niE 'http://|https://|127\.0\.0\.1|localhost|enterprise|:8080|:11434|gguf|llama-server' scripts/sync-opencode-explore-assets.sh` returns no matches (DEC-no-private-references).
  </acceptance_criteria>
  <done>A dedicated `scripts/sync-opencode-explore-assets.sh` exists, mirrors the sibling sync scripts, manages exactly `command/context-explore.md`, round-trips clean (--apply then --check in sync) against a temp live root, and carries no private references.</done>
</task>

<task type="auto">
  <name>Task 2: Add docs/operating.md parity for the new sync script (D-04)</name>
  <files>docs/operating.md</files>
  <read_first>
    - docs/operating.md — read the "Setup order (OpenCode)" section (the fenced block listing the five `sync-opencode-*-assets.sh --apply` commands) and the "Verifying the install" section (the bulleted list including `scripts/sync-claude-assets.sh --check`). These are the two insertion points.
    - .planning/phases/08-operating-layer-wiring/08-RESEARCH.md — Open Question 1 / Pitfall 1: documentation parity (not CI) is the correct D-04 fulfillment; no sibling script has real CI automation.
  </read_first>
  <action>
    Make two surgical insertions in `docs/operating.md` (use Edit, not Write):

    1. In the "Setup order (OpenCode)" fenced command block, add a line after the `sync-opencode-graphify-assets.sh --apply` line: `scripts/sync-opencode-explore-assets.sh --apply   # context-explore command`. Align the trailing comment with the existing sibling lines' column style.

    2. In the "Verifying the install" bulleted list, add a bullet directly after the existing `Claude operating layer: scripts/sync-claude-assets.sh --check ...` bullet: an "OpenCode operating layer" line stating that `scripts/sync-opencode-explore-assets.sh --check` (and its sibling `sync-opencode-*-assets.sh --check` runs) should report no drift after an apply. Keep it a manual sanity-check line — do NOT claim any CI job runs it (none exists; see RESEARCH.md Pitfall 1).

    Touch nothing else in the file. No endpoint/model/host names (DEC-no-private-references).
  </action>
  <verify>
    <automated>[ "$(grep -c 'sync-opencode-explore-assets.sh' docs/operating.md)" -ge 2 ] && grep -q 'sync-opencode-explore-assets.sh --apply' docs/operating.md && grep -q 'sync-opencode-explore-assets.sh --check' docs/operating.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `docs/operating.md` references `sync-opencode-explore-assets.sh` at least twice (once in the OpenCode setup `--apply` list, once as a `--check` verification line).
    - The `--apply` line sits inside the "Setup order (OpenCode)" command block among the other five sibling scripts.
    - The `--check` line sits in the "Verifying the install" section beside the existing `sync-claude-assets.sh --check` bullet and is framed as a manual sanity check (no false CI claim).
    - No other sections of `docs/operating.md` are changed; no private references introduced.
  </acceptance_criteria>
  <done>docs/operating.md documents the new sync script for both install (`--apply`) and drift-check (`--check`), giving the OpenCode command the same documented install+verify parity every sibling feature has, with no invented CI claim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo source → live config tree | The sync script copies repo-managed `./opencode/command/context-explore.md` into the live OpenCode config root (`$OPENCODE_CONFIG_DIR` or `$HOME/.config/opencode`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04 | Tampering | sync-script `--live-root` target | low | accept | Same trust model as the five existing sibling scripts (`cmp -s` + `install`/`cp` into a user-owned config dir); no new privilege boundary. `--live-root` is a developer-supplied local path, not attacker-controlled input. |
| T-08-05 | Information Disclosure | sync script + docs | medium | mitigate | DEC-no-private-references: no endpoint/model/host/IP/vendor default in the script or the docs edit; enforced by the `grep -niE` acceptance criterion on the script (copied from the private-reference-free wiki template). |
| T-08-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs zero external packages (RESEARCH.md Package Legitimacy Audit: N/A) — no supply-chain surface. |
</threat_model>

<verification>
- `bash -n scripts/sync-opencode-explore-assets.sh` clean; file executable; `ASSETS` = exactly `command/context-explore.md`.
- Round-trip: `--apply --live-root $tmp` then `--check --live-root $tmp` both exit 0 and report in-sync (functional proof the script installs and detects drift like its siblings).
- `docs/operating.md` references `sync-opencode-explore-assets.sh` for both `--apply` and `--check`.
- DEC-no-private-references `grep -niE` scan passes on the script.
- D-04 fulfillment note: no CI job is added (verified: `.github/workflows/` has no `sync-*` reference); documentation parity is the honest fulfillment consistent with all five sibling scripts.
</verification>

<success_criteria>
- CTX-05 (install path): the OpenCode `/context-explore` command installs via the new dedicated `sync-opencode-explore-assets.sh`, mirroring the existing asset-sync pattern (D-04) — parity with Claude (whose command auto-installs via `sync-claude-assets.sh`).
- The script round-trips clean and is documented in `docs/operating.md` for install and drift-check.
</success_criteria>

<output>
Create `.planning/phases/08-operating-layer-wiring/08-02-SUMMARY.md` when done.
</output>
