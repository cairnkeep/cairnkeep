---
phase: 04-opencode-parity-operating-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - opencode/command/remember.md
  - opencode/command/recall.md
  - scripts/sync-opencode-memory-assets.sh
autonomous: true
requirements: [OCP-03, OCP-04]
must_haves:
  truths:
    - "Running remember in OpenCode persists a durable finding to AgentFS project scope (OCP-03)"
    - "Running recall in OpenCode retrieves known info across AgentFS + wiki + optional AnythingLLM (OCP-04)"
    - "The two commands install via the existing sync-opencode-memory-assets.sh (per D-12) with no new sync script"
  artifacts:
    - opencode/command/remember.md
    - opencode/command/recall.md
    - scripts/sync-opencode-memory-assets.sh
  key_links:
    - "remember.md/recall.md frontmatter tools: map -> OpenCode command permission model (not Claude allowed-tools:)"
    - "sync-opencode-memory-assets.sh ASSETS[] -> live ~/.config/opencode/command install"
---

<objective>
Port the `remember` (write half) and `recall` (read half) memory commands from the verified Claude path to OpenCode's command layer, and wire them into the existing OpenCode memory-asset sync script. Per D-06 the OpenCode `remember` drops the Claude-only file-memory layer (that directory is a Claude-runtime artifact, not part of the shared parity contract) and treats AgentFS as the sole durable structured store; per D-07 every other, layer-agnostic semantic is preserved verbatim.

Purpose: Give OpenCode users the same `remember`/`recall` surface Claude has, backed by the shared `cairn-memory` MCP.
Output: `opencode/command/remember.md`, `opencode/command/recall.md`, and the extended `scripts/sync-opencode-memory-assets.sh`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-opencode-parity-operating-layer/04-CONTEXT.md
@.planning/phases/04-opencode-parity-operating-layer/04-RESEARCH.md
@claude/commands/remember.md
@claude/commands/recall.md
@opencode/command/memory-sync.md
@scripts/sync-opencode-memory-assets.sh
</context>

## Artifacts this phase produces

- `opencode/plugins/memory-wakeup.ts` (rewritten, OCP-05) — native surfacing, no `~/.claude` shell-out
- `opencode/plugins/memory-capture.ts` (new, OCP-01) — `session.idle` → `extract` → staging
- `opencode/plugins/memory-recall.ts` (new, OCP-02) — `tool.execute.before` → stem match → throw-to-surface
- `opencode/command/remember.md` (new, OCP-03, this plan) — AgentFS write + doc-layer flagging
- `opencode/command/recall.md` (new, OCP-04, this plan) — AgentFS + wiki + optional AnythingLLM read
- `scripts/sync-opencode-plugin-assets.sh` — `ASSETS[]` += `plugins/memory-capture.ts`, `plugins/memory-recall.ts`; new `@@INFRA_ROOT@@`→`$ROOT_DIR` rendering
- `scripts/sync-opencode-memory-assets.sh` (this plan) — `ASSETS[]` += `command/remember.md`, `command/recall.md`

<tasks>

<task type="auto">
  <name>Task 1: Author opencode/command/remember.md (OCP-03)</name>
  <read_first>
    - claude/commands/remember.md (the write-half semantics to port)
    - opencode/command/memory-sync.md (OpenCode command frontmatter house style: `tools:` map, MCP tool references)
    - .planning/phases/04-opencode-parity-operating-layer/04-CONTEXT.md (D-06 drop file-memory, D-07 preserve layer-agnostic semantics verbatim)
    - .planning/phases/04-opencode-parity-operating-layer/04-RESEARCH.md (MCP tool naming: `cairn-memory_<tool>` single underscore, not `mcp__cairn-memory__<tool>`)
  </read_first>
  <files>
    - opencode/command/remember.md
  </files>
  <action>
    Write `opencode/command/remember.md` as the OpenCode port of `claude/commands/remember.md`. Frontmatter uses the OpenCode `tools:` boolean map house style from `memory-sync.md` (`read`, `write`, `edit`, `grep`, `glob`, plus the cairn-memory MCP tools) — NOT Claude's `allowed-tools:` list. Reference MCP tools by their OpenCode surface name `cairn-memory_memory_read`, `cairn-memory_memory_search`, `cairn-memory_memory_write`, `cairn-memory_memory_supersede` (single underscore). Preserve verbatim (D-07): the empty-`$ARGUMENTS` guard (ask and stop, never invent a fact), dedupe-before-write via `memory_search` scope `project`, write via `memory_write` scope `project` with a short kebab-case key OR `memory_supersede` when a near-duplicate key should keep its prior value for audit, relative-to-absolute date conversion, no em dashes or double-hyphen dashes, and the flag-don't-run rule for the document layers (suggest `/wiki-ingest <path>` for stable doc-worthy synthesis and `domain_knowledge_sync` for changed repo docs, never auto-execute them). Per D-06, the durable write path has exactly two targets: AgentFS (project scope, direct write) and the flagged-only document layers — there is no Claude file-memory step and no home-directory memory path. Update the layer-shapes context block accordingly so it names AgentFS as the sole direct-write structured store.
  </action>
  <acceptance_criteria>
    - `opencode/command/remember.md` frontmatter uses a `tools:` map (`grep -c '^tools:' opencode/command/remember.md` returns 1) and does not use a Claude `allowed-tools:` line
    - The write path lists exactly two durable targets — AgentFS (direct write) and the flagged-only doc layers (wiki, AnythingLLM) — with no third file-memory target and no home-directory memory path
    - References the cairn-memory MCP write tool by its OpenCode name (`grep -c 'cairn-memory_memory_write' opencode/command/remember.md` returns >= 1)
    - Preserves the empty-argument guard, dedupe-before-write, absolute-date conversion, dash rule, and doc-layer flag-don't-run behavior
  </acceptance_criteria>
  <verify>
    <automated>test -f opencode/command/remember.md && [ "$(grep -c '^tools:' opencode/command/remember.md)" -eq 1 ] && grep -q 'cairn-memory_memory_write' opencode/command/remember.md && ! grep -q '^allowed-tools:' opencode/command/remember.md</automated>
  </verify>
  <done>remember.md is an OpenCode-native command that writes durable findings to AgentFS and flags (not runs) the doc layers, with the Claude file-memory step dropped per D-06.</done>
</task>

<task type="auto">
  <name>Task 2: Author opencode/command/recall.md (OCP-04)</name>
  <read_first>
    - claude/commands/recall.md (the read-half semantics to port verbatim)
    - opencode/command/memory-sync.md (frontmatter house style)
    - .planning/phases/04-opencode-parity-operating-layer/04-CONTEXT.md (D-06/D-07)
    - .planning/phases/04-opencode-parity-operating-layer/04-RESEARCH.md (MCP naming convention)
  </read_first>
  <files>
    - opencode/command/recall.md
  </files>
  <action>
    Write `opencode/command/recall.md` as the OpenCode port of `claude/commands/recall.md`. Frontmatter uses the `tools:` map plus the read-side cairn-memory MCP tools referenced by OpenCode name: `cairn-memory_memory_read`, `cairn-memory_memory_search`, `cairn-memory_domain_knowledge_query`. Preserve the read order verbatim (D-07): (1) AgentFS project scope via `memory_search` scope `project`, retry scope `all` if nothing relevant; (2) read `.planning/wiki/index.md` if present and follow links relevant to `$ARGUMENTS`; (3) optional `domain_knowledge_query` against the project workspace only if AgentFS and wiki are insufficient. Keep the report step: summarize what each layer returned, and when layers disagree prefer the canonical raw source and note staleness.
  </action>
  <acceptance_criteria>
    - `opencode/command/recall.md` frontmatter uses a `tools:` map (`grep -c '^tools:' opencode/command/recall.md` returns 1) and no `allowed-tools:` line
    - Read order is AgentFS (`memory_search`, retry scope all) → `.planning/wiki/index.md` → optional `domain_knowledge_query`, matching Claude recall.md
    - References `cairn-memory_memory_search` and `cairn-memory_domain_knowledge_query` by OpenCode name
  </acceptance_criteria>
  <verify>
    <automated>test -f opencode/command/recall.md && [ "$(grep -c '^tools:' opencode/command/recall.md)" -eq 1 ] && grep -q 'cairn-memory_memory_search' opencode/command/recall.md && grep -q 'domain_knowledge_query' opencode/command/recall.md && ! grep -q '^allowed-tools:' opencode/command/recall.md</automated>
  </verify>
  <done>recall.md is an OpenCode-native cross-layer read command matching Claude recall semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Wire remember.md and recall.md into sync-opencode-memory-assets.sh</name>
  <read_first>
    - scripts/sync-opencode-memory-assets.sh (the ASSETS[] array and idempotent --check/--apply mechanism)
    - .planning/phases/04-opencode-parity-operating-layer/04-CONTEXT.md (D-12: extend the existing ASSETS array, no new sync script)
  </read_first>
  <files>
    - scripts/sync-opencode-memory-assets.sh
  </files>
  <action>
    Add `command/remember.md` and `command/recall.md` to the `ASSETS` array in `scripts/sync-opencode-memory-assets.sh` (per D-12). Add nothing else — the script's idempotent `install`/`cmp` copy loop already handles new command markdown with no INFRA_ROOT rendering needed (commands carry no server path). Verify by applying into a scratch live-root and re-checking.
  </action>
  <acceptance_criteria>
    - The `ASSETS` array in `scripts/sync-opencode-memory-assets.sh` contains both `command/remember.md` and `command/recall.md`
    - `bash scripts/sync-opencode-memory-assets.sh --apply --live-root "$(mktemp -d)"` exits 0 and its trailing `--check` reports memory/review assets in sync
    - A second `--apply` against the same scratch root reports all files already matched (idempotent)
  </acceptance_criteria>
  <verify>
    <automated>grep -q '"command/remember.md"' scripts/sync-opencode-memory-assets.sh && grep -q '"command/recall.md"' scripts/sync-opencode-memory-assets.sh && D=$(mktemp -d) && bash scripts/sync-opencode-memory-assets.sh --apply --live-root "$D" && bash scripts/sync-opencode-memory-assets.sh --check --live-root "$D"</automated>
  </verify>
  <done>Both new commands install idempotently via the existing memory-asset sync script.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenCode command → cairn-memory MCP | `$ARGUMENTS` (user-supplied fact/topic) crosses into MCP tool calls |
| sync script → live OpenCode config tree | `install` copies command markdown into `~/.config/opencode/command` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07 | Tampering | `remember` writing unbounded/duplicate AgentFS keys | low | mitigate | Preserve the dedupe-before-write and supersede-for-audit semantics from Claude remember.md; AgentFS scope-path containment is already enforced server-side (Phase 2 SEC-0001) |
| T-04-05 | Information Disclosure | `recall` echoing stored memory/wiki content into model context | low | accept | Content is the project's own memory/wiki, not secrets; no new data source is introduced — only the existing read surface ported to OpenCode |
| T-04-SC | Tampering | command frontmatter referencing MCP tools | low | accept | Commands are markdown; no package install occurs. The cairn-memory server contract is v1.0-validated and unchanged this phase |
</threat_model>

<verification>
- Both command files exist with OpenCode `tools:` frontmatter (no `allowed-tools:`), correct MCP tool names, and Claude-parity semantics minus the D-06 file-memory drop.
- `sync-opencode-memory-assets.sh` installs both commands idempotently into a scratch live-root.
</verification>

<success_criteria>
OpenCode has `remember` and `recall` commands at parity with the Claude path (OCP-03, OCP-04), installable via the existing memory-asset sync script.
</success_criteria>

<output>
Create `.planning/phases/04-opencode-parity-operating-layer/04-02-SUMMARY.md` when done.
</output>
