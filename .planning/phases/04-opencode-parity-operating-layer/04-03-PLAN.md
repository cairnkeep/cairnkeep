---
phase: 04-opencode-parity-operating-layer
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - opencode/plugins/memory-wakeup.ts
autonomous: true
requirements: [OCP-05]
must_haves:
  truths:
    - "Starting an OpenCode session surfaces AgentFS project memory, the wiki index, open HARD contradictions, and a staged-candidates count"
    - "The wakeup plugin resolves the cairn-memory server via a rendered repo path, not a ~/.claude hook shell-out"
    - "The plugin is a guarded, fail-open no-op outside a managed repo"
  artifacts:
    - opencode/plugins/memory-wakeup.ts
  key_links:
    - "memory-wakeup.ts -> node @@INFRA_ROOT@@/mcp-memory-server/dist/index.js wakeup (rendered at install by 04-06)"
    - "04-01 CHOSEN-CHANNEL decision -> the injection mechanism used here"
---

<objective>
Rewrite `opencode/plugins/memory-wakeup.ts` to be self-sufficient of Claude assets (OCP-05, D-04). Today the plugin shells out to `~/.claude/hooks/memory-wakeup.sh`; the rewrite reimplements that surfacing natively in TypeScript, calling the shared `cairn-memory` server directly and reading the same `.planning/` files, so the OpenCode wakeup works with no Claude-rendered assets on disk.

The hard acceptance bar (verified in Plan 04-06): after installing only the OpenCode assets and with no reachable `~/.claude`, an OpenCode session's opening turn still reflects surfaced AgentFS memory.

Purpose: Remove the last Claude-asset dependency from the OpenCode path.
Output: the rewritten `opencode/plugins/memory-wakeup.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-opencode-parity-operating-layer/04-CONTEXT.md
@.planning/phases/04-opencode-parity-operating-layer/04-RESEARCH.md
@.planning/phases/04-opencode-parity-operating-layer/04-SPIKE-INJECTION.md
@opencode/plugins/memory-wakeup.ts
@claude/hooks/memory-wakeup.sh
</context>

## Artifacts this phase produces

- `opencode/plugins/memory-wakeup.ts` (rewritten, OCP-05, this plan) — native surfacing, no `~/.claude` shell-out
- `opencode/plugins/memory-capture.ts` (new, OCP-01) — `session.idle` → `extract` → staging
- `opencode/plugins/memory-recall.ts` (new, OCP-02) — `tool.execute.before` → stem match → throw-to-surface
- `opencode/command/remember.md` (new, OCP-03) — AgentFS write + doc-layer flagging
- `opencode/command/recall.md` (new, OCP-04) — AgentFS + wiki + optional AnythingLLM read
- `scripts/sync-opencode-plugin-assets.sh` — `ASSETS[]` += `plugins/memory-capture.ts`, `plugins/memory-recall.ts`; new `@@INFRA_ROOT@@`→`$ROOT_DIR` rendering
- `scripts/sync-opencode-memory-assets.sh` — `ASSETS[]` += `command/remember.md`, `command/recall.md`

<tasks>

<task type="auto">
  <name>Task 1: Reimplement memory-wakeup.ts natively (D-04)</name>
  <read_first>
    - opencode/plugins/memory-wakeup.ts (the plugin skeleton: Plugin type, `$` shell handle, once-per-session `surfaced` Set, injection hook)
    - claude/hooks/memory-wakeup.sh (the exact four-section surfacing logic to replicate, including the CONTRADICTIONS awk region + `severity: hard` filter and the staged-count logic)
    - .planning/phases/04-opencode-parity-operating-layer/04-SPIKE-INJECTION.md (the CHOSEN-CHANNEL decision from Plan 04-01)
    - .planning/phases/04-opencode-parity-operating-layer/04-CONTEXT.md (D-03 fail-open, D-04 surfacing, D-05 INFRA_ROOT resolution)
  </read_first>
  <files>
    - opencode/plugins/memory-wakeup.ts
  </files>
  <action>
    Rewrite the plugin so it no longer imports `node:os` to build a home-directory hook path and no longer shells out to the Claude hook. Resolve the server entry from the install-rendered token `@@INFRA_ROOT@@` (per D-05: `@@INFRA_ROOT@@/mcp-memory-server/dist/index.js`; Plan 04-06 adds the rendering step to the sync script — mirror the token exactly as the Claude hooks use it). Surface the same four sections `memory-wakeup.sh` produces, assembled natively: (1) `## Project memory (AgentFS)` from `node @@INFRA_ROOT@@/mcp-memory-server/dist/index.js wakeup` via the `$` shell handle; (2) `## Wiki index` = contents of `.planning/wiki/index.md`; (3) `## Open HARD contradictions` = the `severity: hard` lines inside the `wiki:contradictions:open:start`/`end` region of `.planning/wiki/CONTRADICTIONS.md`; (4) `## Staged memory candidates (N session(s))` = count of `.planning/memory-staging/*.json` with the run-`/memory-review` prompt. Guard: no-op unless `.agentfs/project.db` OR `.planning/wiki/index.md` exists (relative to the OpenCode working directory / repo root — do NOT rely on `process.cwd()` if it can differ from the project root; resolve the repo root the same way the guard file check implies). Keep the once-per-session dedupe via the `surfaced` Set keyed on `sessionID`. Deliver the assembled text through the injection channel recorded as CHOSEN-CHANNEL in 04-01: if `system.transform`, push onto `output.system` exactly as the existing plugin does; if `instruction-file`, write/refresh an `AGENTS.md`-style instruction file that OpenCode's instruction-file discovery loads at system-prompt assembly (guard it to refresh once per session, and only within a managed repo). Wrap all work in `try/catch` and bail silently on any error or missing input (D-03 fail-open) — never wedge a session.
  </action>
  <acceptance_criteria>
    - `opencode/plugins/memory-wakeup.ts` invokes `node @@INFRA_ROOT@@/mcp-memory-server/dist/index.js wakeup` (`grep -c '@@INFRA_ROOT@@' opencode/plugins/memory-wakeup.ts` returns >= 1) and contains no `bash ${HOOK}`-style shell-out to a home-directory hook
    - The plugin assembles all four sections: AgentFS project memory, wiki index, open HARD contradictions (severity hard, open region only), and the staged-candidates count
    - The plugin is a guarded no-op unless `.agentfs/project.db` or `.planning/wiki/index.md` exists, retains the once-per-session `surfaced` dedupe, and wraps its body in try/catch (fail-open)
    - The injection channel used matches the `CHOSEN-CHANNEL` recorded in `04-SPIKE-INJECTION.md`
    - The shared server contract is untouched: `cd mcp-memory-server && npm run typecheck` and `npm run check:extract` still pass
  </acceptance_criteria>
  <verify>
    <automated>grep -q '@@INFRA_ROOT@@' opencode/plugins/memory-wakeup.ts && grep -q 'mcp-memory-server/dist/index.js' opencode/plugins/memory-wakeup.ts && grep -q 'surfaced' opencode/plugins/memory-wakeup.ts && grep -q 'catch' opencode/plugins/memory-wakeup.ts && ! grep -q 'homedir' opencode/plugins/memory-wakeup.ts && cd mcp-memory-server && npm run check:extract</automated>
  </verify>
  <done>memory-wakeup.ts natively surfaces all four session-start sections via the shared server and the chosen injection channel, with no Claude-asset shell-out, guarded and fail-open. The live no-Claude-assets acceptance test runs in Plan 04-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenCode session → wakeup plugin | session-start hook runs plugin code with the session context |
| wakeup plugin → local cairn-memory server | plugin executes `node <INFRA_ROOT>/mcp-memory-server/dist/index.js wakeup` |
| wakeup plugin → `.planning/` filesystem | plugin reads wiki index, CONTRADICTIONS.md, and the staging dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Elevation of Privilege | INFRA_ROOT resolving to an attacker-controlled path → arbitrary `node` target | medium | mitigate | Resolve the server path only from the install-rendered `@@INFRA_ROOT@@` (repo root computed by the sync script in 04-06), never from env or session input; do not accept a runtime-overridable server path |
| T-04-05 | Information Disclosure | surfaced AgentFS/wiki/contradiction content echoed into model context | low | accept | Content is the project's own memory/wiki, not secrets; this is the same surface the Claude wakeup already exposes |
| T-04-04 | Denial of Service | fail-open abuse (induced errors suppress surfacing) | low | accept | D-03 deliberately trades enforcement for availability — the plugin must never wedge a session; missing surfacing is an acceptable degraded state |
| T-04-SC | Tampering | `@opencode-ai/plugin` (SUS scanner verdict) | high | mitigate | Type-only, runtime-resolved import (no install), matching the existing plugin; a blocking-human checkpoint verifying `npmjs.com/package/@opencode-ai/plugin` is required only if an executor introduces a `package.json` install |
</threat_model>

<verification>
- `memory-wakeup.ts` resolves the server via `@@INFRA_ROOT@@`, surfaces all four sections, keeps the once-per-session dedupe, is guarded + fail-open, and has no home-directory hook shell-out.
- Server contract intact (`npm run check:extract`).
- Live "no reachable ~/.claude, wakeup still surfaces" acceptance is exercised in Plan 04-06.
</verification>

<success_criteria>
OpenCode wakeup surfaces session-start context natively via the shared server, self-sufficient of Claude assets (OCP-05).
</success_criteria>

<output>
Create `.planning/phases/04-opencode-parity-operating-layer/04-03-SUMMARY.md` when done.
</output>
