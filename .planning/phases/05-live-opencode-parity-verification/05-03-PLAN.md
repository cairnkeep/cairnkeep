---
phase: 05-live-opencode-parity-verification
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - .planning/phases/05-live-opencode-parity-verification/05-UAT.md
  - docs/operating.md
autonomous: false
requirements: [OCP-06]
tags: [opencode, verification, uat, evidence, interactive-session, docs]

must_haves:
  truths:
    - "05-UAT.md exists with one test per stage (wakeup, recall-on-edit, capture, remember->recall, negative controls) recording expected / result / raw evidence inline — canary IDs, command output, model responses, negative-control results — so the execution record stands on its own (D-05)."
    - "05-UAT.md explicitly names OCP-01/02/03/04 as the Phase-4-deferred (04-UAT test 2) live round-trip discharged here, closing the audit trail rather than orphaning it."
    - "One genuine interactive OpenCode session (D-01) confirms the workflow in a real live session, OR the harness-only fallback is recorded as an explicit gap in 05-UAT.md — never silently dropped."
    - "docs/operating.md no longer instructs operators to install Claude assets before the OpenCode plugin sync, and no longer claims the OpenCode memory-wakeup plugin depends on a rendered Claude hook — it reflects the Phase-4 self-sufficiency (D-04)."
  artifacts:
    - ".planning/phases/05-live-opencode-parity-verification/05-UAT.md — the execution-evidence artifact (D-05)"
    - "docs/operating.md — stale OpenCode-wakeup precondition corrected (RESEARCH Pitfall 5)"
  key_links:
    - "05-UAT.md's evidence is the raw output of 05-02's harness main() run plus the interactive session — not a re-read of Phase-4 evidence."
    - "The interactive session (D-01) is the literal 'in a live OpenCode session' bar the ROADMAP wording requires; its result (pass or fallback-gap) is recorded in 05-UAT.md."
  prohibitions:
    - "DEC-no-private-references [LOCKED]: every canary and scratch value transcribed into 05-UAT.md is a synthetic test string; no private host/vendor/path or private repo name appears in the UAT, the docs edit, or commits."
    - "DEC-no-ai-authorship [LOCKED]: no AI/assistant authorship references in the UAT, docs, or commit messages."
    - "The docs edit is a documentation correction only — no new capability, no change to the OpenCode plugins or commands."
---

<objective>
Capture the live parity run as the standard GSD 05-UAT.md with raw evidence inline (D-05), run the one genuine interactive OpenCode session that satisfies the literal "in a live OpenCode session" bar (D-01), and correct the one stale documentation line the phase's own proof contradicts (RESEARCH Pitfall 5).

This closes OCP-06: the four owed 04-UAT test-2 items (OCP-01/02/03/04 live round-trip) plus the integrated wakeup->recall->capture and remember->recall round-trips are recorded as execution evidence, matching how v1.0 verified the Claude path by execution.

Purpose: turn the harness's deterministic run + the interactive confirmation into the durable, self-standing evidence record and fix the one doc line that would otherwise mislead an operator.
Output: 05-UAT.md (evidence), a corrected docs/operating.md, and the interactive-session result recorded.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-live-opencode-parity-verification/05-CONTEXT.md
@.planning/phases/05-live-opencode-parity-verification/05-RESEARCH.md
@.planning/phases/05-live-opencode-parity-verification/05-VALIDATION.md
@.planning/phases/05-live-opencode-parity-verification/05-02-SUMMARY.md
@.planning/phases/04-opencode-parity-operating-layer/04-UAT.md
@scripts/verify-opencode-live-parity.sh
@docs/operating.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full harness and author 05-UAT.md with raw evidence inline</name>
  <files>.planning/phases/05-live-opencode-parity-verification/05-UAT.md</files>
  <read_first>
    - .planning/phases/04-opencode-parity-operating-layer/04-UAT.md (the GSD UAT shape to match: numbered tests with expected / result / evidence; test 2 lists the exact OCP-01/02/03/04 live items owed to this phase)
    - .planning/phases/05-live-opencode-parity-verification/05-02-SUMMARY.md (the per-stage results and synthetic canary values from the harness run)
    - scripts/verify-opencode-live-parity.sh (the harness whose main() full run produces the evidence)
    - .planning/phases/05-live-opencode-parity-verification/05-CONTEXT.md § D-05 (raw evidence inline; close the owed items explicitly)
  </read_first>
  <action>
    Run the full scripted harness (scripts/verify-opencode-live-parity.sh full mode) end to end against the live model + registered cairn-memory MCP, capturing the raw output.

    Author .planning/phases/05-live-opencode-parity-verification/05-UAT.md in the standard GSD UAT structure (mirror 04-UAT.md's frontmatter + numbered-tests + Summary + Gaps shape). Write one test per stage: wakeup (OCP-05 re-confirmed live), recall-on-edit (OCP-02), capture (OCP-01), remember->recall (OCP-03/OCP-04), and the negative-control sweep. For each: state expected, result (pass/fail), and embed raw evidence inline per D-05 — the synthetic canary IDs used, the command output, the model responses (FOUND / NOT-FOUND / echoed fact), the staged-JSON filename and its canary content, and each negative-control result. Record which local model endpoint was actually used (D-04 fallback allowed — note it).

    Add an explicit statement that this UAT discharges the four OCP-01/02/03/04 live round-trip items deferred from Phase 4 (04-UAT test 2), naming them, so the audit trail from 04-UAT is closed rather than orphaned. If 05-02 applied an in-scope memory-capture.ts fix, note it in the capture test's evidence.

    Every value transcribed is synthetic; no private host/path appears (DEC-no-private-references). Leave the interactive-session test result to be filled by Task 3.
  </action>
  <verify>
    <automated>bash scripts/verify-opencode-live-parity.sh --full && test -f .planning/phases/05-live-opencode-parity-verification/05-UAT.md && grep -q 'OCP-01' .planning/phases/05-live-opencode-parity-verification/05-UAT.md && grep -q 'OCP-04' .planning/phases/05-live-opencode-parity-verification/05-UAT.md && echo "OK: harness full run green + UAT authored naming owed items"</automated>
  </verify>
  <done>
    The full harness run completes green. 05-UAT.md exists with one test per stage, raw evidence inline (synthetic canary IDs, command output, model responses, negative-control results, endpoint used), and an explicit statement discharging OCP-01/02/03/04 (04-UAT test 2). No private host/path in the file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct the stale OpenCode-wakeup precondition in docs/operating.md</name>
  <files>docs/operating.md</files>
  <read_first>
    - docs/operating.md lines 84-92 (the OpenCode install section and the stale precondition paragraph immediately after the sync-script block)
    - opencode/plugins/memory-wakeup.ts (proves the plugin is self-sufficient: it surfaces AgentFS memory natively via experimental.chat.system.transform and references no Claude asset anywhere)
    - .planning/phases/05-live-opencode-parity-verification/05-RESEARCH.md § Common Pitfalls #5 (the stale line and why it now misleads)
  </read_first>
  <action>
    In docs/operating.md, replace the bolded precondition paragraph that follows the OpenCode sync-script block — the one instructing operators to install the Claude assets before the OpenCode plugin sync and claiming the OpenCode memory-wakeup plugin reuses a rendered Claude hook as its single source of truth. That coupling is false as of Phase 4 (D-04 rewrote the plugin to be self-sufficient — the current opencode/plugins/memory-wakeup.ts references no Claude asset). Replace it with an accurate one-line note that the OpenCode memory-wakeup plugin is self-sufficient of Claude assets and surfaces session-start memory natively — no Claude asset install is required for the OpenCode path.

    This is a surgical documentation correction only — do not restructure the surrounding sections, do not touch any Claude-path documentation that legitimately references the Claude hook, and change no code.
  </action>
  <verify>
    <automated>! grep -q 'Install the Claude assets first' docs/operating.md && grep -qi 'self-sufficient' docs/operating.md && echo "OK: stale OpenCode-wakeup precondition removed; self-sufficiency noted"</automated>
  </verify>
  <done>
    docs/operating.md no longer tells operators to install Claude assets before the OpenCode plugin sync and no longer claims the OpenCode wakeup plugin depends on a rendered Claude hook; it accurately states the plugin is self-sufficient. Claude-path documentation is untouched; no code changed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Interactive live OpenCode session (D-01 literal live-session bar)</name>
  <action>This is a blocking human-verify checkpoint — do NOT automate it. Present the verification steps below to the operator and wait for the resume signal. The scripted harness (05-02) already proves each stage headlessly; this checkpoint is the one genuine interactive OpenCode session the ROADMAP's "in a live OpenCode session" wording requires (D-01). Record the outcome (pass, or the D-01 harness-only fallback gap) in 05-UAT.md before resuming — never silently drop it.</action>
  <what-built>
    A fully scripted, green live-parity harness (05-02) and an authored 05-UAT.md (Task 1) recording the wakeup -> recall-on-edit -> capture lifecycle and the remember -> recall round-trip against the registered cairn-memory MCP in a scratch-isolated OpenCode env with no reachable ~/.claude. What remains is the one genuine interactive OpenCode session the ROADMAP's "in a live OpenCode session" wording requires (D-01).
  </what-built>
  <how-to-verify>
    1. In a scratch project with HOME pointed at a scratch dir (the harness's setup_scratch establishes this; reuse it or replicate it), launch the OpenCode TUI: `opencode` in the scratch project.
    2. Observe the session-start turn surfaces the seeded project memory (wakeup).
    3. Edit a stem-matching file and confirm the recall context is injected before the edit proceeds.
    4. Run `/remember <a fresh synthetic fact>`, then in a later turn of the same conversation run `/recall <that topic>` and confirm it is returned — a live same-session round trip.
    5. Confirm the whole flow worked in one continuous live conversation.
    D-01 fallback: if a genuine interactive session is impractical at execution time, record that explicitly as a gap in 05-UAT.md (the scripted harness already proves each stage) — do NOT silently drop it. Either way, record the interactive result (pass or fallback-gap) in 05-UAT.md before resuming.
  </how-to-verify>
  <verify>
    <human-check>Operator confirms the interactive session surfaced session-start memory, injected recall context on a matching edit, and round-tripped /remember -> /recall in one continuous live conversation — OR the D-01 harness-only fallback gap is recorded in 05-UAT.md. Real ~/.config/opencode and ~/.claude confirmed untouched afterward.</human-check>
  </verify>
  <done>The interactive session outcome (pass or fallback-gap) is recorded in 05-UAT.md; if pass, the full live workflow ran in one continuous OpenCode conversation; real config untouched.</done>
  <resume-signal>Type "approved" (interactive session confirmed) or "fallback" (record the harness-only gap in 05-UAT.md), or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness/interactive session → filesystem | The interactive session runs under a scratch HOME; the real ~/.config/opencode and ~/.claude must remain untouched |
| evidence transcription → committed 05-UAT.md | Only synthetic canary values and non-private output may cross into the committed record |
| local model endpoint (loopback) → recorded evidence | Model output is transcribed as evidence, never executed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | committed 05-UAT.md + docs/operating.md | high | mitigate | Every canary/scratch value transcribed is a synthetic test string; scan the UAT and docs edit for any private host/vendor/path before commit (DEC-no-private-references, DEC-commit-scanning) |
| T-05-02 | Tampering | interactive session under scratch HOME | high | mitigate | The interactive session runs under a scratch HOME; confirm real ~/.config/opencode and ~/.claude are unmodified afterward (git status / dir listing) |
| T-05-05 | Repudiation (false evidence) | 05-UAT.md evidence provenance | medium | mitigate | Evidence is the raw output of THIS phase's harness run + interactive session, not a re-read of Phase-4 evidence; negative-control results are recorded alongside each positive result |
| T-05-SC | Tampering (supply chain) | package installs | low | accept | No package installs; documentation + evidence only |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- `.planning/phases/05-live-opencode-parity-verification/05-UAT.md` — the execution-evidence artifact (D-05): one test per stage, raw evidence inline, explicitly discharging OCP-01/02/03/04 (04-UAT test 2), plus the interactive-session result (pass or fallback-gap).
- `docs/operating.md` — the stale OpenCode-wakeup precondition corrected to reflect Phase-4 self-sufficiency (RESEARCH Pitfall 5).

Synthetic values only; no private host/path committed. The scratch env and scratch opencode.json used for the interactive session remain ephemeral and cleaned up.
</artifacts_this_phase_produces>

<verification>
- Full harness `--full` run is green (Task 1 automated check).
- 05-UAT.md exists with per-stage tests, raw synthetic evidence inline, and the explicit OCP-01/02/03/04 discharge statement.
- docs/operating.md no longer carries the stale Claude-asset precondition and states self-sufficiency; Claude-path docs untouched.
- Interactive session result (pass or fallback-gap) recorded in 05-UAT.md.
- Real ~/.config/opencode and ~/.claude untouched; no private host/path in any committed file.
</verification>

<success_criteria>
- OCP-06 is proven by execution and recorded: the full lifecycle + remember/recall round-trip against the registered cairn-memory MCP, a fresh OpenCode-only install reproducing the workflow, and the parity run captured as 05-UAT.md — matching the v1.0 verify-by-execution bar.
- The one interactive live session (D-01) is completed or its fallback recorded as an explicit gap.
- The stale doc line that contradicts the proven self-sufficiency is corrected.
</success_criteria>

<output>
Create `.planning/phases/05-live-opencode-parity-verification/05-03-SUMMARY.md` when done. Reference 05-UAT.md as the evidence artifact and record the interactive-session outcome (pass or fallback-gap).
</output>
