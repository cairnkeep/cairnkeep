---
phase: 26-guided-setup-and-harness-onboarding
plan: "09"
type: execute
wave: 7
depends_on: ["26-08"]
files_modified: [docs/learning/lessons/L23-guided-setup.md, docs/learning/README.md, docs/learning/CURRICULUM-MAP.md, docs/learning/FEATURE-GUIDE.md, docs/learning/tracks/quickstart.md, docs/learning/tracks/operator.md, scripts/phase26-test-manifest.mjs, scripts/verify-pi-mcp-bridge.mjs]
autonomous: true
requirements: [SETUP-05, SETUP-06, PI-MCP-01, PI-MCP-02, PI-MCP-03]
must_haves:
  truths:
    - "A reachable Ready L23 lesson teaches deterministic setup, Pi local stdio, Git-less limits, recovery/uninstall, exact D-12 annotation boundaries, and privacy with tested version metadata (D-18)."
    - "Curriculum indexes and Quickstart/Operator tracks route to the same Ready lesson without release claims (D-19)."
    - "Only after every Phase 26 GREEN owner and the L23 learning surface land, routine root dispatch executes exactly the five required .mjs contracts through its shared manifest (D-18)."
  artifacts:
    - {path: docs/learning/lessons/L23-guided-setup.md, provides: "Ready guided setup/Pi learning lesson"}
    - {path: docs/learning/CURRICULUM-MAP.md, provides: "Consistent L23 curriculum placement"}
    - {path: docs/learning/tracks/quickstart.md, provides: "Quickstart route to L23"}
    - {path: docs/learning/tracks/operator.md, provides: "Operator route to L23"}
    - {path: scripts/phase26-test-manifest.mjs, provides: "Exact five-entry routine Phase 26 dispatch manifest"}
    - {path: scripts/verify-pi-mcp-bridge.mjs, provides: "Provider-neutral real-Pi minimum/current release acceptance runner with explicit skip and required-release modes"}
  key_links:
    - {from: docs/learning/README.md, to: docs/learning/lessons/L23-guided-setup.md, via: "Ready curriculum index"}
    - {from: docs/learning/tracks/operator.md, to: docs/learning/lessons/L23-guided-setup.md, via: "executable setup/recovery route"}
    - {from: scripts/run-repository-tests.mjs, to: scripts/phase26-test-manifest.mjs, via: "shared routine dispatcher proven against exact recorded spawns"}
    - {from: scripts/verify-pi-mcp-bridge.mjs, to: "real Pi 0.84.1/current plus direct tools/list", via: "required-release full/read-only/custom catalog, metadata/details, call, cancellation, shutdown, and orphan conformance"}
---

<objective>Publish the Phase 26 guided learning path and executable real-Pi acceptance boundary after runtime/operator documentation stabilizes. Purpose: make deterministic setup, Pi operation, recovery, privacy, and authority boundaries teachable and give final release readiness a provider-neutral, non-skippable conformance command. Output: Ready L23, consistent curriculum/track links, and `scripts/verify-pi-mcp-bridge.mjs`.</objective>
<execution_context>@/home/stondo/.codex/gsd-core/workflows/execute-plan.md
@/home/stondo/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/26-guided-setup-and-harness-onboarding/26-CONTEXT.md
@.planning/phases/26-guided-setup-and-harness-onboarding/26-RESEARCH.md
@.planning/phases/26-guided-setup-and-harness-onboarding/26-08-SUMMARY.md
@scripts/test-learning-docs.sh
@docs/learning/lessons/L22-native-windows.md</context>

## Artifacts this phase produces

- New file: `docs/learning/lessons/L23-guided-setup.md`.
- Learning links: L23 entries in learning README, curriculum map, feature guide, Quickstart track, and Operator track.
- Tested teaching contract: Pi 0.84.1 provisional minimum, current-release final gate, and exact metadata/details preservation without a native annotation claim.
- Routine runner activation: the five manifest entries switch from RED-only to routine only after L23 and both required tracks are green.
- Real-Pi release runner: explicit outside-release skip plus required-release execution against supplied Pi 0.84.1 and current executables; it is deliberately outside automatic `test-*` discovery and the exact five-entry Node manifest.

<tasks>
<task type="auto" tdd="true"><name>Task 1: Publish the Ready L23 lesson and curriculum indexes</name><read_first>scripts/test-learning-docs.sh, docs/learning/lessons/L22-native-windows.md, docs/learning/README.md, docs/learning/CURRICULUM-MAP.md, docs/learning/FEATURE-GUIDE.md, docs/harness-compatibility.md</read_first><files>docs/learning/lessons/L23-guided-setup.md, docs/learning/README.md, docs/learning/CURRICULUM-MAP.md, docs/learning/FEATURE-GUIDE.md</files><action>Follow the established lesson template with Outcome, Exercise, Recovery exercise, Common failures, Privacy and trust boundary, and tested version metadata. Cover missing/empty target, deterministic non-TTY replay, Git-less limitations, local Pi stdio verification, explicit machine sync, cancellation/shutdown observation, recovery, and uninstall. State Pi 0.84.1 as the provisional minimum; explain exact annotation preservation in trusted metadata/details per locked D-12 without claiming a native field. Use generic sanitized examples and link the Ready lesson consistently from all curriculum indexes. Keep the routine Phase 26 manifest RED-only until Task 2 completes the required track routing.</action><acceptance_criteria>L23 is reachable from the curriculum indexes, status/sequence agrees everywhere, commands match production, and the Pi/D-12 boundary is exact.</acceptance_criteria><done>L23 is reachable from the curriculum indexes, status/sequence agrees everywhere, commands match production, and the Pi/D-12 boundary is exact.</done><verify><automated>bash scripts/verify-docs-parity.sh &amp;&amp; bash scripts/verify-no-private-references.sh &amp;&amp; node scripts/run-repository-tests.mjs --verify-phase26-registration=red</automated></verify></task>
<task type="auto"><name>Task 2: Route Quickstart and Operator to L23, then activate the complete routine manifest</name><read_first>docs/learning/tracks/quickstart.md, docs/learning/tracks/operator.md, docs/learning/README.md, docs/learning/lessons/L23-guided-setup.md, scripts/phase26-test-manifest.mjs, scripts/run-repository-tests.mjs</read_first><files>docs/learning/tracks/quickstart.md, docs/learning/tracks/operator.md, scripts/phase26-test-manifest.mjs</files><action>Add L23 at the correct sequence point in the Quickstart and Operator tracks per PATTERNS.md, preserving their existing goals and prerequisite order. Keep commands and Ready status identical to the canonical lesson and avoid release/tag claims per D-19. After the lesson, indexes, and both required tracks pass their focused contract, switch all five manifest entries from RED-only to routine. Run the runner's executable registration proof through the same dispatch function with a recording spawn adapter; it must compare the exact setup-preflight, setup-reconcile, setup-compatibility, Pi lifecycle, and bridge-smoke paths and prove each is scheduled once. Then run the first complete root and `check:public` gates of the phase; no earlier plan may claim either complete gate.</action><acceptance_criteria>Quickstart and Operator both reach L23 consistently, executable dispatch proof schedules exactly all five required contracts once, and the first complete `npm test` plus `check:public` gates are green only after all GREEN owners and learning docs have landed.</acceptance_criteria><done>Quickstart and Operator both reach L23 consistently, executable dispatch proof schedules exactly all five required contracts once, and the first complete `npm test` plus `check:public` gates are green only after all GREEN owners and learning docs have landed.</done><verify><automated>bash scripts/test-learning-docs.sh &amp;&amp; node scripts/run-repository-tests.mjs --verify-phase26-registration=routine &amp;&amp; npm test &amp;&amp; npm run check:public</automated></verify></task>
<task type="auto" tdd="true"><name>Task 3: Create the executable real-Pi minimum/current acceptance runner</name><read_first>mcp-memory-server/scripts/smoke-pi-mcp-bridge.mjs, mcp-memory-server/scripts/smoke-mcp-trust.mjs, scripts/test-pi-lifecycle.mjs, pi/extensions/cairnkeep-memory.ts, docs/harness-compatibility.md, .planning/phases/26-guided-setup-and-harness-onboarding/26-VALIDATION.md</read_first><files>scripts/verify-pi-mcp-bridge.mjs</files><behavior>- Default mode prints a sanitized explicit SKIP reason and exits 0 when real-Pi fixtures are unavailable; `--required-release` refuses every skip and fails unless both supplied executables pass.
- Required release accepts explicit `--pi-0-84-1 PATH`, `--pi-current PATH`, and `--pi-current-version VERSION` inputs (with equivalent `CAIRN_PI_0841_BIN`, `CAIRN_PI_CURRENT_BIN`, and `CAIRN_PI_CURRENT_VERSION` variables), verifies the reported versions, and requires distinct minimum/current executable paths. The explicit current version may equal 0.84.1 while that remains the registry-current release; when the registry advances, the new exact version remains required.
- For each real Pi executable, direct-versus-Pi full/read-only/custom catalogs match exactly in tool names, order, input/output schemas, and trusted bridge metadata/details annotations; no native-annotation propagation is asserted.
- Each version completes a harmless read call, per-call cancellation without session death, awaited shutdown, and bounded no-orphan-child detection.</behavior><action>Implement a provider-neutral Node acceptance runner outside `scripts/test-*` discovery and do not add it to the exact five-entry Phase 26 manifest. Use a disposable project and isolated Pi roots, fixed executable/argv with `shell: false`, bounded startup/call/shutdown waits, and sanitized machine-readable evidence that contains versions/profile/count/status facts but never executable paths, usernames, hostnames, repository names, environment values, or child output beyond allow-listed diagnostics. Query the package-owned memory server directly for paginated `tools/list` under full, read-only, and custom profiles, then launch each supplied real Pi executable with the installed Cairnkeep extension and an isolated acceptance observer to record the registered catalog and trusted call details. Compare names, stable order, input/output schemas, and exact Phase 21 annotations in trusted bridge metadata/details; explicitly reject any claim or fixture that expects native Pi annotation propagation per locked D-12. Invoke one harmless read-only memory call, cancel a delayed call while proving the session remains usable, request shutdown, and poll bounded process identity/state until no memory-server child remains. Add `--self-test` using two deterministic executable/version fixtures to prove skip semantics, required-release missing-input failure, sanitized evidence, profile mismatch failure, cancellation, shutdown, and orphan detection without substituting for the real release run. The default no-fixture path must emit an explicit `SKIP` record; `--required-release` must never emit SKIP and must return nonzero for a missing fixture, wrong version, mismatch, failed call/cancel/shutdown, or orphan.</action><acceptance_criteria>The runner's self-test proves its control and conformance gates; default no-fixture execution reports an explicit sanitized SKIP; required-release mode is structurally incapable of skipping and requires passing Pi 0.84.1 plus the explicitly versioned current executable across catalogs, exact trusted metadata/details, call, cancellation, shutdown, and orphan checks.</acceptance_criteria><done>The runner's self-test proves its control and conformance gates; default no-fixture execution reports an explicit sanitized SKIP; required-release mode is structurally incapable of skipping and requires passing Pi 0.84.1 plus the explicitly versioned current executable across catalogs, exact trusted metadata/details, call, cancellation, shutdown, and orphan checks.</done><verify><automated>node scripts/verify-pi-mcp-bridge.mjs --self-test &amp;&amp; node scripts/verify-pi-mcp-bridge.mjs &amp;&amp; node scripts/run-repository-tests.mjs --verify-phase26-registration=routine</automated></verify></task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Runtime behavior/evidence to public curriculum | Lessons can overclaim authority, compatibility, or expose private replay details. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-26-04 | Information Disclosure | lesson/indexes | high | mitigate | generic examples and public-reference scan. |
| T-26-10 | Tampering | Pi claims | high | mitigate | exact trusted metadata/details preservation and native-field limitation. |
| T-26-03 | Tampering | uninstall teaching | medium | mitigate | backup-first/user-preservation instructions match tests. |
</threat_model>
<verification>Learning and docs parity pass; Quickstart and Operator route to L23; executable dispatch proof schedules the exact five required contracts; the first complete root/public gates pass only after every GREEN owner and learning surface lands; and the provider-neutral real-Pi runner self-validates while default absence is an explicit sanitized skip.</verification>
<success_criteria>Users can learn, operate, recover, and uninstall setup/Pi through a reachable, tested, privacy-safe curriculum, and final release readiness has an executable non-skipping Pi 0.84.1/current conformance path.</success_criteria>
<output>Create `.planning/phases/26-guided-setup-and-harness-onboarding/26-09-SUMMARY.md` when done.</output>
