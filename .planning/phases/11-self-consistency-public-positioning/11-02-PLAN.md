---
phase: 11-self-consistency-public-positioning
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - "~/PARA/Projects/token-miser (cross-repo working tree — no cairnkeep tracked files touched)"
autonomous: false
requirements: [SC-01]
user_setup:
  - service: local-denylist
    why: "The scrub gate must catch the 147 known employer-name hits before the tree goes public (D-02/D-04). Those literal terms live only in the operator's uncommitted denylist (D-06) — the guard cannot catch them without it."
    env_vars:
      - name: CAIRN_GUARD_DENYLIST
        source: "Operator-created uncommitted file — one literal private/vendor term per line (employer name, private repo names). Required for this plan's scrub gate. Never committed to either repo."
  - service: github
    why: "Publish token-miser as a public repo under the cairnkeep org"
    dashboard_config:
      - task: "Confirm gh is authenticated as a cairnkeep-org member (RESEARCH verified: stondo, member). Re-run `gh auth status` if the publish step reports an auth error."
        location: "GitHub CLI (`gh auth status`)"

must_haves:
  truths:
    - "github.com/cairnkeep/token-miser exists and is PUBLIC (`gh repo view cairnkeep/token-miser --json visibility` → PUBLIC)"
    - "The published tree carries no employer/private-repo/vendor reference — the guard (with CAIRN_GUARD_DENYLIST set) exits 0 against the trimmed tree before push"
    - "The published tree is a single clean commit with no private history (re-init `git init`, not a transfer/history-rewrite) — D-01"
    - "The trimmed public tree builds standalone (`cargo build` succeeds) — D-04"
    - "The published tree contains src/ + Cargo.toml + Cargo.lock + scrubbed README/QUICKSTART/docs + neutrally-renamed example config(s) + Apache-2.0 LICENSE, and omits .ai/, .planning/, bench/, CLAUDE.md, AGENTS.md, .gitlab-ci.yml, .github/, target/ — D-04"
  artifacts:
    - "github.com/cairnkeep/token-miser (public GitHub repo)"
    - "LICENSE in the token-miser publish tree (Apache-2.0, copied verbatim from cairnkeep's LICENSE, copyright line updated)"
    - "A neutrally-named example config replacing config.enterprise.example.toml in the publish tree"
  key_links:
    - "the guard from Plan 01 gates the publish tree — CAIRN_GUARD_DENYLIST MUST be set so the specific-term stage catches employer literals; a generic-only run would pass a tree still carrying employer names"
    - "the human-verify checkpoint sits between the zero-hit/clean-build gate and the irreversible public push"
---

<objective>
Publish token-miser as a public cairnkeep-org sibling (SC-01 prerequisite,
D-01/D-02/D-03/D-04). Clean-slate: scrub the working tree to a trimmed core, add
Apache-2.0, re-init history from scratch (no private history ever pushed), and
create github.com/cairnkeep/token-miser as PUBLIC. This is a cross-repo
operation on ~/PARA/Projects/token-miser — it touches zero cairnkeep tracked
files.

Purpose: make the docs' "public sibling" claim (Plan 03) simply true, and give
the routing/explore wires a real public dependency instead of an implied private
one. Sequencing is LOCKED: the Plan 01 guard must exist first (it gates this
tree); cairnkeep docs land after (they link this repo).
Output: a live public repo, verified PUBLIC, that passed the guard zero-hit and
`cargo build` before its first push.
</objective>

<execution_context>
@/home/stondo/PARA/Projects/cairnkeep/.claude/gsd-core/workflows/execute-plan.md
@/home/stondo/PARA/Projects/cairnkeep/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-self-consistency-public-positioning/11-CONTEXT.md
@.planning/phases/11-self-consistency-public-positioning/11-RESEARCH.md
@.planning/phases/11-self-consistency-public-positioning/11-PATTERNS.md
@LICENSE
@scripts/verify-no-private-references.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scrub, trim, re-init, and gate the token-miser publish tree</name>
  <files>~/PARA/Projects/token-miser (cross-repo working tree)</files>
  <read_first>
    - .planning/phases/11-self-consistency-public-positioning/11-CONTEXT.md (D-01, D-02, D-03, D-04 — the exact publish contract and trimmed-tree set)
    - .planning/phases/11-self-consistency-public-positioning/11-RESEARCH.md (Architecture Pattern 3 recipe; Runtime State Inventory; Anti-Patterns to Avoid; Assumptions A1/A2)
    - .planning/phases/11-self-consistency-public-positioning/11-PATTERNS.md (cross-repo publish section — the verified git/gh recipe)
    - LICENSE (this repo's Apache-2.0 text — the verbatim copy source for token-miser's LICENSE, update only the copyright line)
    - scripts/verify-no-private-references.sh (the guard this task runs against the trimmed tree)
    - ~/PARA/Projects/token-miser/README.md, QUICKSTART.md, docs/, config.enterprise.example.toml, config.local.toml, config.hybrid.toml, config.toml (the scrub targets — 147 employer hits across 26 of 76 tracked files, 0 in src/)
  </read_first>
  <action>
    Operate in ~/PARA/Projects/token-miser. Do NOT push in this task.

    1. Trim to the D-04 publish set: keep src/, Cargo.toml, Cargo.lock, scrubbed
       README.md/QUICKSTART.md/docs/, and example config(s). Remove .ai/,
       .planning/, bench/, CLAUDE.md, AGENTS.md, .gitlab-ci.yml, .github/, and
       target/ from the tree that will be committed.
    2. Add LICENSE: copy this repo's LICENSE verbatim, updating only the
       copyright line to token-miser's (Apache-2.0, "Copyright 2026 Stefano Tondo").
    3. Neutral-rename config.enterprise.example.toml to a provider-neutral name
       (e.g. config.enterprise.example.toml or config.remote.example.toml — your
       discretion) and scrub every employer/vendor/internal-host literal out of
       ALL kept configs and docs, replacing with provider-neutral wording ("any
       OpenAI-compatible endpoint"-style, per CONTEXT Claude's Discretion).
       Employer-specific values move to a private overlay per D-03 — they do not
       ship in the public tree.
    4. Re-init clean history (D-01): remove the existing .git, `git init`,
       `git add -A`, and commit with a plain message and no AI-authorship trailer
       (DEC-no-ai-authorship). The private stondo/token-miser repo stays an
       untouched private archive (D-03) — do not modify or delete it. Do NOT use
       filter-repo/BFG history rewrite (rejected, D-01 anti-pattern).
    5. Gate the tree, from inside the token-miser dir, with CAIRN_GUARD_DENYLIST
       set to the operator's denylist (so employer literals are caught): run
       cairnkeep's scripts/verify-no-private-references.sh against the fresh
       committed tree — it must exit 0 (zero hits). If it flags anything, scrub
       and re-commit until zero. Then `cargo build` — it must succeed standalone.

    Capture the guard's zero-hit output and the `cargo build` success for the
    checkpoint. Note whether `gh repo view cairnkeep/token-miser` already exists
    (handle as data for Task 3, not an error here).
  </action>
  <verify>
    <automated>cd ~/PARA/Projects/token-miser && test -n "${CAIRN_GUARD_DENYLIST:-}" && bash /home/stondo/PARA/Projects/cairnkeep/scripts/verify-no-private-references.sh && cargo build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - The trimmed committed tree omits .ai/, .planning/, bench/, CLAUDE.md, AGENTS.md, .gitlab-ci.yml, .github/, target/ (`git ls-files` in token-miser shows none of these)
    - `git ls-files` includes src/, Cargo.toml, Cargo.lock, LICENSE, a neutrally-named example config, and scrubbed README/QUICKSTART/docs
    - No file named `config.enterprise.example.toml` remains tracked; a provider-neutral example config exists instead
    - token-miser's `.git` is a fresh single-commit history (`git rev-list --count HEAD` == 1) with no AI-authorship trailer in the commit message
    - `verify-no-private-references.sh` (with CAIRN_GUARD_DENYLIST set) exits 0 against the trimmed token-miser tree
    - `cargo build` succeeds in the trimmed tree
    - LICENSE is present and byte-identical to cairnkeep's LICENSE except the copyright line
  </acceptance_criteria>
  <done>The trimmed token-miser tree is scrubbed, licensed, re-init'd to clean history, passes the guard zero-hit with the specific denylist set, and builds — staged and ready for the pre-push confirmation, not yet public.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Pre-push confirmation (irreversible public push gate)</name>
  <read_first>
    - ~/PARA/Projects/token-miser `git ls-files` output (the exact tracked set about to go public)
    - scripts/verify-no-private-references.sh (the guard the operator re-runs)
    - a spot-check sample of the scrubbed token-miser README/QUICKSTART/docs and the neutrally-renamed config
  </read_first>
  <action>Blocking human-verify gate before the irreversible public push. Present the operator with the trimmed file set, the guard's zero-hit output, and the `cargo build` result; do not run Task 3 (the push) until the operator types "approved". This gate exists because publishing is irreversible (once public, a leak is a leak) — the automated guard is necessary but not sufficient for a first-ever public release (T-11-03/T-11-04).</action>
  <what-built>A trimmed, scrubbed, Apache-2.0-licensed, clean-history token-miser tree that passed the no-private-references guard (specific denylist set → zero hits) and `cargo build`, staged locally but NOT yet pushed.</what-built>
  <how-to-verify>
    Publishing is irreversible (once public, a leak is a leak). Before the push,
    confirm:
    1. Review the tracked file set: `cd ~/PARA/Projects/token-miser && git ls-files` — confirm no .ai/, .planning/, bench/, CLAUDE.md, AGENTS.md, .gitlab-ci.yml, .github/, target/, and no `config.enterprise.example.toml`.
    2. Re-run the guard yourself: `CAIRN_GUARD_DENYLIST=<your denylist> bash /home/stondo/PARA/Projects/cairnkeep/scripts/verify-no-private-references.sh` from the token-miser dir — expect exit 0, zero hits.
    3. Spot-check a couple of scrubbed docs/configs for any remaining employer/vendor/internal-host name the denylist might not cover.
    4. Confirm `cargo build` succeeded and `git rev-list --count HEAD` is 1.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize the public push, or describe what still needs scrubbing.</resume-signal>
  <acceptance_criteria>
    - Operator has reviewed `git ls-files` and confirmed no dropped path (.ai/, .planning/, bench/, CLAUDE.md, AGENTS.md, .gitlab-ci.yml, .github/, target/) and no config.enterprise.example.toml remains
    - Operator has re-run the guard (denylist set) and seen exit 0 / zero hits
    - Operator confirmed `cargo build` succeeded and `git rev-list --count HEAD` == 1
    - Operator typed "approved" (or the plan halts with the requested scrub fixes)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Publish public and verify PUBLIC</name>
  <files>~/PARA/Projects/token-miser (cross-repo working tree)</files>
  <read_first>
    - .planning/phases/11-self-consistency-public-positioning/11-RESEARCH.md (Code Examples "Cross-repo publish verification triad"; Common Pitfall 3 — the already-exists branch; Assumption A1 on the exact `gh` invocation)
    - .planning/phases/11-self-consistency-public-positioning/11-PATTERNS.md (cross-repo publish recipe)
  </read_first>
  <action>
    Only after the checkpoint is approved. From ~/PARA/Projects/token-miser:

    First re-check live state (Pitfall 3): `gh repo view cairnkeep/token-miser --json visibility`.
    - If it errors "not found" → create + push: `gh repo create cairnkeep/token-miser --public --source=. --push`. Confirm the exact flag syntax against `gh repo create --help` at runtime (Assumption A1) before running.
    - If it already exists → do NOT recreate; push the clean-history tree to it and verify visibility is PUBLIC (flip it public via `gh repo edit cairnkeep/token-miser --visibility public` only if it is private and you own it).

    Then verify PUBLIC: `gh repo view cairnkeep/token-miser --json visibility` must report PUBLIC. Record the command, the trimmed-tree guard output, the `cargo build` result, and the PUBLIC verdict in the SUMMARY (this is the SC-01 evidence triad from CONTEXT "Specific Ideas").
  </action>
  <verify>
    <automated>gh repo view cairnkeep/token-miser --json visibility</automated>
  </verify>
  <acceptance_criteria>
    - `gh repo view cairnkeep/token-miser --json visibility` returns `"visibility":"PUBLIC"`
    - The pushed history has exactly one commit (no private history carried over)
    - The SUMMARY records the three-part evidence: guard zero-hit output, `cargo build` success, and the PUBLIC verdict
  </acceptance_criteria>
  <done>github.com/cairnkeep/token-miser is live, public, single-clean-commit — the docs (Plan 03) can now link a repo that exists.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (introduced by this plan)

- `github.com/cairnkeep/token-miser` — new public GitHub repository (cross-repo)
- `LICENSE` (Apache-2.0) added to the token-miser publish tree
- A provider-neutral example config replacing `config.enterprise.example.toml` in
  the token-miser publish tree
- No new cairnkeep symbols, files, or env vars — this plan touches zero cairnkeep
  tracked files
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local scrubbed tree → public GitHub | The push is an irreversible disclosure boundary — anything in the committed tree/history becomes world-readable |
| operator denylist → scrub gate | Determines whether employer/private-repo literals are caught before the boundary is crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-03 | Information Disclosure | private git history leaked via publish | critical | mitigate | Clean-slate `git init` (D-01) — no prior history carried; acceptance asserts `git rev-list --count HEAD` == 1; filter-repo/BFG rewrite explicitly forbidden |
| T-11-04 | Information Disclosure | residual employer/private literal in the trimmed tree goes public | critical | mitigate | Guard with CAIRN_GUARD_DENYLIST set → zero-hit gate BEFORE push, plus a blocking-human checkpoint between the gate and the irreversible push |
| T-11-05 | Information Disclosure | denylist file itself accidentally committed into the publish tree | high | mitigate | Denylist lives outside the repo (env-referenced, D-06); the guard scans the tree and would flag it; checkpoint reviews `git ls-files` |
| T-11-SC | Tampering | npm/pip/cargo installs | low | accept | N/A — no new packages; token-miser's existing Cargo.toml/Cargo.lock are published unchanged (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- token-miser `git ls-files` shows the D-04 trimmed set only; no dropped paths remain; no `config.enterprise.example.toml`
- Guard (denylist set) exits 0 on the trimmed tree; `cargo build` succeeds; `git rev-list --count HEAD` == 1
- Human-verify checkpoint approved before push
- `gh repo view cairnkeep/token-miser --json visibility` → PUBLIC
</verification>

<success_criteria>
token-miser is published PUBLIC under cairnkeep with clean history, an Apache-2.0
LICENSE, and a scrubbed provider-neutral tree that passed the guard and built —
SC-01's real-world prerequisite is met and the docs can link it.
</success_criteria>

<output>
Create `.planning/phases/11-self-consistency-public-positioning/11-02-SUMMARY.md` when done.
</output>
