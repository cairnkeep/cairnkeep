---
phase: 06-fastcontext-reliability-spike
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - .planning/phases/06-fastcontext-reliability-spike/06-SPIKE.md
autonomous: false
requirements: [CTX-06]
tags: [fastcontext, reliability, verdict, spike, go-no-go, live-probe, operator-gated]

user_setup:
  - service: fastcontext-llama-server
    why: "The go/no-go verdict requires a LIVE run against the actually-deployed FastContext GGUF + `llama-server --jinja` endpoint (D-03/D-07). Claude cannot stand up a GPU model server on the operator's private infra; per DEC-no-private-references no host/IP is committed. RESEARCH confirms `llama-server` is absent from PATH as of 2026-07-04 — bringing it up is the operator's first real step."
    dashboard_config:
      - task: "Stand up llama-server with the exact validated recipe recorded in 06-RESEARCH.md finding #4: `llama-server -m <fastcontext-1.0-4b-*-q8_0.gguf> --alias <alias> -ngl 99 -c 24576 --jinja --host <loopback> --port 8081 --flash-attn on --cache-type-k q4_0 --cache-type-v q4_0`. Pass `--jinja` explicitly regardless of the build default (RESEARCH State of the Art). If it cannot be stood up at all, that is itself a documented no-go (D-08), not a silent skip."
        location: "Operator's local inference infra (private; never committed here)"
    env_vars:
      - name: FASTCONTEXT_PROBE_URL
        source: "Ambient shell / gitignored .ai/.env — the loopback base URL of the operator's llama-server (e.g. http://127.0.0.1:8081/v1). Never committed (D-02, DEC-no-private-references)."

must_haves:
  truths:
    - "The operator stands up the FastContext `llama-server --jinja` endpoint with the recorded recipe and runs `scripts/verify-fastcontext-reliability.sh --full` against it — OR, if the server cannot be stood up at all, that failure is itself captured as a documented no-go blocker (D-08), never a silent skip."
    - "A live `--full` run records the observed finish_reason for every turn across ≥5 prompts × ≥3 turns (≥15 total) and the raw `GET /props` payload (chat_template_tool_use present-or-absent, chat_template_caps, build_info, chat_template) — satisfying ROADMAP SC#1 and SC#2."
    - "The go/no-go verdict is anchored to gate #2 (every turn tool_calls + a well-formed call = go; any narration turn = no-go, D-06); gate #1 (chat_template_tool_use presence) is recorded EVIDENCE and does NOT by itself force a no-go (refined D-05 — the operator's in-session 'Evidence, not hard gate' decision)."
    - "06-SPIKE.md pins the exact combination probed (D-08): mitkox repo + quant as reported by /props, llama.cpp build_info, `--jinja`, and the chat-template excerpt — so the verdict is reproducible when the deployed combo changes."
  artifacts:
    - ".planning/phases/06-fastcontext-reliability-spike/06-SPIKE.md — the committed documented go/no-go verdict (ROADMAP SC#3) with the D-08 pinned combination and scrubbed evidence excerpts."
  key_links:
    - "The verdict MUST NOT auto-NO-GO solely because chat_template_tool_use is absent — the single most important refinement, grounded in 06-RESEARCH.md finding #1/#4 (absence is architectural for this Qwen3-family GGUF, not misconfiguration)."
    - "06-SPIKE.md is scrubbed of the private endpoint URL and any secret before commit (DEC-no-private-references); the raw 06-EVIDENCE.log stays gitignored (matched by the existing `*.log` rule)."
  prohibitions:
    - "DEC-no-private-references [LOCKED]: no host/IP/vendor endpoint or secret in the committed 06-SPIKE.md; only a loopback-shaped or redacted endpoint reference."
    - "DEC-no-ai-authorship [LOCKED]: no AI/assistant authorship references in the verdict doc or commit messages."
    - "Do NOT reinterpret the verdict toward a GO on narrated (content-only) turns, nor toward a NO-GO purely on gate-1 field absence — the verdict is the JSON-structure evidence from gate #2 (RESEARCH Anti-Patterns)."
---

<objective>
Run the probe from Plan 06-01 live against the actually-deployed FastContext GGUF + `llama-server --jinja` endpoint, and record the documented go/no-go verdict that gates Phases 7-9 (ROADMAP SC#1-3, CTX-06). Standing up the server is an operator-provided runtime prerequisite (D-07): per 06-RESEARCH.md `llama-server` is absent from PATH and no host/IP is committed (DEC-no-private-references), so a `checkpoint:human-verify` hands the live run to the operator, who supplies the endpoint via FASTCONTEXT_PROBE_URL and reports the observed result.

The verdict is scored per the operator's in-session refinement of D-05 ("Evidence, not hard gate"): anchored to the empirical per-turn gate #2, with gate #1 (`chat_template_tool_use` presence) recorded as evidence — never an auto-blocker. A server that cannot be stood up at all is itself a documented no-go (D-08), never a silent skip.

Purpose: establish the empirically-grounded go/no-go that de-risks the OCP-04 failure class before any Phase 7-9 code depends on FastContext.
Output: `.planning/phases/06-fastcontext-reliability-spike/06-SPIKE.md` — the committed verdict + D-08 pinned combination + scrubbed evidence.
</objective>

<d05_refinement>
## D-05 scoring — the operator's in-session "Evidence, not hard gate" decision (verdict rubric)

Read literally, D-05 gate #1 ("`chat_template_tool_use` is present") would force a NO-GO for the pinned FastContext GGUF even if it invokes tools reliably every turn, because 06-RESEARCH.md finding #1/#4 established (source-verified) the field is ABSENT by architecture — a single unified Qwen3-family ChatML template with an inline `{%- if tools %}` / `<tool_call>` XML block, not a separately-named `tool_use` variant.

The operator resolved this in-session:
- The probe records `/props` verbatim (satisfies ROADMAP SC#2).
- The VERDICT is anchored to gate #2 (≥15 turns, every one `finish_reason == "tool_calls"` with a well-formed call).
- `chat_template_tool_use` absence is a **recorded caveat, not an automatic blocker**.

06-SPIKE.md MUST state this rubric explicitly and apply it, so the executor, the verifier, and a future maintainer all see how "go" was defined. The checkpoint below asks the operator to confirm the recorded rubric matches this decision before the verdict is finalized (per 06-RESEARCH.md Open Question #1's "surface for explicit confirmation" recommendation).
</d05_refinement>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-fastcontext-reliability-spike/06-CONTEXT.md
@.planning/phases/06-fastcontext-reliability-spike/06-RESEARCH.md
@.planning/phases/06-fastcontext-reliability-spike/06-VALIDATION.md
@.planning/phases/06-fastcontext-reliability-spike/06-01-SUMMARY.md
@scripts/verify-fastcontext-reliability.sh
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Operator live probe — stand up FastContext server, run --full, report evidence</name>
  <action>
    Operator-only step (Claude cannot reach the private infra). The operator stands up the FastContext `llama-server --jinja` endpoint (D-07), exports FASTCONTEXT_PROBE_URL (loopback/env, never committed — D-02), runs `scripts/verify-fastcontext-reliability.sh --props-only` then `--full` against it, and reports the observed evidence (build_info, chat_template_tool_use present/absent, the ≥15-turn pass/fail tally, `--full` exit code) — OR reports that the server could not be stood up at all, which is itself a documented no-go (D-08). See <how-to-verify> for the exact steps and the recipe. Do not finalize the verdict until the operator confirms the recorded rubric matches their in-session "Evidence, not hard gate" decision.
  </action>
  <what-built>
    scripts/verify-fastcontext-reliability.sh (Plan 06-01) — the committed bash+curl+jq probe with `--props-only`, `--full`, `--self-test`, and `-h`. Its offline `--self-test` already passed in 06-01 (fixtures: PASS, narration-FAIL, all-PASS->GO, one-narration->NO-GO, absent-field+all-PASS->GO). What remains is the LIVE run against the operator's endpoint — the only part Claude cannot perform (private infra, no committed host/IP).
  </what-built>
  <how-to-verify>
    1. Stand up the FastContext server (D-07) with the exact validated recipe in 06-RESEARCH.md finding #4:
       `llama-server -m <fastcontext-1.0-4b-*-q8_0.gguf> --alias <alias> -ngl 99 -c 24576 --jinja --host <loopback> --port 8081 --flash-attn on --cache-type-k q4_0 --cache-type-v q4_0`.
       Pass `--jinja` explicitly regardless of the build default.
       - If the server CANNOT be stood up at all: record that fact (what was attempted, what failed) — this is a documented NO-GO blocker per D-08, not a skip. Report it at the resume signal and stop.
    2. Export the endpoint (never committed): `export FASTCONTEXT_PROBE_URL=http://<loopback>:8081/v1` (from the ambient shell or gitignored .ai/.env, D-02).
    3. Fast pre-flight (gate #1): run `scripts/verify-fastcontext-reliability.sh --props-only`. Confirm it dumps `/props` and records chat_template_tool_use as PRESENT or ABSENT (ABSENT is expected and fine — refined D-05).
    4. Full run (gate #2): run `scripts/verify-fastcontext-reliability.sh --full`. It executes the ≥5-prompt × ≥3-turn matrix and writes raw evidence to 06-EVIDENCE.log (gitignored). Note the exit code (0 = GO, non-zero = NO-GO / unreachable) and the per-turn PASS/FAIL tally.
    5. Confirm the verdict rubric matches your in-session "Evidence, not hard gate" decision: gate #2 is the anchor; chat_template_tool_use absence alone must NOT force a NO-GO.
    6. At the resume signal, report: (a) build_info + the quant/alias /props reported; (b) chat_template_tool_use PRESENT/ABSENT; (c) the ≥15-turn pass/fail tally and the `--full` exit code; (d) any narration turns' raw arguments; OR (e) "server could not be stood up" with the failure detail.
  </how-to-verify>
  <resume-signal>Paste the `--props-only` and `--full` evidence summary (build_info, chat_template_tool_use present/absent, per-turn tally, exit code) OR report "server un-standable + reason", then type "recorded".</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write the go/no-go verdict artifact 06-SPIKE.md</name>
  <files>.planning/phases/06-fastcontext-reliability-spike/06-SPIKE.md</files>
  <read_first>
    - .planning/phases/06-fastcontext-reliability-spike/06-RESEARCH.md § Summary + finding #1/#4 (the refined-D-05 rationale to cite) and § "Anti-Patterns to Avoid" (score on JSON structure, never narrated content)
    - This plan's <d05_refinement> block (the rubric the verdict must state and apply)
    - The operator's evidence from the checkpoint above (build_info, chat_template_tool_use present/absent, per-turn tally, exit code, or the un-standable-server report) and the raw 06-EVIDENCE.log (gitignored)
    - .planning/ROADMAP.md § "Phase 6" (the three success criteria the verdict must satisfy)
  </read_first>
  <action>
    Write `.planning/phases/06-fastcontext-reliability-spike/06-SPIKE.md` — the committed go/no-go verdict (ROADMAP SC#3) from the checkpoint's evidence. Include:

    - Verdict header: GO ("reliably invokes tools — safe to build Phase 7 on") or NO-GO ("narrates instead of invoking — a hard blocker requiring remediation before proceeding") or NO-GO/BLOCKED ("server could not be stood up" — D-08). Never a silent assumption either way.
    - The refined-D-05 rubric verbatim from this plan's <d05_refinement>: gate #2 is the anchor; gate #1 (chat_template_tool_use presence) is recorded evidence and absence alone is NOT a blocker. State that the operator confirmed this rubric at the checkpoint.
    - D-08 pinned combination: mitkox repo + quant/alias as reported by /props, llama.cpp build_info, `--jinja` on, and a short chat_template excerpt — so the verdict is reproducible.
    - Gate #1 evidence: chat_template_tool_use PRESENT or ABSENT (+ chat_template_caps if captured), with the one-line explanation that absence is architectural for this Qwen3-family GGUF (cite 06-RESEARCH.md finding #1), not misconfiguration.
    - Gate #2 evidence: the ≥5-prompt × ≥3-turn (≥15-turn) per-turn PASS/FAIL tally and the `--full` exit code. If any turn narrated, quote its finish_reason + the raw arguments.
    - Forward note for Phase 7: any path/pattern signal from the recorded tool-call arguments (the Docker-mount-path quirk from RESEARCH/PITFALLS is a Phase 7 concern, recorded here, not gated on now); and the D-04 token_miser corroboration outcome (run or skipped-absent).
    - Re-runnability note: to re-establish the verdict when the deployed combo changes, re-run `scripts/verify-fastcontext-reliability.sh --full` (D-01 rationale).

    Scrub the endpoint URL and any secret from 06-SPIKE.md before committing (only a loopback-shaped or redacted reference); the raw 06-EVIDENCE.log stays gitignored. No AI-authorship references (DEC-no-ai-authorship).
  </action>
  <verify>
    <automated>test -f .planning/phases/06-fastcontext-reliability-spike/06-SPIKE.md && grep -Eiq 'GO|NO-GO' .planning/phases/06-fastcontext-reliability-spike/06-SPIKE.md && grep -q 'chat_template_tool_use' .planning/phases/06-fastcontext-reliability-spike/06-SPIKE.md && grep -q 'build_info' .planning/phases/06-fastcontext-reliability-spike/06-SPIKE.md && ! grep -nE '([0-9]{1,3}\.){3}[0-9]{1,3}' .planning/phases/06-fastcontext-reliability-spike/06-SPIKE.md | grep -qv '127\.0\.0\.1' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 06-SPIKE.md exists and states an explicit verdict token: GO, NO-GO, or NO-GO/BLOCKED (never silent).
    - It records the refined-D-05 rubric (gate #2 anchor; gate #1 absence not a blocker) and states the operator confirmed it.
    - It pins the D-08 combination: quant/alias, build_info, `--jinja`, chat_template excerpt.
    - It records gate #1 (chat_template_tool_use present/absent) and the gate #2 per-turn tally + exit code.
    - No non-loopback IPv4/host or secret appears in the committed doc (the automated grep for a non-loopback IPv4 finds none).
  </acceptance_criteria>
  <done>
    A committed 06-SPIKE.md carries an explicit go/no-go verdict scored by the refined-D-05 rubric, the D-08 pinned combination, the recorded gate #1 evidence and gate #2 per-turn tally, and a Phase 7 forward note — scrubbed of any private endpoint/secret. ROADMAP SC#1-3 are satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator infra (llama-server) → probe | Network boundary; endpoint supplied via FASTCONTEXT_PROBE_URL (loopback/env), model output only asserted-on, never executed |
| runtime evidence (raw /props + responses) → committed verdict (06-SPIKE.md) | The private endpoint URL / secret must not cross into git; only scrubbed excerpts are committed, raw evidence stays in the gitignored `*.log` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Information Disclosure | committed 06-SPIKE.md | high | mitigate | Verdict embeds only scrubbed excerpts; endpoint URL/secret redacted to a loopback-shaped/redacted reference; raw evidence stays in the gitignored 06-EVIDENCE.log; automated grep for a non-loopback IPv4 must find none (DEC-commit-scanning) |
| T-06-06 | Spoofing (false GO / false NO-GO) | verdict scoring | high | mitigate | Verdict anchored to gate #2 JSON-structure evidence, never narrated content; the refined-D-05 rubric prevents both a false GO on narrated turns and a false NO-GO from gate-1 field absence |
| T-06-SC | Tampering (supply chain) | package installs | low | accept | No npm/pip/cargo installs this phase; only the operator-managed `llama-server` binary (out of package-audit scope, like git/ssh) and the already-committed probe; Package Legitimacy Audit N/A per 06-RESEARCH.md |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces (this plan)

- `.planning/phases/06-fastcontext-reliability-spike/06-SPIKE.md` — the committed go/no-go verdict (NEW). Carries the D-08 pinned combination + scrubbed evidence excerpts; no private host/IP/secret.
- `.planning/phases/06-fastcontext-reliability-spike/06-EVIDENCE.log` — raw live-run evidence written by the probe at runtime (NEW, gitignored via the existing `*.log` rule). Not committed; the operator's local record the verdict cites.

No new CLI flags (the probe's flags are introduced in Plan 06-01).
</artifacts_this_phase_produces>

<verification>
- The checkpoint's live evidence (build_info, chat_template_tool_use present/absent, ≥15-turn tally, `--full` exit code) OR an un-standable-server report is recorded.
- 06-SPIKE.md exists, states an explicit GO / NO-GO / NO-GO-BLOCKED verdict, records the refined-D-05 rubric, pins the D-08 combination, and captures gate #1 + gate #2 evidence.
- Automated grep confirms no non-loopback IPv4 and no secret in the committed 06-SPIKE.md; 06-EVIDENCE.log remains gitignored.
- ROADMAP SC#1 (finish_reason recorded every turn), SC#2 (/props checked), SC#3 (documented verdict exists) are all satisfied.
</verification>

<success_criteria>
- A documented go/no-go verdict exists in the phase artifacts (06-SPIKE.md), never a silent assumption (ROADMAP SC#3, D-08).
- The verdict is anchored to the empirical per-turn gate #2 and records gate #1 as evidence per the refined D-05 (operator-confirmed).
- The exact probed combination is pinned and reproducible (D-08); no private endpoint/secret is committed.
</success_criteria>

<output>
Create `.planning/phases/06-fastcontext-reliability-spike/06-02-SUMMARY.md` when done. Record the final verdict (GO / NO-GO / NO-GO-BLOCKED), the pinned combination, whether chat_template_tool_use was present or absent, the gate #2 tally, and confirmation that 06-SPIKE.md is scrubbed of any private endpoint/secret — so Phase 7 planning can gate on this verdict directly.
</output>
